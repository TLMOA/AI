**结论**
- ✅ 今天完成了前端关于 Hadoop（Hive/HDFS/HBase）支持的 UI 迭代与若干修复；后端新增了基于 `pyhive` 的 Hive 导出最小实现并更新依赖。

**当前进度**
- ✅ 前端：
  - 在 [v1-frontend/index.html](v1-frontend/index.html#L1) 添加 `Hive/HDFS/HBase` 选项并拆分了 Database/User 行，避免隐藏联动问题。
  - 在 [v1-frontend/app.js](v1-frontend/app.js#L1) 实现按 `dbType` 动态渲染字段，新增 `hbaseRowKeyPrefix`、HDFS 路径标签切换，并修复 HDFS 模式下的 `password` 显示问题。
- ✅ 后端：
  - 在 [v1-backend/app/export_worker.py](v1-backend/app/export_worker.py#L1) 增加 `_export_from_hive()` 最小实现（使用 `pyhive` 做全表导出，避免改动关系型增量逻辑）。
  - 更新 [v1-backend/requirements.txt](v1-backend/requirements.txt#L1)，加入 `pyhive` 与 `thrift` 依赖。
- ⚙️ 环境验证：
  - 在本地虚拟环境中安装依赖过程中发现 `sasl` 编译问题（因使用 Conda Python 3.13 与系统头文件不匹配）。为解决该问题进行了依赖与环境调整尝试，并建议使用 Conda 新建 Python 3.10 环境来安装二进制包；你已授权我创建该环境的尝试并执行初步操作（部分命令在当前环境下失败，后续按计划可创建新 conda env）。

**遇到的问题与风险**
- ⚠️ `sasl` C 扩展在当前 Conda Python 3.13 环境下无预编译包，源码编译缺少 CPython 匹配头文件（longintrepr.h），导致无法在该环境下用 pip 成功安装。已提供两条可行方案（新建 conda env 或使用系统 Python venv）。
- ⚠️ Hive 集群若启用 Kerberos/SASL/SSL，需要额外配置（keytab / krb5.conf / thrift-sasl），目前后端仅实现最小连通路径，不包含 Kerberos 集成。

**下一步（优先级排序）**
- 1️⃣ （优先）验证并在兼容 Python 环境中完成 `pyhive` + `sasl` 的联通性测试，修复任何连接或认证问题。（我已建议使用 conda 环境 `python=3.10`）
- 2️⃣ 在后端补充 HDFS（文件/Parquet/ORC 转换）与 HBase（Thrift/scan）的导出实现，并加入相应的测试。
- 3️⃣ 为 Hive 导出添加增量导出支持（需要确定增量键策略）并补充单元/集成测试。
- 4️⃣ 更新文档：README 与导出功能使用说明，补充 Hadoop 相关认证与网络要求说明。

**产出清单**
- 修改/新增文件：
  - [v1-frontend/index.html](v1-frontend/index.html#L1)
  - [v1-frontend/app.js](v1-frontend/app.js#L1)
  - [v1-backend/app/export_worker.py](v1-backend/app/export_worker.py#L1)
  - [v1-backend/requirements.txt](v1-backend/requirements.txt#L1)
  - 本日报：每日工作进度/2026-04-22.md

**请求/决定点**
- 请确认是否同意我接下来：
  - A. 在新的 conda 环境（python=3.10）继续完成 `pyhive` 联通性测试并修复（推荐）；
  - B. 或切回系统 Python venv 完成安装与测试。

*报告人：xy*
*日期：2026-04-22*
