**结论**
- ✅ 今天定位并部分解决两个主要问题：
  - 后端被用户会话中启动的 `uvicorn` 占用 127.0.0.1:8081，导致 systemd 管理的实例无法绑定端口（已终止占用进程，端口已释放）。
  - 登录依然报“用户名或密码错误”属于密码校验不匹配（DB 可访问且 `nifi.iot_users` 中存在 bcrypt hash），不是 DB 不可达问题。

**当前进度**
- ✅ 已收集并确认今日代码/配置变更（见下清单）。
- ✅ 已识别并优雅终止占用 8081 的用户级 `uvicorn` 进程（用户会话会自动重启该进程的原因已定位为用户级 systemd 单元 invocation:iot-backend.service）。
- ✅ 已在 NiFi MySQL 中创建并验证测试用户（确认注册写入 `iot_users`，并能用相同密码登录）。
- ⏳ 尚未由 root 完成 `systemd` system 单元的重启与接管（需 sudo 权限重启并验证）。

**今日工作区变更（事实清单）**
- Git 未提交/已修改文件（`git status --porcelain` 列出）：
  - M README.md
  - M V2.../V1-NiFi统一实施可执行方案.md
  - M v1-backend/app/main.py
  - M v1-backend/deploy/iot-backend.service.override.conf
  - M v1-frontend/app.js
  - M v1-frontend/config.js
  - 若干未跟踪项（`backup_sqlite/`, `v1-backend/app/nifi_orchestrator.py`, 若干 docs）

- 今日在工作区新修改或新建的文件（按修改时间查找）：
  - v1-frontend/config.js
  - v1-frontend/app.js
  - v1-backend/internal/或其它（详见仓库 `git status` 与文件时间戳）

- 运行/运维动作（命令与结果摘要）：
  - 查看到 127.0.0.1:8081 被用户会话的 `python -m uvicorn app.main:app`（PID 示例：450258 / 537306）监听。
  - 已向该进程发送 `SIGTERM`（必要时 `SIGKILL`），端口释放。
  - 在用户 runtime 目录发现 `/run/user/1000/systemd/units` 中存在 `invocation:iot-backend.service`，说明是用户级 systemd 启动。
  - 使用 `curl` 测试：向 `POST /api/v1/auth/register` 注册测试用户返回 200，随后 `POST /api/v1/auth/login` 返回 200（但注意返回的 `username` 字段在某次测试显示为空串，需核查前端/后端返回格式）。
  - 在 MySQL `nifi.iot_users` 中确认 `admin` 与多条用户记录，密码以 bcrypt (`$2b$...`) 保存。

**进行中 / 待做（优先级）**
- 高：由 root 重启并让 systemd system 单元接管后端（避免用户会话再次占端口）：
  - `sudo systemctl daemon-reload && sudo systemctl restart iot-backend.service`，然后验证 `ss -ltnp | grep :8081` 与 `systemctl status`。
- 高：禁用/删除用户级自动启动源（避免重复）：
  - 以用户身份停止并 disable 用户单元：
    ```bash
    sudo -u yhz DBUS_SESSION_BUS_ADDRESS=unix:path=/run/user/1000/bus \
      XDG_RUNTIME_DIR=/run/user/1000 systemctl --user stop iot-backend.service
    sudo -u yhz DBUS_SESSION_BUS_ADDRESS=unix:path=/run/user/1000/bus \
      XDG_RUNTIME_DIR=/run/user/1000 systemctl --user disable --now iot-backend.service
    ```
  - 删除或禁用 `~/.config/autostart` 中相关 .desktop（若存在）或其它启动脚本。
- 中：核查为何部分登录返回的 JSON 中 `username` 为空（回溯 `auth.py` 返回构造与前端解析）。
- 中：若需要保留历史账号（例如 `777`），选择迁移策略（从 `backup_sqlite` 读取 hash 并插入到 `nifi.iot_users`，保留 bcrypt hash）。

**产出物清单**
- ✅ 本日报（文件路径见底部）。
- ✅ 备份目录（若有）：`backup_sqlite/`（存在未跟踪的备份文件）。
- ✅ MySQL 中新增的测试用户记录（用于验证）。

**命令快照（供复现）**
- 查看工作区未提交变更：
  - `git status --porcelain`
- 查找今天改动的文件：
  - `find . -type f -newermt '2026-05-06' -not -path './.git/*' -printf '%TY-%Tm-%Td %TH:%TM %p\n'`
- 停止用户级服务（示例）：
  - `sudo -u yhz DBUS_SESSION_BUS_ADDRESS=unix:path=/run/user/1000/bus XDG_RUNTIME_DIR=/run/user/1000 systemctl --user stop iot-backend.service`
- 重启系统级服务（示例，需要 sudo）：
  - `sudo systemctl daemon-reload && sudo systemctl restart iot-backend.service`

如需我代为执行 `sudo` 步骤并把输出贴回，请回复“代为重启并验证”，我会在得到授权后执行并更新本日报状态（把 `进行中` 标为完成）。

---
更新记录：生成于 2026-05-06，由自动脚本收集 `git status`、文件修改时间及 `journalctl` 与端口监听检查结果整理。
