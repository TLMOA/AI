# 2026-04-19 日报

**结论**：✅ 后端多数据库导出支持已实现并部署；前端服务 `iot-frontend.service` 已配置为开机自启并运行（Restart=always），断电重启后会自动启动。

**今日目标**
- 完成多数据库导出后端的稳定性与驱动配置（优先）。
- 验证前端/后端服务的断电自启行为。

**完成事项**
- ✅ 在后端实现对 MySQL/Postgres/SQLite/MSSQL/Oracle 的引擎支持（`engine_factory.py`）。
- ✅ 更新 `db_connect.py` 增加连接尝试超时保护（线程 + 8s 超时），避免 ODBC 阻塞请求。
- ✅ 更新 `export_worker.py`，实现主目标写入后镜像/归档到 `exports/<factory>/<job>`。
- ✅ 在虚拟环境中安装并验证 `pyodbc==5.3.0` 与 `oracledb==3.4.2`。
- ✅ 在宿主机安装 Microsoft ODBC Driver (`msodbcsql17`) 与 `unixodbc-dev`，并确认 Oracle Instant Client 位于 `/opt/oracle/instantclient_23_26`。
- ✅ 检查并确认 `iot-backend.service` 与 `iot-frontend.service` 的 systemd 状态；`iot-frontend.service` 为 `enabled` 且 `ActiveState=active`（含 `Restart=always`）。

**进行中事项**
- ⏳ E2E 导出验证（需要可访问的目标数据库实例或凭证）。
- ⏳ 如需，调整 `iot-frontend.service` 的 `ExecStart` 指向项目虚拟环境的 Python（尚未修改）。

**遇到问题**
- ⚠️ 当前无本地可访问的 Postgres/SQL Server/Oracle 实例（端口 5432/1433/1521 未监听），无法完成本地 E2E 验证。
- ⚠️ 某些 ODBC 驱动在失败时会长时间阻塞，已用线程超时临时规避，但仍建议后续在生产环境中调整更优的重试/限时策略。

**明日计划**
- 📌 获取或部署测试数据库实例（Postgres/SQL Server/Oracle），执行导出 E2E 验证。
- 📌 如需，修改 `iot-frontend.service` 的 `ExecStart` 指向 `.venv/bin/python` 并执行 `systemctl daemon-reload` + `systemctl enable --now iot-frontend.service`。
- 📌 完善导出作业的原子写入与失败回滚方案，避免部分导出产物残留。

**风险与协调项**
- ⚠️ 若无法提供目标数据库访问，E2E 验证将受阻；需运维提供临时测试实例或凭证。
- ⚠️ 安装原生数据库驱动与系统包需要 sudo 权限，需协调运维窗口以减少对线上服务影响。

**产出物清单**
- `v1-backend/app/engine_factory.py`（多数据库支持）
- `v1-backend/app/db_connect.py`（连接超时保护）
- `v1-backend/app/export_worker.py`（镜像/归档逻辑）
- 虚拟环境依赖更新：`pyodbc==5.3.0`, `oracledb==3.4.2`

---
报告人：xy
