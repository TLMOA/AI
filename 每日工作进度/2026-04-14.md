2026-04-14 日报

记录人：xy

1. 结论
- ✅ 今天完成了数据库导出多库支持的需求实现（需求整理、前端原型、后端模型与接口、引擎工厂扩展），并在本地启动临时服务进行了冒烟测试；部分 E2E（MySQL 全量导出验证）、文档与运维部署（systemd 重启）尚待完成。

2. 当前进度
- ✅ 需求与原型：完成 `v1-frontend/prototypes/connection_profiles.md` 与交互原型 `v1-frontend/prototypes/admin_connection_profiles.html`。
- ✅ 后端模型与接口：在 `v1-backend/app/db_models.py` 中新增 `ConnectionProfileModel`，并为 `ExportJobModel` 添加 `connection_profile_id` 字段（含轻量迁移逻辑）。
- ✅ 引擎工厂：在 `v1-backend/app/engine_factory.py` 添加对 `dsn` 的支持，扩展了对 sqlite、mssql、oracle 的基础支持。
- ✅ 调度/执行：在 `v1-backend/app/export_worker.py` 和 `v1-backend/app/main.py` 中接入配置解析与运行时解析（profile -> db_config），导出逻辑改用工厂创建 engine 并支持增量导出路径。
- ✅ 冒烟测试：在项目虚拟环境中以 uvicorn 启动临时后端（127.0.0.1:18081），验证了：创建 profile、测试连接（SELECT 1）、创建带 `connection_profile_id` 的导出任务、任务列表返回关联字段。
- ⚠️ 运维/部署：无法在当前会话重启 systemd 服务（需要 sudo），因此生产端口（127.0.0.1:8081）的服务尚未替换为新代码。
- ⚠️ E2E：对 MySQL 的全量/增量导出、并发与性能验证还在进行中（需在目标 MySQL 环境执行完整导出并验证文件内容）。

3. 产出物清单
- [v1-frontend/prototypes/connection_profiles.md](v1-frontend/prototypes/connection_profiles.md)
- [v1-frontend/prototypes/admin_connection_profiles.html](v1-frontend/prototypes/admin_connection_profiles.html)
- [v1-backend/app/db_models.py](v1-backend/app/db_models.py)
- [v1-backend/app/engine_factory.py](v1-backend/app/engine_factory.py)
- [v1-backend/app/export_worker.py](v1-backend/app/export_worker.py)
- [v1-backend/app/main.py](v1-backend/app/main.py)

4. 明日/下一步
- ⏳ 在有权限时执行 `systemctl restart iot-backend`（或由运维执行），使生产服务加载新代码并对外提供接口。
- ⏳ 在生产或测试 MySQL 实例上执行 E2E 导出（增量/全量），验证导出文件内容、格式与断点续传行为。
- ✅ 将前端正式页面（定时任务面板）接入真实 API：下拉读取 `GET /api/v1/connection-profiles?enabled=true`，保存任务时包含 `connection_profile_id`。
- 📌 撰写并合并操作文档：包括 Secret 管理建议（Vault）、迁移步骤、systemd 重启 SOP、以及回滚策略。

5. 风险与阻塞
- ⚠️ 当前无法在宿主机重启 systemd 服务（需要 sudo），阻碍了在生产端完整验证与联调。
- ⚠️ 密钥/凭证的存储目前为 MVP（`secret_ref`），上线前需替换为安全存储（Vault 或加密存储）。

---
补充说明：上述内容基于本地开发与冒烟测试结果汇总。需要我代为执行 systemd 重启并完成 E2E 验证（需 sudo/目标 MySQL 访问凭证）吗？
