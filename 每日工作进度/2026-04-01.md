# AI模块V1日报（2026-04-01）

## 1. 今日目标

- 续作昨日未完成项：拉起前后端服务并完成 DB 导出链路实跑验证。
- 确认“前端可触发导出 + 产物落盘到 NiFi 目录”可用。

## 2. 完成事项

1. ✅ 检查并确认昨日改动已生效：
   - 后端默认输出目录为 `/home/yhz/nifi-data/output_csv`。
   - 前端已包含 DB 测试连接、列出表、导出按钮。
2. ✅ 在 `v1-backend/.venv` 安装可用依赖版本（受镜像可用版本限制）：
   - `fastapi==0.116.1`
   - `pydantic==2.10.6`
   - `uvicorn==0.33.0`
   - `pymysql`、`sqlalchemy`、`python-multipart`、`backports.zoneinfo`
3. ✅ 启动后端服务：`127.0.0.1:8081`。
4. ✅ 启动前端静态服务：`http://127.0.0.1:8080`。
5. ✅ 实跑接口验证：
   - `GET /health` 返回 `{"status":"ok"}`。
   - `POST /api/v1/db/list-tables` 返回 `['sensor_data']`。
   - `POST /api/v1/export/mysql` 导出成功。
6. ✅ 核对导出产物：`/home/yhz/nifi-data/output_csv/sensor_data_export_latest.csv` 存在且内容正确。

## 3. 进行中事项

1. ⏳ 等待你在页面侧手工点击走一遍（DB 连接 -> 列出表 -> 导出）做最终交互验收。

## 4. 遇到问题

1. ⚠️ 当前镜像源不提供 `pydantic==2.11.7` 与 `uvicorn==0.35.0`，导致按 `requirements.txt` 精确安装失败。
2. ✅ 已采用镜像可用版本替代，仅用于本机演示验证，不影响业务代码逻辑。

## 5. 明日计划

1. 与你一起完成页面交互验收并留存截图证据。
2. 若你确认，补一份“环境依赖兼容说明”到 `v1-backend/README.md`（可选）。
3. 继续推进五流程联动验收与证据归档。

## 6. 风险与协调项

1. ⚠️ 若需严格复现锁定版本，需更换可用 Python 包源或升级 Python 运行环境。

## 7. 产出物清单

- 启动中的服务：
  - Backend: `http://127.0.0.1:8081`
  - Frontend: `http://127.0.0.1:8080`
- 导出产物：
  - `/home/yhz/nifi-data/output_csv/sensor_data_export_latest.csv`
