# 日报 — 2026-04-17

## 1. 今日目标
- ✅ 完成并验证全站登录/注册/管理员权限（后端 + 前端）。
- ✅ 恢复/添加前端登录页面（/login.html），并在首页加入入口。
- ✅ 在后端新增 `iot_users` 表，预置 admin 并使用 bcrypt 哈希。
- ✅ 给内部管理 API 添加管理员校验并验证权限正确性。
- ✅ 前端根据 `/api/v1/auth/me` 控制“内部管理”链接显示。

## 2. 完成事项
- ✅ 后端实现并测试接口：`POST /api/v1/auth/register`、`POST /api/v1/auth/login`、`GET /api/v1/auth/me`、`POST /api/v1/auth/logout`。
- ✅ `iot_users` 表在启动时自动创建，预置管理员 `admin`（初始密码 `123456`，已以 bcrypt 哈希存储）。
- ✅ 注册接口禁止注册 `admin`（返回 403）。
- ✅ 登录后通过 HttpOnly cookie 分发 JWT（`IOT_COOKIE_SECURE` 环境变量控制 `Secure` 标志）。
- ✅ 在后端内部路由加入 `_require_admin` 校验，已验证 admin 可访问、普通用户被拒绝（403）。
- ✅ 在前端添加 `login.html`（已放置于 `v1-frontend/login.html`）并在首页显示登录入口；前端 `app.js` 已在初始化时调用 `/auth/me` 控制 internal 链接显示。

## 3. 进行中事项
- ⏳ 报告整理与提交（本日报完成）。
- ⏳ 后续安全加固（TLS 部署、登录速率限制、初始管理员强制改密等）计划编写并排期。

## 4. 遇到的问题与风险
- ⚠️ 生产环境需启用 HTTPS 并设置 `IOT_COOKIE_SECURE=true`，否则浏览器不会发送带 `Secure` 的 cookie。请在部署时确认 TLS 与反向代理设置。
- ⚠️ 需注意在同库建表的风险，部署前请备份目标数据库快照并在低峰期执行建表脚本（方案中已有说明）。

## 5. 明日计划
- 📌 将登录/权限相关改动整理成提交（git commit + 注释），并准备推送/PR（如需我执行请确认）。
- 📌 实施安全加固：1) 在部署脚本与文档中加入 `IOT_COOKIE_SECURE` 与 HTTPS 指引；2) 考虑实现登录失败次数限制与初始管理员强制改密。
- 📌 若需要：编写 pytest 集成测试覆盖注册/登录/权限流程。

## 6. 风险与协调项
- 📌 请运维在部署前备份 NiFi 数据库（若使用生产 NiFi DB），并在低峰期执行。若需要我可以输出建表脚本和备份/回滚步骤说明。

## 7. 产出物清单
- `v1-backend/app/auth.py`：实现 auth 接口并使用 cookie 下发 JWT。
- `v1-backend/app/db_models.py`：新增 `iot_users` 表及启动时插入默认 admin 的逻辑。
- `v1-frontend/login.html`：新增登录页面。
- `v1-frontend/index.html`、`v1-frontend/app.js`：添加登录入口与 `internal` 链接控制逻辑。
- 运行验证记录（curl 与 sqlite 验证）已在本地执行并通过。

---

若需要，我可以：
- 生成对应的 git 提交并推到远端（需要确认分支策略）；
- 立即编写集成测试或安全加固的实现（请指示优先级）。
