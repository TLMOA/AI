# AI模块V1日报（2026-03-31）

## 1. 今日目标

- 按 Day2 任务单完成文件中心、标签中心和执行器解耦。
- 保持前端可演示链路不受影响。

## 2. 完成事项

1. ✅ 后端新增执行器解耦：`MockExecutor`、`NiFiExecutor`。
2. ✅ 增加环境开关：`APP_EXECUTOR_MODE=mock|nifi`。
3. ✅ 新增 NiFi 流程映射模板：`v1-backend/app/nifi_flow_mapping.template.json`。
4. ✅ 后端新增系统接口：`GET /api/v1/system/executor`。
5. ✅ 后端新增文件列表接口：`GET /api/v1/files`。
6. ✅ 文件预览增强支持 CSV/JSON/TSV + offset/limit 分页。
7. ✅ 标签接口参数校验增强（手动标签 records 校验）。
8. ✅ 前端新增文件中心：列表、预览、下载。
9. ✅ 前端新增标签中心：规则加载、自动标签、手动标签。
10. ✅ Day2 冒烟测试通过（任务创建、状态流转、产物预览、标签接口、文件列表）。
11. ✅ 已实现真实 NiFi REST 客户端：`v1-backend/app/nifi_client.py`。
12. ✅ 已将 `NiFiExecutor` 升级为真实 API 调用路径（含登录、触发、状态查询）。
13. ✅ 增加真实模式失败可读错误：`NIFI_FLOW_UNMAPPED` / `NIFI_EXEC_ERROR`。
14. ✅ 完成 mock/nifi 双模式启动验证与接口联调。
15. ✅ 已新增 Day3 任务单并进入真实流程ID映射阶段。
16. ✅ 已回填 5 条真实 NiFi 流程ID映射并完成加载验证。
17. ✅ 已完成 `CONVERT`、`TAG_AUTO`、`COPY_MULTI_FORMAT`、`IMPORT_EXTERNAL` 真实链路端到端验证（全部成功）。
18. ✅ 已完成失败策略验证：错误流程ID返回 `NIFI_EXEC_ERROR`，错误信息可读。
19. ✅ 已恢复正式映射配置，避免测试配置污染。
20. ✅ 已完成 `TAG_MANUAL` 正常映射真实链路验证（成功产生产物绑定）。
21. ✅ 已在执行器加入 NiFi 调用重试与状态轮询参数化能力。
22. ✅ 已完成 NiFi 调用错误分级与错误码映射（认证/网络/流程不存在/通用）。
23. ✅ 已完成前端任务页错误码提示映射，并展示失败原因与排查提示。
24. ✅ 已补齐前端任务类型选择（含 `IMPORT_EXTERNAL`、`TAG_MANUAL`）用于全量回归。
25. ✅ 已新增 nifi 模式批量回归脚本 `nifi_batch_smoke.py`，支持多轮结果输出。
26. ✅ 已新增 Day3 封板验收记录模板，覆盖接口、前端、压测与证据项。
27. ✅ 已运行 1 轮 nifi 批量回归脚本，5 类任务全部成功并生成报告文件。
28. ✅ 已回填 Day3 封板验收记录实测数据（任务结果与结论）。
29. ✅ 已完成 2 轮 nifi 批量回归验证（10/10 任务成功），并生成 round2 报告。
30. ✅ 已新增《前后端联调检查清单》，用于统一联调验收口径。
31. ✅ 已新增《方案完成度对照》，明确已完成/未完成项。

32. ✅ 修正后端 `NIFI_OUTPUT_DIR` 默认值为 `/home/yhz/nifi-data/output_csv`，并在启动时确保目录存在；同时同步更新前端说明与 `v1-backend/README.md`。
33. ✅ 修复导出后文件注册的路径错误，避免在 `out_path.replace()` 后尝试注册已移动的临时路径。
34. ✅ 在后端新增 `/api/v1/db/list-tables` 接口，并在前端 `index.html` 与 `app.js` 中添加数据库连接表单、测试连接、列出表与导出按钮，实现从前端直接发起 MySQL 导出流程。
35. ✅ 在工作区使用默认 MySQL 参数验证并成功导出 `sensor_data` 表，产物生成：`/home/yhz/nifi-data/output_csv/sensor_data_export_latest.csv` 并已注册。
36. ⚠️ 本地尝试启动后端用于浏览器演示时发现系统缺少 `uvicorn` 可执行，已记录并提供安装/启动命令，待明日安装或由用户本地启动后进行页面演示。

## 3. 进行中事项

1. ⏳ 补齐封板截图证据与关键日志摘要归档。
2. ⏳ 启动并演示前端数据库导出页面（等待 `uvicorn` 安装或用户同意由我在工作区安装并启动服务）。

## 4. 遇到问题

1. ⚠️ 通过 IP (`https://127.0.0.1:8443`) 调用 NiFi token 接口会触发 `Invalid SNI`。
2. ✅ 已切换默认地址为 `https://localhost:8443`，鉴权与流程查询恢复正常。

## 5. 明日计划

1. 补齐封板证据截图与关键日志摘要。
2. 补充前后端联调检查清单（错误码到提示文案一致性）。
3. 汇总 Day3 封板输出并准备 Day4 进入 JetLinks 联调。
4. 在工作区完成依赖安装并启动后端与前端，完成浏览器端的“DB 连接→列表→导出”端到端演示，采集演示截图与日志归档。

## 6. 风险与协调项

1. ⚠️ 需与 NiFi 当前流程配置保持一致（流程ID、参数命名）。
2. ⚠️ 需预留 JetLinks 联调窗口，避免后续字段返工。

## 7. 产出物清单

- 代码：v1-backend/app/executors.py
- 代码：v1-backend/app/main.py
- 代码：v1-backend/app/nifi_flow_mapping.template.json
- 代码：v1-backend/app/nifi_client.py
- 代码：v1-frontend/index.html
- 代码：v1-frontend/app.js
- 代码：v1-frontend/styles.css
- 文档：v1-backend/README.md
- 文档：v1-frontend/README.md
- 文档：V1执行清单/Day3详细任务单.md
- 文档：V1执行清单/Day3封板验收记录.md
- 文档：V1执行清单/前后端联调检查清单.md
 - 文档：V1执行清单/方案完成度对照-2026-03-31.md
 - 文档：V1执行清单/封板证据归档-2026-03-31.md
