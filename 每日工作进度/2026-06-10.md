# AI模块V1日报（2026-06-10）

## 1. 整体概览

本日报汇总自上次记录（2026-06-01）至今（2026-06-10）的全部工作内容，主要集中于 **V3 阶段方案设计**、**设计文档撰写**、**标签配置与文件元数据方案落地** 三大方向。

---

## 2. 完成事项

### 2.1 设计文档

1. **《设计文档-IoT智慧平台AI模块.md》**（v2.0，2026-06-02）
   - 完整梳理项目概述、系统架构（前端层 → 统一编排层 → 执行器层 → 数据流层 → 训练层）
   - 详细记录 V1 已完成功能清单：任务管理、文件管理、数据库导出（8 种数据源）、标签管理、静默导出、认证授权等
   - 双模执行器设计（Local / NiFi）架构说明
   - 版本规划：V1（基础平台已完成）→ V2（Hadoop+AI 规划中）→ V3（xattr 元数据增强方案设计中）

2. **《AI模块综合执行方案（融合NiFi、联邦学习与双模部署）.md》**（v4.2）
   - 总体架构设计：前端 → 编排 → 执行器 → 数据流 → Client Agent → 训练层
   - 公有化/私有化双模部署架构设计
   - 数据处理双轨制（Local 执行器 + NiFi 执行器）

### 2.2 V3 执行清单（全新方案体系）

3. **《公有化与私有化部署执行方案.md》**（v3.3，2026-06-03）
   - 定义公有化部署（数据存公司 Ceph，直接中心训练）和私有化部署（数据存工厂 Ceph，管理员拉取后训练）两种模式
   - 明确用户与工厂一对一关系、管理员/普通用户权限边界
   - 完整核心数据流设计与目录结构规划

4. **《标签配置与文件元数据执行方案_文件配置版.md》**（V3）
   - 为每个数据文件配套独立 `.meta.json` 配置文件（sidecar 模式）
   - 定义元数据字段 schema：fileId、fileName、sourceType、hasTag、tagColumn、tagName、tagRange、failedTag 等
   - 覆盖数据库导出、文件上传、预览编辑、手动打标、自动打标全场景

5. **《零文件元数据方案.md》**（V3 进阶方案）
   - 提出用 Linux xattr 扩展属性 + AES-256-GCM 加密替代 `.meta.json` 文件
   - 实现磁盘上零额外文件、inode 级附着、加密不可见
   - 附全流程架构说明（文件生成 → 后端索引 → 文件管理 → 外部传输）

6. **《标签配置与文件元数据功能优先级清单.md》**（V3）
   - 按 P0/P1/P2 三级优先级拆分功能
   - P0：meta_json 工具模块、后端导出/上传/预览/手动打标/自动打标接口、前端标签表单、目录规则落地（共 10 项，全部勾选完成）
   - P1：自动打标 UI、训练文件 API、打标入口集成等（4 项）
   - P2：训练提交、JWT 鉴权、审计日志、版本控制、迁移脚本等（12 项，延后至汇报后）

7. **《宿主机 IP 变更执行清单.md》**（2026-06-05，已推送到 GitHub）
   - 列出 IP 变更时必须修改的 16 个文件位置（前端 html/js、docker-compose、nifi.properties、README 等）
   - 附带建议修改项（SSH 隧道脚本、MySQL 配置等）

8. **《网站前端功能使用手册.md》**（V3 适用版）
   - 全面覆盖顶部全局切换、数据库导出卡片（含标签配置）、文件中心、预览编辑区等功能使用说明
   - 明确所有修改均不涉及 NiFi

### 2.3 代码实现

9. **新建 `v1-backend/app/meta_json.py`**（标签配置文件读写模块）
   - 实现核心读写函数：`meta_path / write_meta / read_meta / has_meta / delete_meta`
   - 实现版本备份机制：修改前自动备份最近 5 个历史版本到 `meta_backups/`
   - 实现 `build_meta()` 工厂函数（统一构造 metadata dict）
   - 实现 `meta_summary()` 摘要生成（用于前端预览展示）
   - 目录常量化：`NIFI_BASE_DIR`、`TAGGED_OUTPUT_DIR`、`META_BACKUP_DIR`

10. **`v1-backend/app/main.py` 标签集成改造**
    - 导出接口、上传接口均新增 `hasTag / tagColumn / tagName / tagRange / failedTag` 参数支持
    - 文件预览接口集成 meta_json 摘要读取
    - 手动打标接口增加 tagRange 校验逻辑
    - 自动打标接口完善多列规则 + meta 回写
    - 训练文件列表接口完成基础版（不含 storagePath）

11. **新建 `v1-backend/run_silent_export_worker.py`**
    - 提供静默导出 worker 的单次运行入口，用于 demo/testing 场景

### 2.4 V3 辅助文件

12. 创建 `V3执行清单/cleanup_meta.py`（元数据清理脚本）
13. 创建 `V3执行清单/meta.schema.json`（meta 字段 JSON Schema 定义）
14. 创建 `V3执行清单/sample.csv.meta.json`（示例配置文件）
15. 创建 `V3执行清单/说明.md`（xattr 扩展属性技术原理说明）

---

## 3. 进行中事项

- ⏳ 公有化/私有化部署方案细化（数据拉取机制、Ceph 挂载路径确认）
- ⏳ 汇报用 PPT 准备（围绕 "brokenble 数据集全流程实现" 主题）
- ⏳ xattr 方案技术验证（需确认目标环境文件系统支持情况）

---

## 4. 遇到问题与决策记录

- **`.meta.json` vs `xattr` 方案选型**：先以 `.meta.json` sidecar 模式落地 P0 功能，确保 Local 端到端流程跑通；xattr 加密方案留待汇报后的 V3 进阶阶段实施。
- **方案范围收敛**：明确 V3 P0/P1/P2 全部针对 Local 后端进行改造，不涉及 NiFi 侧改动，避免范围蔓延。

---

## 5. 明日计划

1. 完成汇报材料的最终整理（PPT + 演示脚本）
2. 对已完成的 meta_json 功能进行端到端回归验证（导出→上传→预览→手动打标→自动打标）
3. 根据反馈决定是否启动 P1 项（自动打标 UI、训练文件 API 增强）

---

## 6. 风险与协调项

- ⚠️ 汇报时间紧迫，P1 功能需评估是否在汇报前补齐
- ⚠️ xattr 方案依赖文件系统支持（ext4/xfs/btrfs），需确认部署环境兼容性
- ⚠️ 私有化部署中工厂 Ceph 的权限与网络连通性需与运维协调
- ℹ️ NiFi 后端和扩展属性（xattr）安排在汇报后进行
- ℹ️ 联邦学习相关延后至后续阶段

---

## 7. 产出物清单

- 文档：
  - `设计文档-IoT智慧平台AI模块.md`（v2.0，全新）
  - `AI模块综合执行方案（融合NiFi、联邦学习与双模部署）.md`（v4.2，更新）
  - `V3执行清单/公有化与私有化部署执行方案.md`（v3.3，全新）
  - `V3执行清单/标签配置与文件元数据执行方案_文件配置版.md`（全新）
  - `V3执行清单/零文件元数据方案.md`（全新）
  - `V3执行清单/标签配置与文件元数据功能优先级清单.md`（全新）
  - `V3执行清单/宿主机IP变更执行清单.md`（全新，已推送 GitHub）
  - `V3执行清单/网站前端功能使用手册.md`（全新）
  - `V3执行清单/说明.md`（xattr 原理说明，全新）
- 代码：
  - `v1-backend/app/meta_json.py`（全新，标签配置文件读写模块）
  - `v1-backend/app/main.py`（标签配置集成改造）
  - `v1-backend/run_silent_export_worker.py`（全新，静默导出测试入口）
  - `V3执行清单/cleanup_meta.py`（全新，元数据清理脚本）
- 配置/规范：
  - `V3执行清单/meta.schema.json`（全新，meta 字段 Schema）
  - `V3执行清单/sample.csv.meta.json`（全新，示例配置）
- GitHub：
  - 提交 `docs(V3执行清单): 新增"宿主机IP变更执行清单"`（2026-06-05）

---

报告人：xy
时间：2026-06-10