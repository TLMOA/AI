# AI模块V1日报（2026-04-04）

## 1. 今日目标

- 将“用户上传转化”从扇出模式改为六条独立单转换流程。
- 打通前后端 `convertType` 契约，确保一次上传只执行一个转换方向。
- 完成六条链路回归验证并形成可追溯结果。

## 2. 完成事项

1. ✅ 完成后端上传接口单路由改造：
   - `POST /api/v1/upload/inbox_csv` 支持 `convertType=csv_to_json|csv_to_tsv`。
   - `POST /api/v1/upload/inbox_json` 支持 `convertType=json_to_csv|json_to_tsv`。
   - `POST /api/v1/upload/inbox_tsv` 支持 `convertType=tsv_to_json|tsv_to_csv`。
2. ✅ 完成前端上传入口拆分为 6 个独立转换选项：
   - `CSV -> JSON`
   - `JSON -> CSV`
   - `CSV -> TSV`
   - `TSV -> CSV`
   - `JSON -> TSV`
   - `TSV -> JSON`
3. ✅ 完成前端上传参数联动：
   - 依据选项映射上传端点与 `convertType`，并按单转换结果回显。
4. ✅ 完成 6 条链路实测：
   - 6 次上传接口返回均为 `code=0`。
   - `csv_to_json/json_to_csv/csv_to_tsv/tsv_to_csv/json_to_tsv/tsv_to_json` 六目录各命中 1 个测试产物。
5. ✅ 修复 TSV 预览与保存链路：
   - TSV 预览改为返回真实表头，不再显示 `col_1/col_2...`。
   - `manual-table` 对 TSV 源文件改为写回 `tagged_output/*.tsv`。
   - 重启后端后完成端到端验证，TSV 文件保存可正确落到标签目录。

## 3. 进行中事项

1. ⏳ 将新“6独立转换”规则补充到用户操作文档与页面说明细节（含示例）。
2. ⏳ 补充前端上传区更显式的错误提示（非法 `convertType` 场景）。
3. ⏳ 继续检查其他预览格式是否存在类似“元数据格式与写出格式不一致”的问题。

## 4. 遇到问题

1. ⚠️ 前端历史实现为按源格式三分流（CSV/JSON/TSV），不满足“6 独立转换”。
2. ✅ 已处理：改为“选项 -> 端点 + `convertType`”映射，完成契约统一。
3. ⚠️ TSV 预览曾返回伪列名，导致编辑保存时列名不一致；已修复并验证。

## 5. 明日计划

1. 补充转换链路自动化脚本（6 路 smoke test）。
2. 增加上传区域转换说明与使用示例，降低误操作。
3. 继续推进 NiFi 真实流程封装对接（触发、状态回填、产物绑定）。

## 6. 风险与协调项

1. ⚠️ 若外部调用方仍按旧参数调用，可能出现“不符合预期转换方向”的问题。
2. 📌 需要同步接口变更说明给相关联调方，明确 `convertType` 为必选语义参数。

## 7. 产出物清单

- 代码：
  - `v1-backend/app/main.py`
  - `v1-frontend/index.html`
  - `v1-frontend/app.js`
- 验证：
  - 6 条上传转换链路命令行回归（8081）
   - TSV 预览与保存端到端验证通过（保存文件落到 `tagged_output/*.tsv`）
- 文档：
  - `每日工作进度/2026-04-04.md`
