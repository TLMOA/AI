# AI模块V1日报（2026-04-03）

## 1. 今日目标

- 完成后端服务高可恢复（断电/重启后自动恢复）配置。
- 修复并增强文件预览/弹窗编辑链路，支持在表格中新增字段列。
- 收口当日可演示闭环并输出可追溯产出物。

## 2. 完成事项

1. ✅ 完成 systemd 稳定化：
   - 用户级服务 `iot-backend.service` 已启用并配置自动重启。
   - 启用 `loginctl enable-linger yhz`，保证重启后用户服务可恢复。
2. ✅ 完成 system-wide 服务落地：
   - 生成并安装 `/etc/systemd/system/iot-backend.service`。
   - 新增健康检查脚本与 timer（按分钟巡检，失败自动拉起）。
   - 服务状态验证通过（active/running，8081 监听正常）。
3. ✅ 完成启动顺序增强：
   - `iot-backend.service` 增加 `Wants/After=network-online.target nifi.service remote-fs.target`（非强依赖）。
   - 增加 `ExecStartPre=/bin/sleep 2`，降低启动竞争。
4. ✅ 完成前端能力增强：
   - 在文件预览/弹窗编辑区新增“新增字段”按钮。
   - 实现新增列后可编辑并保存到后端输出文件。
5. ✅ 完成端到端回归：
   - `/api/v1/files` 返回正常，示例 total=62。
   - 文件预览与编辑流程可用。
6. ✅ 完成 TSV 全链路补齐（阶段三）：
   - 后端 `export/mysql` 支持 `TSV` 分支（输出到 `output_tsv`）。
   - 前端上传来源新增 `TSV`，并映射到 `/api/v1/upload/inbox_tsv`。
   - 前端目录树已纳入 `output_tsv/inbox_tsv/tsv_to_json/json_to_tsv/csv_to_tsv/tsv_to_csv`。
   - 已实测 `CSV/JSON/TSV` 上传均返回 `code=0` 且在目标目录产生产物。

## 3. 进行中事项

1. ⏳ 健康检查仍为基础版，可继续增强对 NiFi/DB 的就绪与可写校验。
2. ⏳ 依赖服务（NiFi/数据库/挂载）的强依赖编排（Requires）待按实际服务名进一步收口。
3. ⏳ TSV 全链路第二阶段已推进：后端已新增 `upload/inbox_tsv` 与 CSV/JSON 上传到 TSV 的互转逻辑，前端入口与导出 TSV 分支待继续收口。

## 4. 遇到问题

1. ⚠️ systemd 落地阶段存在端口 8081 被历史进程占用，导致短时启动失败。
2. ⚠️ 安装 system-wide 单元时遇到 sudo 交互阻塞（需手动输入密码）。
3. ✅ 已处理：清理冲突进程、重启服务并复验通过。

## 5. 明日计划

1. 增强 health-check：加入 NiFi API、数据库写入、磁盘阈值检查。
2. 明确依赖服务单元名并收敛 `Requires/After` 启动顺序。
3. 补充备份与告警策略（至少完成一版可执行脚本）。

## 6. 风险与协调项

1. ⚠️ 若 NiFi/数据库未纳入统一开机编排，重启后可能出现“服务已启动但业务不完整可用”的窗口期。
2. ⚠️ 异常断电下 SQLite 存在小概率损坏风险，需落实定时备份与恢复演练。

## 7. 产出物清单

- 文档：
  - `每日工作进度/2026-04-03.md`
- 接口：
  - `GET /api/v1/files`
  - `POST /api/v1/tags/manual-table`
- 页面：
  - `v1-frontend/index.html`（新增“新增字段”入口）
  - `v1-frontend/app.js`（新增列编辑逻辑）
- 测试：
  - systemd 服务状态与端口监听检查通过
  - API 自检返回 `code=0`
