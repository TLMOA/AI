# IoT 智慧平台安全审查报告

> 审查日期：2026-08-09 | 审查范围：v1-backend + v1-frontend + NiFi 容器 + AI 模块

## 一、严重问题（必须修复）

### 1.1 JWT 密钥使用默认值

- **位置**：`v1-backend/app/auth.py` L22
- **代码**：`SECRET_KEY = os.getenv("IOT_SECRET_KEY", "dev-secret-change-me")`
- **风险**：生产环境未设置 `IOT_SECRET_KEY` 时，攻击者可用公开的默认密钥伪造任意用户的 JWT token（含 admin），完全绕过认证
- **验证**：`iot-backend.service.override.conf` 中未设置该变量
- **修复**：`openssl rand -base64 32` 生成密钥，写入 override.conf

### 1.2 前端硬编码数据库凭据

- **位置**：`v1-frontend/app.js` L1199-1206
- **内容**：`202.113.76.55` 的 root 密码、postgres 密码 `difyai123456`、oracle 密码等 8 组真实凭据
- **风险**：任何能访问前端页面的人都能看到生产数据库密码
- **修复**：删除默认凭据，改为空值 + 用户手动填写

### 1.3 CORS 配置错误

- **位置**：`v1-backend/app/main.py` L1162-1168
- **代码**：`allow_origins=["*"]` + `allow_credentials=True`
- **风险**：`*` + credentials 组合无效且危险；若配合 cookie 认证，恶意站点可发起跨站请求
- **修复**：改为具体域名白名单，如 `allow_origins=["https://iot.factory.com"]`

### 1.4 路径穿越漏洞

- **位置**：`v1-backend/app/main.py` L1355-1357 `_normalize_username` + L275 `_resolve_user_storage_root`
- **代码**：
  ```python
  def _normalize_username(value):
      return (value or "").strip() or DEFAULT_USERNAME   # 不过滤 .. / \
  root = IN_DATA_BASE_DIR / username                     # 直接拼接
  ```
- **风险**：username 含 `../../` 时可读写任意路径；多个 API 接受 username 作为 Query 参数
- **修复**：所有用户名统一用 `_sanitize_filename_component` 过滤，或 `_normalize_username` 内加白名单校验

### 1.5 DB 导出 SSRF

- **位置**：`v1-backend/app/main.py` L5320-5326 `MySQLExportReq`（host/port 可任意）
- **风险**：用户可让后端连接任意内网 IP/端口，探测内网、攻击内网服务
- **修复**：限制目标 host 白名单（如仅允许配置的数据库服务器），或禁止非本机连接

## 二、重要问题（建议修复）

| # | 问题 | 位置 | 风险 | 修复 |
|---|------|------|------|------|
| 1 | 无 TLS | 全部 | 明文传输密码/数据 | Nginx HTTPS |
| 2 | MySQL 默认密码 | override.conf | 数据库入侵 | 强密码+仅本机 |
| 3 | NiFi 默认密码 | nifi_orchestrator L23 | NiFi 被接管 | 改密码 |
| 4 | NiFi 挂载整个 /home/yhz | docker-compose L11 | 容器逃逸 | 最小化挂载 |
| 5 | 无 IP 限速 | auth.py | 暴力破解 | 加限流 |
| 6 | 无审计日志 | 全局 | 无法追溯 | 加操作审计 |
| 7 | Docker 无资源限制 | docker-compose | 资源耗尽 | 加 limit |

## 三、低风险/观察项

- `allow_anonymous.authentication=false`（NiFi）✅ 正确
- 登录失败锁定机制已实现（auth.py L336-368）✅
- Cookie `httponly=True, samesite=lax` ✅
- 文件下载有 `_check_file_access` 权限校验 ✅
- `_sanitize_filename_component` 已用于上传文件名 ✅

## 四、修复优先级建议

1. **立即**：JWT 密钥、前端凭据、路径穿越、CORS
2. **上线前**：SSRF、TLS、MySQL/NiFi 密码、限流
3. **持续**：审计日志、资源限制、最小化挂载

---

> 修复完成后建议重新跑一遍 `test-scripts/` 下的功能测试，确认无回归。