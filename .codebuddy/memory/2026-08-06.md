# 2026-08-06

## 安装 AI 编程 CLI

- **当前 IDE 远程连接的主机**：`ubunto`（校园网 202.113.76.55，tailscale 100.121.225.98），Ubuntu 22.04，Node v22.22.2/npm 10.9.7
- **成功安装**：CodeBuddy Code CLI `@tencent-ai/codebuddy-code@2.132.0`（`npm i -g`），命令名 `codebuddy` / `codebuddy-code` / `cbc`
- **Trae 未成功**：`trae@1.4.2` 包名不对（npm 上的 trae 包没有 bin），需要官方安装脚本
- **解决 bash hash 缓存问题**：装完后终端里找不到 codebuddy 是因为 bash 命令缓存，`hash -r` 清空后即可
- 用户希望在远程主机上使用 AI CLI 自动干代码任务
