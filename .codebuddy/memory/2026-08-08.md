# 2026-08-08 工作日志

## AI 训练网站前端全流程测试（10 单模型 + 3 模型组合）

### 用户需求
1. 训练/测试必须通过前端模拟真人操作（同主网站测试方式）
2. 每个模型单独训练+用该模型预测；再用任意 3 个模型组合训练+预测（pima 糖尿病集）
3. 测试报告 AI 训练网站篇幅与主网站相当或更多

### 关键修复
- **pipeline.py label_column=auto_tag 回退**：前端训练默认传 `label_column=auto_tag` 占位符，pipeline 原逻辑把它当真实标签列导致失败。修复：`explicit_label != 'auto_tag'` 才用显式值，否则回退配置文件 tagColumn；config_info 写 label_column_final
- 重启 AI 后端（8002）加载新代码：`kill <pid>; cd banckend && nohup python main.py`

### 数据准备（前端可访问）
- 训练：`/home/yhz/admin/nifi-data/pima_diabetes/pima_train.csv` + `pima_train.meta.json`（tagColumn=Outcome, failedTag=1, datesetname=糖尿病预测数据集）
- 预测：`/home/yhz/admin/tagged_nifi_data/pima_predict/pima_test.csv` + `pima_test.meta.json`
- 前端训练文件夹只认 `{user}/nifi-data` 或 `real_nifi_data`；预测文件夹只认 `tagged_nifi_data` 或 `tagged_real_nifi_data`

### Playwright 前端自动化要点（重要）
- snapshot 保存到**当前工作目录**（非 .playwright-cli），需找最近 yml
- Element Plus 下拉：eval .click() 不生效，必须 playwright `click` 点击 generic [cursor=pointer] 容器，再 snapshot 找 option ref
- checkbox：必须 playwright click 点击 label generic ref 才触发 Vue 状态
- tab 切换：必须 playwright click tab ref（eval .click 无效）
- eval 返回 stdout 含 JS 源码，判断结果用 `'"clicked"' in str(r)` 而非 `"disabled" in`
- 预测文件夹下拉是预测 tab 内第 1 个 select（训练 tab 的 select 隐藏但仍在 DOM），文件下拉是第 2 个
- 部署：leaderboard 每模型一行，需遍历所有 Ready 行点部署按钮

### 测试结果（pima 768 行，train_ratio=0.8）
| 模型 | 训练F1 | 训练Acc | 预测故障/768 | 预测准确率 |
|------|-----|-----|------|------|
| xgboost | 0.7778 | 0.7857 | 343 | 81.64% |
| LightGBM | 0.6948 | 0.7338 | 227 | 80.34% |
| CatBoost | 0.6929 | 0.7468 | 269 | 94.40% |
| hgb | 0.6713 | 0.6948 | 272 | 94.53% |
| TabNet | 0.4901 | 0.6484 | 72 | 64.84% |
| TabTransformer | 0.4585 | 0.6003 | 164 | 64.58% |
| Deep-MLP | 0.5801 | 0.7148 | 65 | 71.48% |
| 1D-CNN | 0.7381 | 0.7643 | 257 | 76.43% |
| Bi-LSTM | 0.6929 | 0.7513 | 165 | 75.13% |
| Bi-GRU | 0.7381 | 0.7643 | 257 | 76.43% |

### 重要更正：故障数超诊断条数的真相（2026-08-08 晚）
- **非单模型过拟合**！日志证明每个模型 fault_count 恒 ≤ 768（如 catboost=311, lightgbm=227, hgb=298...）
- 之前 881/1179/1568/1984/2235 是 **total_faults = 所有已部署模型 fault_count 累加**（predict_service.py `_run_multi_model_prediction` L720 `sum(fault_count)`）
- 预测接口按 dataset_name 匹配**所有**已部署模型，前端 msg 显示累加值
- 单模型准确率用 `/home/yhz/iot/_ai_accuracy_one.py`（子进程逐模型，避免 pycaret 全局状态污染）评估：用原始 Outcome 作真实标签对比 768 行完整预测
- CatBoost/hgb 准确率最高（94%+），xgboost 81.64%，Ludwig 系（TabNet/TabTransformer/Deep-MLP）召回率低（13-30%）

### 3 模型组合（xgboost+LightGBM+CNN）
- 训练完成：xgboost F1=0.7778, lightgbm 0.6948, CNN 0.5923
- 用户要求：训练出三个后**选 F1 最高的 xgboost 预测**（不三个都预测）
- 预测 768 条故障 343，准确率 81.64%

### 报告更新（用户要求）
- **一个报告内分两大块**：`第一部分：数据处理模块测试` + `第二部分：AI 大模型模块测试`（不混在一起）
- 命名：主网站→数据处理模块，AI 模型训练网站→AI 大模型模块
- 去掉"通过浏览器模拟真人操作"段落、meta.json tagColumn 段落、7.4 括号、total_faults 说明段
- 预测加准确率/精确率/召回率/F1 评估 + **指标含义解释**（Accuracy/Precision/Recall/F1 定义）
- **解释第四节训练 F1 与第五节预测 F1 不一致**：第四节是训练时 154 行测试子集内部评估，第五节是完整 768 行与真实标签比对
- 组合训练 1D-CNN F1 与单模型训练不同（0.5923 vs 0.7381）已注明属不同批次波动
- **补充训练/预测术语解释**（用户要求）：
  - 修正准确率公式原文错误"（预测对 + 预测对正常）"→"（预测对故障 + 预测对正常）"
  - 第五节：训练 F1 vs **预测 F1**（用户指出应对比 F1 而非准确率）——训练 F1（154 行未知测试子集=泛化能力）vs 预测 F1（768 行含 614 行训练数据会虚高），举例 xgboost 0.7778→0.7692、CatBoost 0.6929→0.9199
  - **删除**"测试说明：预测集与训练集 614 行重叠"段（用户要求去掉）
  - 第六节结论、第八节结论统一为"选模型依据训练 F1（xgboost 最高）"，修正原"建议优先选用 CatBoost/hgb"为"优先选用 xgboost"
- `test_report.html` + `测试报告.docx`（45KB）同步

### 遗留
- 临时脚本保留在 /home/yhz/iot/（_ai_*.py、_gen_docx.py），用户拒绝清理
- deployed_models/admin 当前有全部 10 个模型（含重复 xgboost/CNN）；test 模型下线按钮残留（无实际文件）