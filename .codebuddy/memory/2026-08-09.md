# 2026-08-09 工作日志

## 1. AI 模块代码修改回顾（本次会话）
- 本次会话对 AI 模块（solo_ai_iot）的代码修改仅 1 处：
  - `banckend/pipeline.py`（08-08 15:20 修改）：`label_column=auto_tag` 占位符回退修复
  - 修复内容：前端训练默认传 `label_column=auto_tag`，pipeline 原逻辑把它当真实标签列导致训练失败。改后：`explicit_label != 'auto_tag'` 才用显式值，否则回退配置文件 tagColumn；config_info 写 label_column_final
- 其余均为测试脚本和测试报告修改，非 AI 模块代码

## 2. 测试脚本整理（用户要求）
- 新建 `test-scripts/` 目录，按模块归档：
  - `test-scripts/ai-module/`（14 个 _ai_*.py + 2 个结果 json）：前端自动化、准确率计算、结果汇总
  - `test-scripts/data-module/`（7 个）：_test_*.py、_v*.py、clean_nifi_flows.py
  - `test-scripts/utils/`（4 个）：_gen_docx.py、_migrate_tagged.py、_probe.py、_clean_remaining.py
- 根目录残留（未删，用户拒绝删除）：debug.yaml、_probe.log、__pycache__、test_*.py 若干、test_report_local.html 等

## 3. 数据处理模块提交 GitHub
- 提交 e04e41d：17 个文件（v1-backend 11 + v1-frontend 3 + docker/nifi 1 + real_nifi_conf 3），628 insertions
- 已 push 到 origin main（git@github-yhz:TLMOA/AI.git）
- 提交内容：hasTag 独立顶层目录 + datasetName 字段 + xattr 双写 + NiFi 容器真实执行
- 未提交：.a/问题.md、项目框架.md（文档）、test-scripts/、test_report.html、各种 docx、pima-dataset.csv 等

## 残留文件清理（用户确认删除）
- 已删除：debug.yaml（Playwright 页面快照）、_probe.log（API 探测日志）、__pycache__（字节码缓存）、test_report_local.html（旧版报告）、test_run.log、test_upload.csv
- 已归档 10 个 test_*.py → test-scripts/data-module/（端到端/NiFi/hasTag 测试）
- 根目录仅剩 test_report.html（正式报告，保留）
- test-scripts 最终：ai-module 16 + data-module 18 + utils 4 = 38 文件

## 4. 文档整理（用户要求）
- 归档：V1执行清单→docs-archive/V1-已完成、V2执行清单→docs-archive/V2-规划未落地、AI模块详细实施方案/AI模块综合执行方案/设计文档-IoT智慧平台AI模块/README.md/智慧数据平台设计文档.doc→docs-archive/旧方案文档、旧测试结果 json+pima-dataset.csv→docs-archive/旧测试结果
- 保留：项目框架.md（已更新 tagged 顶层目录+AI模块+69/69）、需求-最终版.md、智慧数据平台设计文档.md、codebuddy.md、测试报告.docx、每日工作进度/
- 项目框架.md 更新：目录结构、hasTag 规则、关键函数、数据流转、Bug 列表（新增 17-19）、新增 §13 AI 大模型模块、测试报告状态

## 5. 安全审查（用户要求，2026-08-09）
发现严重问题（见 /home/yhz/iot/安全审查报告.md）：
1. JWT 密钥默认值 dev-secret-change-me（auth.py L22，override.conf 未设 IOT_SECRET_KEY）
2. 前端 app.js L1199-1206 硬编码 202.113.76.55 数据库凭据（8 组）
3. CORS allow_origins=["*"]+allow_credentials=True（main.py L1162）
4. 路径穿越：_normalize_username 不过滤 ../，_resolve_user_storage_root 直接拼接（main.py L1355/L275）
5. DB 导出 SSRF：MySQLExportReq host/port 任意（main.py L5320）
重要：无 TLS、MySQL root/root、NiFi admin@nifi123、NiFi 挂载整个 /home/yhz、无 IP 限速、无审计日志
已正确项：NiFi 禁匿名、登录失败锁定、Cookie httponly+samesite、下载权限校验、上传文件名过滤

## 6. 工厂部署文档（用户要求 2026-08-09）
- 生成 /home/yhz/iot/工厂部署文档.md（670 行，15 章）：
  - 架构总览/硬件要求/依赖安装/数据处理模块部署/AI 模块部署/NiFi 容器/数据库初始化
  - 安全加固清单（12 项）/生产配置（Nginx TLS）/启动自检/服务管理/备份恢复/监控日志/故障排查/上线检查清单
- 部署文档路径已统一为 /home/iot/（26 处，无 /home/yhz 或 /home/yang 残留）

## 遗留
- 根目录未跟踪文件：.codebuddy/、.playwright-cli/、各种 docx、pima-dataset.csv、nifi_verify_result.json 等，用户可自行决定