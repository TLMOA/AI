# 2026-09-13

## 交付物复核 + 「卸载包不单独传」定稿

### 复核结果（全部通过）
- `iot-install.run` 实测 sha256 = `2d4d117a…f81`，与 `.sha256` 文件一致（1,757,719,425 字节）
- `iot-offline-20260912.tgz` 实测 sha256 = `7ada96db…f28`，与 `.tgz.sha256` 一致（1,757,717,045 字节）
- `.run` = 69 行头部脚本 + `__IOT_ARCHIVE_BELOW__` 标记 + 上述 tgz **原样拼接**；
  `tail -n +70 iot-install.run | cmp iot-offline-20260912.tgz` **退出码 0** → 内嵌归档与已验证的 tgz 逐字节相同
- 包内顶层只有白名单 5 个目录（`v1-backend` `v1-frontend` `docker` `real_nifi_conf` `deploy-kit`）；
  无 `.venv` / `app.db` / `__pycache__` / `.codebuddy` / `*.run` / `*.sha256`（无自嵌套）
- 解包后 `diff -rq` 与工作区对比：deploy-kit 内**所有脚本逐字节一致**，仅文档有差异（见下）

### 已修正
- `deploy-kit/部署文档.txt`（并同步顶层副本）：把「重跑」提为**首选**并加粗标题，卸载降为「最后手段」；
  开头「需要传的 3 个文件」补注「其他脚本都在包里，包括卸载脚本，不需要单独传」；
  卸载段补「脚本随包提供，不用单独传输」。
  → 与用户决定一致：**卸载包不单独传，部署有不足直接重跑**
- `.codebuddy/memory/MEMORY.md` 已超限被截断 → 重写精简（151 行 → 约 115 行）：
  合并 3 条重复的「交付物」条目、去重 Nginx SNI / ODBC / sasl 等重复段、
  新增「交付与重跑原则」与「当前交付物（含实测 sha256）」小节。

### 遗留（有意不处理）
- 包内 `deploy-kit/部署文档.txt` 是 09-12 19:32 打包时的版本，比工作区少 10 行（卸载段落）。
  起初判断不值得重打 1.7G 包；**用户随后决定重打（"重新打包好，不用考虑新机器"）** → 见下。

---

## 重打包完成（用户要求，不考虑目标机重传成本）

### 产物
```
/home/yhz/iot-offline-20260913.tgz   1,757,712,197 字节  sha256 d98a9b81579b28070f5bed89649441fe2cd2721986e36999b0b23849b012d138
/home/yhz/iot-install.run            1,757,714,577 字节  sha256 866444bff59731071168dcafcaf0b0a21058b2d004ac5407a37ea05f8b9ba77a
/home/yhz/iot-offline-20260913.tgz.sha256 / /home/yhz/iot-install.run.sha256  （均已生成，与实测值一致）
```

### 步骤
1. `cd /home/yhz/iot/deploy-kit && ./make_offline_package.sh`（输出到 `/home/yhz`，约 2.5 分钟）
2. `./make_selfextract.sh /home/yhz/iot-offline-20260913.tgz /home/yhz/iot-install.run`（约 1.5 分钟）
3. `sha256sum iot-install.run > iot-install.run.sha256`
   ⚠️ `make_selfextract.sh` **不生成** `.sha256`，必须手工补
   ⚠️ 该环境的坑：`sha256sum ... | tee 文件` 会返回 **255** → 改用 `sha256sum > /tmp/x` 再 `cp`

### 校验（全部通过）
- `.run` 第 69 行是 `__IOT_ARCHIVE_BELOW__`；`tail -n +70 iot-install.run | cmp <tgz> -` → **rc=0**
- 包内 `部署文档.txt` 与工作区**逐字节一致**（615 行 / 28,714 字节）→ 旧文档问题已消除
- 包内 `deploy.sh` / `check_env.sh` / `install_sysdeps.sh` / `install_odbc.sh` / `setup.sh` /
  `uninstall_all.sh` / `test_e2e.sh` / `tools/patch_no_default_admin.py` 全部与工作区一致
- 顶层仅白名单 5 目录；无 `.venv`/`app.db`/`__pycache__`/`.codebuddy`/`*.run`/`*.sha256`/`*.log`
- 大件齐全：NiFi 镜像 tar ✓、sysdeps 135+108+83 deb ✓、wheels py310/311/312/313 = 51/50/50/50 ✓

### 注意
- 旧 tgz `iot-offline-20260912.tgz`(+.sha256) 仍在磁盘（约 1.7G），已被取代；旧 `.run` 的
  sha256 `2d4d117a…f81` 已作废（同名 `iot-install.run` 被覆盖）。旧 tgz 未删，等用户确认后再清。
- 交付仍是那 3 个文件：`iot-install.run` + `iot-install.run.sha256` + `部署文档.txt`。
