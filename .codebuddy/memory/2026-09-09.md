# 2026-09-09 工作记录

## 网站项目梳理 + 用户暴增问题排查

### 项目组成（2 个网站 + NiFi 基础设施）
- **主站 IoT 智慧数据平台**：v1-frontend(serve.py:5174) + v1-backend(FastAPI:8081, systemd iot-backend)
  - 后端 5 模块（auth/admin_routes/data_routes/metadata_routes/db_connect）+ executors（Mock/NiFi）
  - 前端页：index(仪表盘)、login、register、internal(内部管理)、tasks(任务监控)
  - 双模式：Local 即时 / NiFi 容器异步；xattr 加密元数据
- **AI 训练网站 solo_ai_iot**：frontend(Vue vite :3002) + backend(FastAPI :8002, conda iot_clone, GPU RTX A5000)
  - 10 算法训练 + leaderboard + 一键部署 IoT + 批量预测
- **NiFi**(docker :8080) 为基础设施，非独立网站

### 用户暴增根因（已查明）
内部管理页 `/api/v1/internal/users` 合并多源用户：
- 本地 SQLite `v1-backend/data/app.db` 的 `iot_users`：~40 个（绝大多数是 2026-07~08 自动化/Playwright 测试账号，如 testa_/lock_/qatest_/pimauser_/nf_user_，仅 zzz/hhh 等少数真实）
- MySQL `nifi` 库：`accounts`(44 行，与 SQLite 同批测试用户重复，UTC 时间戳晚 8h)、`users`(1 行 admin)、`iot_users`(1 行)
- 后端 AUTH_DB=nifi，auth 读写 MySQL；`_get_all_nifi_db_users()` 扫描 `accounts/users/user/...` 等表名，把非平台表也当作用户拉入
- 去重仅按 username lower，所以 accounts 与 SQLite 同名不翻倍，但测试账号总量仍 ~40-45，远超用户记忆中的"几个"

建议：清理 SQLite 与 MySQL 测试账号；或在 `_get_all_nifi_db_users` 中收窄扫描范围（只用 `iot_users`，排除 `accounts`/`users`）。

### 实施进展
- 已做方案 A：将 `auth.py` 的 `_get_all_nifi_db_users` candidate_tables 收窄为仅 `["iot_users"]`，重启后端生效。但实测**方案 A 几乎不影响列表数量**。
- 真正根源：SQLite `iot_users` 共 43 行，其中 **38 个测试账号**（testa_/lock_/qatest_/pimauser_/nf_user_/fulltest_/manualtag_ 等），仅 5 个疑似真实（admin/hhh/wangwu/zhaoliu/zzz）。MySQL `iot_users` 仅 admin（已在 SQLite 去重）。
- 磁盘 77 个含 nifi-data 目录中，绝大多数是软件/项目目录（miniconda3/vscode-server/mysql-connector 等）被误建子目录，非用户；仅 38 测试账号 + 5 真实账号在用户库。
- 用户已确认"注册即在根目录建文件夹，故能看出多余用户"。

### 执行结果（已完成）
- 方案 A：auth.py `_get_all_nifi_db_users` candidate_tables 收窄为仅 `["iot_users"]`，重启后端生效。
- 方案 B：用后端 DELETE /api/v1/internal/users/{u}?purge_data=true 删除 38 个测试账号（脚本 /tmp/clean_test_users.py，走 admin 登录拿 cookie）。全部 ok=38 fail=0。
- 删除接口逻辑：删 SQLite 行 → `_delete_nifi_db_user` 清 MySQL accounts/users/iot_users → 删 /home/yhz/{u} 文件夹。
- 残留处理：`testauto_0724173911` 因只在 MySQL 不在 SQLite 被 404 跳过，后单独 `DELETE FROM accounts` 清除（其磁盘文件夹用户拒绝删除，保留）；`888` 有真实打标数据→判为真实账号保留。
- 最终：SQLite 5 用户(admin/hhh/wangwu/zhaoliu/zzz)；MySQL accounts 5 行(888/hhh/wangwu/zhaoliu/zzz)；内部管理页现仅显示 5 个真实用户。磁盘 38 测试文件夹已删，testauto 文件夹保留。

### 关键事实澄清（用户原误以为 SQLite 无关）
- 登录 `IOT_AUTH_DB=nifi`：先查 MySQL(iot_users/users/accounts/user…)，miss 才回退 SQLite。真实用户 hhh/wangwu/zhaoliu/zzz 实际靠 MySQL `accounts` 认证；admin 在 iot_users+users。
- 注册双写：MySQL + `_sync_user_to_local` 写 SQLite；登录成功还会把 NiFi 用户同步回 SQLite。故 SQLite 是同步副本+兜底，方案 B 必须清 SQLite。
- 注册建文件夹：public 模式建 /home/yhz/{u}/（4 数据根×12 子目录）；private+Ceph 模式根改为 ceph_endpoint 远端，不在本地。

### 注册统一到 MySQL iot_users（已完成并验证）
- 用户最终指示：统一到 MySQL 的 `iot_users`，其它不管，只要注册能正常建文件夹。
- `auth.py` `register()` 的 `candidate_tables` 由多表改为仅 `["iot_users"]`，注册只写这一张表。
- MySQL `iot_users` 已 ALTER 增加 `deployment_mode`/`ceph_endpoint`/`failed_attempts`/`locked_until` 列。
- **踩坑**：register 的写入逻辑被误缩进到 `except` 块内（12 空格，应为与 `with` 同级的 8 空格），导致成功路径直接落到 `raise 500 "在 NiFi DB 中写入用户失败"`，且 `_errs` 未定义。用脚本对 552-600 行整体 dedent 4 空格修复。
- **注册验证通过**：POST /api/v1/auth/register {"username":"regtest_verify","password":"...","deployment_mode":"public"}
  返回 `{"success":true,"table":"iot_users"}`；MySQL `iot_users` 有该行；`/home/yhz/regtest_verify/` 建出 nifi-data/real_nifi_data/tagged_nifi_data/tagged_real_nifi_data/meta_backups。该测试用户已清理。
- 清理了调试期遗留的 `print("DBG ...")` 与 `_errs` 收集逻辑，异常改为直接 raise 500 带原因。

### 内部管理接口认证要点（重要）
- `_require_admin(request)` 读的是 **cookie `access_token`**，不是 `x-access-token` 请求头！
- 登录 POST /api/v1/auth/login 响应体**不含 token 字段**（只有 success+user），token 通过 **Set-Cookie** 下发。
- 正确调用姿势：`curl -c cj.txt -X POST .../auth/login` 存 cookie，再 `curl -b cj.txt -X DELETE /api/v1/internal/users/{u}`。
- 删除接口 DELETE /api/v1/internal/users/{u} body `{"purge_data":true}`：删 SQLite 行 → `_delete_nifi_db_user` 清 MySQL → rmtree /home/yhz/{u}。

### 清理 + 全站体检结果（已完成）
- 已删除 3 个调试测试账号 `regtest_tmp_001`/`regtest_dbg_001`/`regtest_exact_001`（均 data_purged=true + nifi_deleted=true）。
- **端到端注册验证通过**：`e2e_check01` 注册→立即登录成功→建出 4 数据根→MySQL iot_users 有行。证明统一到 iot_users 后认证链路没断（登录 candidate_tables 中 iot_users 排首位）。

### 全站现状（2026-09-09 体检）
| 项 | 状态 |
|---|---|
| 前端 serve.py 5174 | ✅ PID 2306，5 个页面(/、login、register、internal、tasks) 全 200，/api/v1 代理通 |
| 后端 uvicorn 8081 | ✅ PID 3421893，/docs 200 |
| NiFi 容器 | ✅ Up，worker 脚本在位 |
| 内部管理页用户 | ✅ total=5（admin/hhh/wangwu/zhaoliu/zzz），测试账号已绝迹 |
| 用户登录 | ✅ zzz/hhh 正常 |

### 用户数据源分布（关键，勿误判为异常）
- MySQL `iot_users` 现**只有 admin**（历史真实用户不在此表）。
- 真实用户 `hhh/wangwu/zhaoliu/zzz` 靠 MySQL **`accounts`** 表认证（accounts 有 888/hhh/wangwu/zhaoliu/zzz）；`users` 表只有 admin。
- SQLite `iot_users` 有 5 个（admin/hhh/wangwu/zhaoliu/zzz）作为同步副本。
- 内部管理页 = MySQL iot_users(1) ∪ SQLite(5) = 5，符合预期。**不要让"MySQL iot_users 只有 admin"误导为数据丢失**。
- `wangwu` 用 wangwu/wangwu 登录报"用户名或密码错误"，疑为历史密码非同用户名，非系统故障（zzz/hhh 同法登录成功）。

### 遗留
- 测试账号 `e2e_check01` 仍在（MySQL iot_users + /home/yhz/e2e_check01），用户拒绝删除命令，待其自行处理。
- 用户通过内部管理页删除了老账号 `wangwu`：验证 /home/yhz/wangwu 文件夹 + MySQL accounts + MySQL iot_users + SQLite 四处置均清空，删除彻底。内部管理页现仅 4 个用户（admin/hhh/zhaoliu/zzz）。

### 部署方案：⚠️ 此前我理解错了，以此为准

用户说的"B"**不是**"每厂一套实例"，而是 **单实例多租户**：
- 部署**一套**平台
- **忽米掌握管理员账号**（admin 角色）
- **每注册一个用户 = 一个工厂租户**
- 即集中托管 SaaS，租户靠 `/home/yhz/{工厂}/` 目录 + `_check_file_access` 隔离

#### 已交付：deploy-kit 可复用部署工具包
`/home/yhz/iot/deploy-kit/`（纯新增，未动现有项目代码），已通过语法校验与补丁实测：
- deploy.sh / add_factory.sh / verify.sh（bash -n 通过，已 chmod +x）
- tools/userctl.py（实测连 MySQL 成功，list 返回 admin）、render.py、patch_private_storage.py
- 私有化补丁在 /tmp 副本实测：正确匹配、二次执行幂等 skip、补丁后 py_compile 语法 OK
- 关键发现：requirements.txt 缺 SQLAlchemy/PyJWT/bcrypt/cryptography/APScheduler，新机器装完后端起不来；已补 templates/requirements-runtime.txt

#### deploy-kit 完善（回答"能否交给别的 agent 直接装"）
首版有三个致命缺口，已补齐：
1. **`IOT_ALLOW_SELF_REGISTER` 后端从未读取** —— 只在 override.conf 配变量等于没关注册，注册仍对任何人开放。
   → 新增 `tools/patch_disable_self_register.py`，在 register() 开头插环境变量判断（目标串已用只读检索确认在 auth.py 527-530 精确匹配）。
2. **前端注册入口没处理** —— 新增 `tools/disable_register_page.py`：register.html→.disabled + 注释 login.html:37 的 registerBtn。
3. **新机器缺基础软件** —— config 新增 `INSTALL_SYSTEM_DEPS=true` 自动 apt 装 python3/nginx/mysql/docker；加 `docker compose` 与 `docker-compose` 兼容。
另：README 新增第 0 章「能否交给别人装」——明确三件必须确认的事（代码如何到目标机/SSH key 或离线包、MySQL root 登录方式、基础软件），
并诚实标注"尚未在干净机器上端到端跑过"。修掉 config.env.example 中 ALLOW_SELF_REGISTER 重复定义。

#### 离线交付：用户选定全离线打包方式，已出包
- 新增 `deploy-kit/make_offline_package.sh`，已生成 **`/home/yhz/iot-offline-20260909.tgz`（80M）** + `.sha256`
- 打的是当前工作区文件，**不需要先 git commit**（今天 auth.py 的修复直接进包），这点对"未提交就交付"场景很关键
- 排除后体积从 6.0G 降到 80M，主要瘦身项：
  `v1-backend/data/app.db`(2.6G，含本机4个用户账号，**绝不能外带**) / `docker/hadoop/images/hadoop-stack.tar`(2.7G，仅 HDFS 数据源才需) / `backup_sqlite`(74M) / `.venv`(384M) / `.git`(91M)
- 保留 `real_nifi_conf/`（NiFi flow 配置，部署必需，1.7M）
- 已验证包内容：deploy-kit 21 个文件在；app.db/hadoop-stack/.git/.venv 均无；deploy.sh、auth.py、serve.py、Dockerfile 齐备
- 需 Hadoop 时加 `--with-hadoop`（回到 ~3G）
- 工厂机器解压必须落到 `/home/yhz` 变成 `/home/yhz/iot`（硬编码路径），否则要同步改 config.env 三个路径变量

#### 部署资产盘点（实测，供复用）
| 资产 | 路径 | 状态 |
|---|---|---|
| 后端 systemd unit | v1-backend/deploy/iot-backend.service | ✅ User=yhz，绑 **127.0.0.1:8081** |
| 后端 override | v1-backend/deploy/iot-backend.service.override.conf | ✅ 但 ExecStart 覆盖为 **0.0.0.0:8081**（覆盖掉 127.0.0.1，暴露面变大） |
| 后端健康检查 | iot-backend-health.service/.timer/.sh | ✅ 有 |
| **前端 systemd unit** | v1-frontend/deploy/iot-frontend.service | ❌ **不存在**（全项目搜 0 结果），故 install_autostart_services.sh 第3步必失败 |
| 一键安装脚本 | scripts/install_autostart_services.sh | ⚠️ 仅装 systemd，且上述缺失会中断 |
| NiFi compose | docker/nifi/docker-compose.yml | ✅ |
| 主站 Nginx 配置 | — | ❌ 无现成文件（只有 docker/nifi/nginx/ 给 NiFi 用的） |
| MySQL 建表 SQL | — | ❌ 无脚本，只有文档内 SQL |
| 工厂部署文档 | 工厂部署文档.md | ✅ 完整 15 章 |

#### 路 B 硬编码清单（搬到别的服务器必改）
- `iot-backend.service`：`User=yhz` + `WorkingDirectory=/home/yhz/iot/v1-backend` + ExecStart 绝对路径
- `override.conf`：ExecStart 绝对路径
- `docker-compose.yml`：`/home/yhz/iot/real_nifi_conf`、`/home/yhz/real_nifi_data`、`/home/yhz:/home/yhz:rw`
- `docker-compose.yml`：`NIFI_WEB_PROXY_HOST` 默认含 **202.113.76.55:9443**（我方服务器 IP，出厂必须改）
- 后端代码 `IN_DATA_BASE_DIR` 默认 `/home/yhz` —— **可用环境变量覆盖**
- 取巧最优解：每厂服务器都建同名用户 `yhz`、代码放 `/home/yhz/iot`，全部硬编码天然生效，只需改 proxy host 与密钥/密码。

#### 关键架构权衡（已向用户说明）
- 路 B 只保证数据**物理不出厂**；若忽米持有管理员账号，仍可登录该厂实例查看全部数据（admin 在 `_check_file_access` 全放行）。若工厂要求"忽米也看不了"，则管理员账号不能交忽米，需另做权限收敛。
- AI 模块建议：训练集中到忽米（GPU 贵），工厂只跑数据处理+打标（NiFi worker，不需 GPU），模型下发到各厂，可省掉每厂 A5000 成本。
