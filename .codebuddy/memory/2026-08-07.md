# 2026-08-07 工作日志

## 新需求 4 项完成

### 1. 打标产物按原文件子目录放标签目录
- `_write_tagged_output` / `_write_edited_output`：从 source_meta.storagePath 提取父目录名，打标产物写到 `tagged_*_data/<原目录>/`
- NiFi 打标回流（auto_tag）也按原目录
- `nifi_auto_tagging_worker.py`：按 sourcePath 父目录输出到 `/home/yhz/tagged_real_nifi_data/<目录>/`
- 手动/自动打标产物继承原文件 datasetName
- 验证：admin/Local 自动打标→tagged_nifi_data/inbox_csv ✅, zzz/Local 手动打标→tagged_nifi_data/inbox_csv ✅, admin/NiFi 自动打标→tagged_real_nifi_data/inbox_csv ✅

### 2. AI 训练网站确认 + 训练/预测
- `/home/yhz/solo_ai_iot/`：FastAPI 8002 + Vue 3002
- 启动：后端用 `conda env iot_clone`（含 ludwig+optuna），前端 `npm run dev`
- GPU：NVIDIA RTX A5000 24GB
- 训练：pima 糖尿病集（768 行），xgboost，F1=0.7778 Acc=0.7857，614/154 训练测试分
- 预测：无标签 pima 768 条，3 模型诊断，故障 1004 条
- 关键：AI 网站数据集需配套 `.meta.json`（tagColumn/datesetname/failedTag）；预测文件同理

### 3. admin/local 真实上传糖尿病集
- 有标签：`admin/tagged_nifi_data/inbox_csv/raw_admin_pima_tagged_*.csv`（datasetName=糖尿病预测数据集）
- 无标签：`admin/nifi-data/inbox_csv/raw_admin_pima_notag_*.csv`

### 4. 测试报告更新
- 新增 四-C 打标产物目录规则验证（4/4）、四-D AI 训练网站训练+预测章节
- 总览：69/69 全部通过 + AI 训练/预测成功

### 关键教训
- AI 训练网站依赖：iot_ai 环境缺 optuna，iot_clone 完整
- 训练接口 `/api/train/start` 需 folder_name + 数据集 .meta.json
- 预测接口 `/api/predict/batch_file` 需预测文件 .meta.json 的 datesetname 匹配已部署模型

## 修正：用真实上传文件训练+预测（21:00-21:55）
- 用户指出 3 问题：①训练数据用了手动复制的 pima_diabetes 而非真实上传文件 ②预测用了 3 个历史模型而非刚训练模型 ③1004 故障是 3 模型累加错误
- 修正：
  - 训练用真实文件 `admin/tagged_nifi_data/inbox_csv/raw_admin_pima_tagged_20260807_205405.csv`（folder_name=admin/tagged_nifi_data/inbox_csv）
  - pipeline 修复：显式 label_column 优先（真实文件标签列是 Outcome 非 tag）；兼容 datasetName 驼峰字段
  - 重启 AI 后端（旧进程占用 8002 需 kill 后重启）
  - 部署刚训练模型 `/api/deploy`
  - 预测用真实无标签文件 `admin/nifi-data/inbox_csv/raw_admin_pima_notag_20260807_205405.csv`，meta 加 datesetname 匹配刚训练模型 + failedTag=1 + tagColumn=Outcome
- 结果：训练 xgboost F1=0.7778 Acc=0.7857；预测 1 个模型诊断 768 条，故障 343 条（非 1004）

## 测试报告重写（22:30）
- 用户要求去掉：生成时间、测试工具(Playwright/JWT)、SQLite测试库行、全面排查其他发现、建议
- 去掉所有 AI 痕迹（AI→模型）
- 重写为 10 章节干净报告：环境/双模测试/容器验证/hasTag顶层目录/打标目录/模型训练/xattr/Bug追踪/结论
- 总览 69/69 + 模型训练/预测成功

## 补充
- 打标 manual 接口 records 需字符串 rowId（Dict[str,str]）