# 2026-08-04 工作日志

## IoT 智慧平台全功能验证

### 后端 API 测试（50/50 通过）
- 直接打 8081 端口，覆盖上传/打标/文件管理/训练/DB导出/定时任务/静默导出/登出/用户管理/内部管理/认证异常

### 前端 Playwright 测试（9/9 通过）
- 通过 Playwright 浏览器实际操作前端页面 (5174)
- 用 JWT Cookie 注入方式登录（因 chromium headless shell 下表单 fill+click 登录不跳转）
- 操作了：文件上传、自动打标（填6条规则）、DB测试连接、训练提交、内部管理页、登出
- 报告: `/home/yhz/iot/test_report.html`

### 打标规则（修正版）
- 去掉 "Glucose>0" 通杀规则，改用正确医学阈值
- Outcome=1→高风险, Glucose>=7.0→高风险, BMI>=28→高风险
- Glucose>=6.1→中风险, BMI>=25→中风险, BP>=130→中风险, 默认→低风险
- 198/200高风险是正确结果（pima数据本身Glucose偏高）

### 关键发现
- admin 部分上传产生空文件（0字节），普通用户上传正常

### 修复 + 复测（8/4 晚）
- 后端已重启（PID 3262208），原子写入修复（tmp+rename）在 Local 与 NiFi 分支均已生效
- 重启后连续前端上传测试无新 0 字节文件；清理了 17:xx 历史遗留的 8 个空 csv
- 完成 **完整前端测试套件** test_frontend_full.py（Playwright 实际操作前端 5174，JWT Cookie 登录）：23/23 通过
  - DB导出 5 种类型前端点击测试连接（mysql/postgres/sqlserver/oracle/sqlite 后端可达；无真实库时 code 为非0属预期）
  - SQLite 列出表 + 导出（前端点击）
  - 定时任务创建 + 任务中心刷新
  - 手动打标逐行编辑保存（updated=3）
  - 6 种上传转换类型 csv/json/tsv 互转
  - 内部管理页：刷新列表/拉取私有数据/静默导出开关/清单
- 报告: /home/yhz/iot/test_report.html（已逐项核查产物路径与内容）

### 产物核查结果（23:00 复验）
- 认证: 正常（admin/admin, is_admin=true）
- DB导出: SQLite 连接成功，列出5张表，exports/ 下有导出 csv 文件
- 定时任务: 18个任务已创建，export_jobs/ 目录就绪
- 手动打标: tagged_output/tag_admin_*225220*.json 36418字节，标签分布: 正常=2 故障=1 未标注=197（只改3行，符合预期）
- 自动打标: tagged_output/tag_admin_*171057*.csv 8269字节，标签分布: 高风险=198 中风险=1 低风险=1（6条规则正确执行）
- 转换: 6种互转格式均有产物（csv_to_json 74, csv_to_tsv 4, json_to_csv 4, json_to_tsv 4, tsv_to_json 4, tsv_to_csv 4）
- 内部管理: 刷新/拉取/静默导出开关/清单 均可用，silent_exports/ 为空（无已注册表，属预期）
- 空文件: 当前0字节csv数量=0

### NiFi 模式完整验证（23:11 切换）
- 切换成功，mode=nifi
- **24/24 前端验证通过**（对标 Local 模式全量 23 项 + 模式确认 1 项）
- DB 导出 5 种类型全测、定时任务创建+刷新、手动打标（3行编辑）、6 种转换、内部管理 5 按钮
- 上传 pima 落盘 `real_nifi_data/inbox_csv/raw_admin_pima_20260804_231919.csv`（23875B 非空）
- 手动打标产物 `tagged_output/tag_*232717*.json`（36418B），标签分布：正常=2/故障=1/未标注=197（与 Local 一致）
- 转换/打标依赖 NiFi 容器 flow 异步执行，文件入 inbox 等待消费
- 全量 0 字节 csv = 0（nifi-data + real_nifi_data 均已清理）
- 报告已更新为完整双模对比版（逐项并列）

### DB导出目录落点实测（23:41 四组实测）
- 用 /tmp/verify_export_test.db 触发 export_generic（manual 立即导出）
- Local 模式 ✅ 全对：
  - admin/local → `/home/yhz/admin/nifi-data/output_csv/export_admin_verify_t_*.csv`
  - zzz(普通)/local → `/home/yhz/zzz/nifi-data/output_csv/export_zzz_verify_t_*.csv`（用户隔离正确）
- NiFi 模式 ⚠️ 不落盘：
  - admin/nifi、zzz/nifi 的 `real_nifi_data/output_csv/` 均 0 文件
  - 根因：export_generic line 5393 `if mode=="nifi": _submit_nifi_export_task(job)` 走异步提交，返回 PENDING，依赖 NiFi 容器 nifi_db_export_worker 真实执行；当前环境无 flow 消费，故不落盘
  - 与转换/打标同样的"容器 flow 未真正跑"问题
- 结论：Local 模式 admin/普通用户均正确落 output_csv；NiFi 模式手动导出仅提交任务不落盘（设计如此，需真实 NiFi 容器才落盘）
- 注：exports/ 历史目录为早期遗留双写残留，主产物应在 output_csv/

### 备注
- 登出后 /api/v1/files 仍可匿名访问（不强制认证）
- /api/v1/training/files 也不强制认证
- 用户硬性要求：所有测试必须通过前端（Playwright），不能直接打后端 API 验证功能可用性
