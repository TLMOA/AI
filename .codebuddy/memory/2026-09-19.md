# 2026-09-19

## 📤 工作区改动推送到 GitHub（main 分支）

**提交（2 个，共 53 文件 / +9785 −104）**
```
9d9a760 chore: 提交 CodeBuddy 本地记忆(2026-09-09 ~ 09-17 工作记录与长期记忆)
1aaa97e feat(deploy-kit): 离线部署工具包 + 第12版修复（密码同步/as_user/登录页补丁/ODBC三套闭包）
```
**推送结果**：`716fa6c..9d9a760  main -> main`（远端 `origin = git@github-yhz:TLMOA/AI.git`）；
`git status -sb` = `## main...origin/main`，HEAD 与 origin/main 均为 `9d9a760`。

**入库内容**：`deploy-kit/`（工具包本体：deploy/setup/check_env/verify/smoke/test_e2e/uninstall_all、
templates/、tools/（含新的 `lib.sh` 与 `sync_site_password.py`）、testdata/sample.csv、README、部署文档）、
顶层 `uninstall_all.sh` 与 `部署文档.txt`、`v1-backend/app/auth.py`、`.gitignore`、全部 memory 文件。

**⚠️ 有意排除的（决策记录）**：`deploy-kit/offline-assets/` 已加入 `.gitignore`
—— 它 2.2G（deb 455M / 424 个 + wheel 114M / 201 个 + NiFi 镜像 tar 1.6G），
全部由 `prepare_offline_assets.sh` / `prepare_sysdeps.sh` / `prepare_odbc_assets.sh` 生成，
仓库只存**生成脚本**不存产物（`.run/.tgz/.tar/.sha256` 原本就已被忽略）。
入库前实测：暂存区最大文件仅 34KB、总 380KB，无大二进制混入。

**顺带清理**：删掉了测试遗留的临时目录 `.tmp-pwdsync2`（此前那次中断没走到自清理）。
