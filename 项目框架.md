# IoT 智慧数据平台 — 项目框架

> 基于项目当前真实代码与部署状态整理，反映系统实际形态。最后更新：2026-08-05。

---

## 1. 项目定位

面向工业场景的多租户数据标注与管理平台。支持多源数据接入（8 种数据库导出 + 文件上传）、手动/自动标签标注、xattr 加密元数据存储，为下游大模型训练提供标准化数据资产。支持 Local（即时处理）和 NiFi（容器异步处理）双模式运行。

---

## 2. 服务端口与启动方式

| 端口 | 服务 | 启动命令 | 备注 |
|------|------|----------|------|
| 5174 | 前端 serve.py | `cd v1-frontend && nohup python3 serve.py > /tmp/frontend.log 2>&1 &` | **必须用 serve.py**，内置 /api/v1/ → 8081 代理 |
| 8081 | 后端 FastAPI | `systemctl restart iot-backend` 或 kill uvicorn 让 systemd 自动拉起 | 环境变量从 `deploy/iot-backend.service.override.conf` 读取 |
| 8080 | NiFi HTTPS | `cd docker/nifi && docker compose up -d` | NiFi 2.8.0，admin/admin@nifi123 |

> **🚨 严禁用 `python3 -m http.server 5174` 替代 serve.py**。serve.py 内置 API 代理，确保远程浏览器（如 tailscale）的 API 请求正确转发到后端 8081。

### 2.1 前端代理链路

```
远程浏览器（tailscale）                服务器
┌──────────┐     HTTP      ┌──────────────────────┐
│ 100.121. │ ──────────→  │ serve.py :5174        │
│ 225.98   │ ←──────────  │   ├─ 静态文件 → v1-frontend/  │
│ :5174    │               │   └─ /api/v1/*  → proxy → 8081  │
└──────────┘               └──────────────────────┘
                                      │
                                      ▼
                           ┌──────────────────────┐
                           │ uvicorn :8081        │
                           │ (iot-backend)        │
                           └──────────────────────┘
```

---

## 3. 顶层目录结构

```
/home/yhz/
│
├── admin/                              ← 管理员用户
│   ├── nifi-data/                      ← Local 无标签数据（详见 §3.1）
│   ├── real_nifi_data/                 ← NiFi 无标签数据（详见 §3.1）
│   ├── tagged_nifi_data/               ← Local 有标签数据（独立顶层目录）
│   └── tagged_real_nifi_data/          ← NiFi 有标签数据（独立顶层目录）
│
├── zzz/                                ← 普通用户（示例）
├── 888/                                ← 私有化用户（示例）
├── hhh/                                ← 私有化用户
│   ├── nifi-data/
│   ├── real_nifi_data/
│   ├── tagged_nifi_data/
│   └── tagged_real_nifi_data/
│        ⋮
│   （每个根目录 12/12 子目录齐全）
│
├── tagged_real_nifi_data/              ← 全局 NiFi 标签产物（worker 写容器内 /home/yhz/tagged_real_nifi_data）
├── real_nifi_data/                     ← 全局 NiFi 共享工作区
│   ├── convert_jobs/{inbox,done,error}/
│   ├── export_jobs/{inbox,done,error}/
│   ├── tagging_jobs/{inbox,done,error}/
│   ├── csv_to_json/ csv_to_tsv/ ...    ← NiFi worker 输出（全局）
│   ├── tagged_output/                  ← 标签产物（全局，等待路由到用户）
│   │   └── {csv_to_json,output_csv,...}/
│   ├── bin/                            ← Worker 脚本（部署在容器内）
│   │   ├── nifi_db_export_worker.py
│   │   ├── nifi_upload_convert_worker.py
│   │   └── nifi_auto_tagging_worker.py
│   └── users/                          ← 用户目录软链接（容器内访问用）
│
├── iot/                                ← 项目代码仓库（详见 §3.2）
│   ├── v1-backend/                     ← FastAPI 后端
│   ├── v1-frontend/                    ← 前端（serve.py + 静态文件）
│   ├── v2-nifi/                        ← NiFi 方案与脚本
│   ├── docker/                         ← Docker 编排
│   ├── scripts/                        ← 部署辅助脚本
│   ├── V1执行清单/ V2执行清单/ V3执行清单/ V4执行清单/
│   ├── real_nifi_conf/                 ← 生产 NiFi 配置
│   ├── 项目框架.md                     ← 本文档
│   ├── 测试报告.html                   ← 测试报告
│   └── 每日工作进度/                    ← 工作日志
│
├── in_data/                            ← 用户监控目录（历史遗留）
├── nifi-data/                          ← 全局 Local 共享（旧，兼容保留）
└── miniconda3/                         ← Python 环境
```

### 3.1 用户数据目录结构（以 admin 为例）

```
/home/yhz/admin/
│
├── nifi-data/                          ← Local 模式（即时处理）
│   ├── inbox_csv/     90 文件 (804K)   ← CSV 上传源文件
│   ├── inbox_json/     4 文件 (28K)    ← JSON 上传源文件
│   ├── inbox_tsv/      4 文件 (28K)    ← TSV 上传源文件
│   ├── csv_to_json/   76 文件 (1.2M)   ← CSV→JSON 转换产物
│   ├── csv_to_tsv/    20 文件 (284K)   ← CSV→TSV 转换产物
│   ├── json_to_csv/    4 文件 (28K)    ← JSON→CSV 转换产物
│   ├── json_to_tsv/    4 文件 (28K)    ← JSON→TSV 转换产物
│   ├── tsv_to_json/    4 文件 (28K)    ← TSV→JSON 转换产物
│   ├── tsv_to_csv/     4 文件 (28K)    ← TSV→CSV 转换产物
│   ├── output_csv/    28 文件 (172K)   ← DB 导出 CSV
│   ├── output_json/    0 文件          ← DB 导出 JSON
│   ├── output_tsv/     2 文件 (16K)    ← DB 导出 TSV
│   ├── export_jobs/{inbox,done,error}/ ← 定时导出任务队列
│   ├── exports/        (历史残留)       ← 导出临时目录
│   ├── silent_exports/ 0 文件          ← 静默导出产物
│   ├── meta_backups/   0 文件          ← 元数据版本备份
│   └── （12 个子目录：inbox_csv/json/tsv、csv_to_json、csv_to_tsv、json_to_csv、json_to_tsv、tsv_to_json、tsv_to_csv、output_csv/json/tsv）
│
├── tagged_nifi_data/                   ← Local 有标签（hasTag=true）
│   ├── inbox_csv/     ← 标签源文件 CSV
│   ├── csv_to_json/   ← 标签转换产物
│   ├── output_csv/    ← 标签 DB 导出
│   └── （12 个子目录齐全）
│
└── real_nifi_data/                     ← NiFi 模式（容器异步处理，无标签）
    ├── inbox_csv/     72 文件 (840K)   ← 结构同 nifi-data
    ├── csv_to_json/   40 文件 (844K)
    ├── csv_to_tsv/    18 文件 (112K)
    ├── output_csv/    28 文件 (3.9M)
    │   ⋮ （上述 inbox_*、*_to_*、output_* 目录齐全）
    ├── convert_jobs/{inbox(0),done(0),error(0)}/  ← NiFi 转换任务
    ├── export_jobs/{inbox(0),done(0),error(0)}/   ← NiFi 导出任务
    ├── tagging_jobs/{inbox(0),done(0),error(0)}/  ← NiFi 打标任务
    └── bin/            ← Worker 脚本（容器内实际执行副本）

└── tagged_real_nifi_data/              ← NiFi 有标签文件（hasTag=true）
    ├── inbox_csv/  csv_to_json/  output_csv/ ...
    └── （12 个子目录齐全）
```

> **规则**：hasTag=true 时，**所有文件**（源文件 + 转换产物 + DB 导出）放入独立顶层目录 `tagged_nifi_data/`（Local）或 `tagged_real_nifi_data/`（NiFi）的对应子目录；hasTag=false 时放入 `nifi-data/` / `real_nifi_data/` 下的 inbox_* / *_to_* / output_* 中。打标产物按原文件所在子目录放置（原文件在 inbox_csv → 打标产物在 tagged_*_data/inbox_csv/）。

### 3.2 项目代码结构

```
iot/
├── v1-backend/                         ← FastAPI 后端（详见 §5）
├── v1-frontend/                        ← 前端静态页面（详见 §6）
├── v2-nifi/                            ← NiFi 方案材料
├── V1执行清单/～V4执行清单/            ← 分阶段实施文档
├── docker/                             ← Docker 编排
│   └── nifi/
│       ├── docker-compose.yml          ← 挂载 /home/yhz/real_nifi_data + /home/yhz
│       ├── Dockerfile
│       └── iot-nifi.service
├── scripts/                            ← 部署辅助脚本
├── real_nifi_conf/                     ← 生产 NiFi 配置
├── test-data/                          ← 测试数据
├── 项目框架.md                         ← 本文档
├── 测试报告.html                       ← 完整测试报告
├── pima-dataset.csv                    ← pima 糖尿病测试集
└── 每日工作进度/                        ← 工作日志
```

---

## 4. 存储架构

### 4.1 用户数据隔离

所有用户（公有化 + 私有化）数据存放在 `/home/yhz/{username}/` 下。注册时自动创建 4 个数据根目录：`nifi-data/`（Local 无标签）、`real_nifi_data/`（NiFi 无标签）、`tagged_nifi_data/`（Local 有标签）、`tagged_real_nifi_data/`（NiFi 有标签），每个根目录含 12 个子目录。

### 4.2 元数据存储（xattr 方案）

```
数据文件 sensor_data.csv
│
├─ 文件内容（cat 可见）: CSV/JSON/TSV 数据
│
└─ inode 扩展属性 xattr（cat/ls 不可见）
   ├─ user.meta     = AES-256-GCM 加密密文（主存储，AES-256-GCM）
   └─ user.checksum = SHA-256 校验码（防磁盘静默损坏）
   │
   └─ .meta.json    = 明文备份（xattr 不可用时回退）
```

- **Linux 生产环境**: 优先从 xattr 读取 → 解密 → 返回；写入时先写 xattr（主），再写 .meta.json（备份）
- **macOS/Windows 开发环境**: 自动降级为 .meta.json 文件
- **密钥管理**: 32 字节 AES 密钥，通过环境变量 `META_XATTR_KEY` 注入，不落盘
- **内部管理页**: 通过 `/api/v1/files/{id}/xattr` 接口查看真实扩展属性名（`user.checksum`、`user.meta`），证明真的在读取文件扩展属性

### 4.3 元数据字段

| 字段 | 类型 | 必须 | 说明 |
|------|------|:--:|------|
| fileId | string | 是 | 唯一标识 |
| fileName | string | 是 | 文件名 |
| sourceType | string | 是 | db_export / upload / manual_tag / auto_tag |
| storageType | string | 是 | plain / tagged |
| hasTag | boolean | 是 | 是否含标签（决定产物落 `tagged_nifi_data/` / `tagged_real_nifi_data/` 还是普通目录） |
| tagColumn | string | 条件 | 标签列 |
| tagName | string | 条件 | 标签名称 |
| tagRule | object | 否 | 自动打标规则 |
| columns | array | 否 | 列名列表 |
| rowCount | number | 否 | 行数 |
| fileSize | number | 否 | 文件大小（字节） |
| createdAt/updatedAt | string | 是 | ISO 8601 |

---

## 5. 后端架构

### 5.1 技术栈

| 组件 | 技术 |
|------|------|
| 框架 | FastAPI |
| 服务器 | Uvicorn（systemd 管理） |
| 数据库 | SQLite 主库 + 支持连接 MySQL/PG/SQLServer/Oracle/SQLite/Hive/HDFS/HBase |
| 认证 | JWT Cookie（bcrypt 哈希） |
| 元数据 | xattr + AES-256-GCM / .meta.json 降级 |
| 加密 | cryptography 库 |

### 5.2 代码文件

```
v1-backend/app/
├── main.py              ← 主入口，70+ API 路由，含双模式调度逻辑
├── auth.py              ← 认证、注册、用户存储目录创建
├── admin_routes.py      ← 管理员路由（用户管理、静默导出）
├── db_connect.py        ← 8 种数据库连接测试、表列举
├── db_models.py         ← SQLAlchemy 数据模型
├── meta_json.py         ← 元数据读写（优先 xattr，回退 .meta.json）
├── meta_xattr.py        ← xattr + AES-256-GCM 实现
├── nifi_orchestrator.py ← NiFi 容器 3 个 Flow 的部署与管理
├── export_worker.py     ← Local 模式导出执行器（run_export_job）
├── silent_export_worker.py ← 静默导出（NiFi 模式下投递任务给容器）
├── scheduler.py         ← 定时任务调度（APScheduler）
│
v1-backend/scripts/      ← NiFi Worker 脚本（部署到容器 bin/）
├── nifi_db_export_worker.py      ← 多数据源导出 Worker
├── nifi_upload_convert_worker.py ← CSV/JSON/TSV 互转 Worker
└── nifi_auto_tagging_worker.py   ← 自动打标 Worker
```

### 5.3 关键函数

| 函数 | 用途 |
|------|------|
| `_get_user_upload_dirs(username, tagged=False)` | 返回用户上传/转换/导出目录，tagged=True 时根于 `tagged_nifi_data/`（Local）或 `tagged_real_nifi_data/`（NiFi） |
| `_parse_bool_query(value)` | 安全解析 Query 布尔值，避免 `bool("false")=True` 陷阱 |
| `_host_to_container_path(host_path)` | 宿主机路径 → 容器内路径（NiFi worker 使用） |
| `_wait_nifi_task_done(job_id, ...)` | 等待 NiFi 任务完成，同时搜索全局和用户 done 目录 |
| `_route_nifi_convert_output_to_user(...)` | 将 NiFi worker 产物从全局目录路由到用户目录（hasTag 时入 tagged_real_nifi_data） |
| `_submit_nifi_export_task(job)` | 提交 DB 导出任务到 NiFi |
| `_submit_nifi_upload_convert_task(...)` | 提交上传转换任务到 NiFi |
| `_build_nifi_export_task(job)` | 构造 NiFi 导出任务 JSON（含 hasTag 和 tagged targetDir） |

### 5.4 数据库模型

```
iot_users              ← 用户表（username, password_hash, is_admin, deployment_mode, ...）
export_jobs            ← 导出任务表（schedule, db_config, payload, status, ...）
jobs                   ← 通用任务表（job_type, progress, payload, ...）
files                  ← 文件注册表（file_id, storage_type, storage_path, ...）
tags                   ← 标签记录表（file_id, row_id, label, operator, ...）
```

### 5.5 API 路由总览

**认证**: `/api/v1/auth/{login, register, me, logout}`

**数据库**: `/api/v1/db/{test-connection, list-tables}`

**导出**: `/api/v1/export`（立即导出）、`/api/v1/export/mysql`、`/api/v1/export-jobs`（CRUD）

**文件**: `/api/v1/files`、`/api/v1/upload/inbox_{csv,json,tsv}`、预览/下载

**标签**: `/api/v1/tags/{manual, manual-table, auto}`

**内部管理**: `/api/v1/internal/{backend-mode, users, pull, factory-tree, ...}`

**xattr**: `/api/v1/files/{file_id}/xattr`（返回 `xattrEnabled`、`xattrKeys`、脱敏后的 meta）

---

## 6. 前端架构

### 6.1 技术栈与文件

纯静态 HTML + JavaScript + CSS，通过 `serve.py`（内置 API 代理）提供。

```
v1-frontend/
├── serve.py            ← 前端服务（内置 /api/v1/ → 8081 代理，必须用此启动）
├── config.js           ← API_BASE 等全局配置
├── app.js              ← 主逻辑（认证、文件中心、导出、打标、上传）
├── internal.js         ← 内部管理页逻辑（xattr 检查、用户管理）
├── index.html          ← 主控制台
├── login.html          ← 登录页
├── register.html       ← 注册页
├── tasks.html          ← 任务中心
├── internal.html       ← 内部管理（管理员专用）
└── styles.css          ← 全局样式
```

### 6.2 页面功能

| 页面 | 功能 | 权限 |
|------|------|------|
| **login.html** | 登录，5 次失败锁定 10 分钟 | 公开 |
| **register.html** | 注册（选公有/私有模式） | 公开 |
| **index.html** | DB 导出、文件中心、标签中心、上传转换 | 登录用户 |
| **tasks.html** | 定时任务管理 | 登录用户 |
| **internal.html** | xattr 检查、用户管理、静默导出、文件浏览 | 管理员 |

---

## 7. 双模式运行（Local / NiFi）

```
┌─────────────────────────────────────────────────────┐
│                  前端切换                            │
│              Local ◄────────────► NiFi               │
└─────────────────────────────────────────────────────┘
         │                              │
         ▼                              ▼
┌─────────────────┐          ┌─────────────────────────┐
│   Local 模式     │          │     NiFi 模式            │
│                 │          │                         │
│ 后端即时处理     │          │ 后端投递任务 JSON 到     │
│ 写入 nifi-data  │          │ real_nifi_data 下        │
│ /home/yhz/{u}/  │          │ NiFi GetFile 消费 →     │
│   nifi-data/    │          │ worker 执行 → 回流到用户  │
│                 │          │ /home/yhz/{u}/           │
│                 │          │   real_nifi_data/        │
└─────────────────┘          └─────────────────────────┘
```

### 7.1 NiFi Flow 详情

| Flow | GetFile Processor | Command Processor | 轮询目录 | Worker 脚本 |
|------|-------------------|-------------------|----------|-------------|
| DB 导出 | iot_db_export_getfile_v1 | iot_db_export_command_v1 | `export_jobs/inbox` | nifi_db_export_worker.py |
| 文件转换 | iot_convert_getfile_v1 | iot_convert_command_v1 | `convert_jobs/inbox` | nifi_upload_convert_worker.py |
| 自动打标 | iot_auto_tagging_getfile_v1 | iot_auto_tagging_command_v1 | `tagging_jobs/inbox` | nifi_auto_tagging_worker.py |

每个 Flow 由 GetFile（轮询全局 inbox）→ ExecuteStreamCommand（调用 worker）→ 后端路由函数（`_route_*_to_user`）回流到用户目录组成。

### 7.2 hasTag=true 时的行为（Local + NiFi 通用）

| 操作 | hasTag=false | hasTag=true |
|------|-------------|-------------|
| 上传源文件 | `nifi-data/inbox_csv/` 或 `real_nifi_data/inbox_csv/` | `tagged_nifi_data/inbox_csv/` 或 `tagged_real_nifi_data/inbox_csv/` |
| 转换产物 | `nifi-data/csv_to_json/` 等 | `tagged_nifi_data/csv_to_json/` 等 |
| DB 导出 | `nifi-data/output_csv/` 等 | `tagged_nifi_data/output_csv/` 等 |
| 打标产物 | — | 按原文件子目录：`tagged_*_data/<原目录>/tag_*.csv` |

---

## 8. Docker 与 NiFi 容器

### 8.1 容器信息

| 项目 | 值 |
|------|-----|
| 容器名 | iot-nifi |
| 镜像 | iot-nifi-python:latest（基于 apache/nifi:2.8.0） |
| 端口 | 宿主机 8080 → 容器 8443 (HTTPS) |
| 挂载 1 | `/home/yhz/real_nifi_data` → `/opt/nifi/nifi-current/data/iot` |
| 挂载 2 | `/home/yhz:/home/yhz:rw`（让容器能访问所有用户目录） |
| API | `https://localhost:8080/nifi-api` |
| FlowFile Repository | WriteAheadFlowFileRepository（持久化，重启后 flow 自动恢复） |

### 8.2 Worker 更新

```bash
# 修改 worker 后必须复制到容器
docker cp v1-backend/scripts/nifi_upload_convert_worker.py iot-nifi:/opt/nifi/nifi-current/data/iot/bin/
docker cp v1-backend/scripts/nifi_db_export_worker.py iot-nifi:/opt/nifi/nifi-current/data/iot/bin/
docker cp v1-backend/scripts/nifi_auto_tagging_worker.py iot-nifi:/opt/nifi/nifi-current/data/iot/bin/
```

---

## 9. 数据流转全景

```
用户操作（导出/上传/打标）
    │
    ▼
前端 ──► 后端 API（识别当前用户 + 模式）
    │
    ├─ Local 模式: 即时读写文件 + xattr → /home/yhz/{username}/nifi-data/（hasTag 时 → tagged_nifi_data/）
    │
    └─ NiFi 模式:
        1) 投递任务 JSON → 用户 real_nifi_data/ 下 + 全局 real_nifi_data/ 下
        2) NiFi GetFile 轮询全局 inbox → 消费任务
        3) ExecuteStreamCommand 调用 worker 脚本
        4) Worker 写入全局 output 目录（hasTag 时入全局 /home/yhz/tagged_real_nifi_data/）
        5) _route_*_to_user() 扫描 done → 路由产物到用户 real_nifi_data/（hasTag 时 → tagged_real_nifi_data/）
        6) 打标产物按原文件子目录放置
```

---

## 10. 安全机制

| 机制 | 说明 |
|------|------|
| 密码存储 | bcrypt 哈希 |
| 会话管理 | JWT Cookie |
| 登录锁定 | 5 次连续失败 → 锁定 10 分钟 |
| 元数据加密 | AES-256-GCM，密钥通过 `META_XATTR_KEY` 环境变量注入 |
| 用户隔离 | 每个用户独立目录，API 层按 owner_id 过滤 |
| storagePath 脱敏 | API 返回 `[protected]`，不对外暴露真实路径 |
| 完整性校验 | SHA-256 checksum（`user.checksum` xattr） |

---

## 11. 关键设计决策

| 决策 | 理由 |
|------|------|
| 用户目录放 `/home/yhz/` 下 | 单机部署，集中管理 |
| xattr 存储元数据 | 零额外文件，加密不可见 |
| xattr + .meta.json 双写 | 主存储 + 备份，xattr 不可用时自动回退 |
| 双模式（Local/NiFi） | Local 即时，NiFi 容器异步，前端一键切换 |
| 纯 HTML 前端 + serve.py 代理 | 零构建依赖，serve.py 内置 /api/v1/ → 8081 转发 |
| tagged_*_data 独立顶层标签目录 | hasTag=true 时所有产物（源文件+转换+导出）入独立顶层目录，与无标签彻底分离 |
| NiFi 容器共享 + 用户目录独立挂载 | 共用容器，用户隔离通过任务目录 + 挂载 `/home/yhz:/home/yhz` 实现 |
| SQLite 主库 | 单机部署足够，无需额外数据库服务 |

---

## 12. Bug 修复一览（已修复）

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | admin 上传产生 0 字节文件 | 客户端断连导致写入不完整 | 原子写入（先 .tmp 再 rename） |
| 2 | NiFi GetFile 匹配不到文件 | File Filter `.*\\\\.json` 多转义 | 改为 `r".*\.json"` |
| 3 | processor 更新 400 | 用缓存旧 revision PUT | PUT 前 GET 刷新 revision |
| 4 | DB 导出找不到 SQLite | `_build_nifi_export_task` 缺 path 字段 | 补充 `path: db_conf.get("path")` |
| 5 | 容器看不到用户目录 | 只挂载全局 real_nifi_data | 加 `/home/yhz:/home/yhz:rw` 挂载 |
| 6 | 打标标签为空 | `tag_rule.get("mapping", {})` 取不到值 | 改为 `tag_rule.get("mapping", tag_rule)` |
| 7 | 普通用户产物不回流 | username 传 Query 默认 "user" | 改为 `username=owner_id` |
| 8 | NiFi 任务超时回退本地 | 超时 30 秒短于 GetFile polling 30 秒 | 增加到 65 秒 |
| 9 | xattr API 泄露 storagePath | 直接返回元数据含路径 | 脱敏 + 移除 meta 中 storagePath |
| 10 | DB 导出产物不移到 tagged | `p.is_dir()` / `manual_` 前缀误判 | 改为分 is_file/is_dir 处理，移除前缀检查 |
| 11 | export_mysql 丢失 hasTag | job dict 未传 hasTag | 补全 hasTag + tagged_output 路径 |
| 12 | 前端 NiFi 模式报"未生成目标文件" | NiFi 异步未完成即查询 | 检测 PENDING 状态显示异步提示 |
| 13 | 远程浏览器登录失败 | `python3 -m http.server` 无 API 代理 | 恢复 `python3 serve.py` 启动 |
| 14 | 老用户 tagged_output 子目录缺失 | 代码补丁前创建的只有空壳 | 批量补全 236 个 tagged_output 12 个子目录 |
| 15 | hasTag 源文件未进 tagged | 只改了转换产物，漏改源文件路径 | 3 个上传接口 Local+NiFi 全改 |
| 16 | `bag` 接口返回路径为 real_nifi_data | 用户级 mode 文件残留 NiFi 状态 | 重置 admin/zzz 的 backend_mode 文件 |
| 17 | 标签文件混在 tagged_output 子目录 | 标签产物与普通产物同根 | 升级为独立顶层目录 tagged_nifi_data / tagged_real_nifi_data |
| 18 | 打标产物不按原文件子目录 | 输出到 tagged 根目录 | 按 source_meta.storagePath 父目录放置 |
| 19 | AI 训练 label_column=auto_tag 失败 | 占位符被当真实标签列 | auto_tag 回退为配置 tagColumn |

---

## 13. AI 大模型模块（solo_ai_iot）

| 项目 | 值 |
|------|-----|
| 前端 | http://127.0.0.1:3002（Vue vite） |
| 后端 | http://127.0.0.1:8002（FastAPI，conda env iot_clone） |
| GPU | NVIDIA RTX A5000 24GB |
| 训练数据 | `{user}/nifi-data/` 或 `real_nifi_data/`（需 .meta.json 含 tagColumn/datesetname/failedTag） |
| 预测数据 | `{user}/tagged_nifi_data/` 或 `tagged_real_nifi_data/` |
| 模型 | xgboost/LightGBM/CatBoost/hgb/TabNet/TabTransformer/Deep-MLP/CNN/LSTM/GRU |
| 部署 | 训练后 leaderboard「一键部署至 IoT」→ deployed_models/admin/ |
| 预测接口 | /api/predict/batch_file（按 dataset_name 匹配已部署模型） |

---

## 测试报告

完整测试报告见 `/home/yhz/iot/test_report.html`，当前状态：**数据处理模块 69/69 全部通过 + AI 大模型模块 10 个模型训练/预测/准确率评估完成**，覆盖 Local + NiFi 双模式、admin + 普通用户、xattr 验证、hasTag 独立顶层目录验证。
