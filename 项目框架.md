# IoT 智慧数据平台 — 项目框架

> 本文档基于项目当前真实代码与部署状态整理，反映系统的实际形态。

---

## 1. 项目定位

面向工业场景的多租户数据标注与管理平台。支持多源数据接入（8 种数据库导出 + 文件上传）、手动/自动标签标注、xattr 加密元数据存储，为下游大模型训练提供标准化数据资产。

---

## 2. 顶层目录结构

```
/home/yhz/
│
│  ┌──────────────────────────────────────────────────────┐
│  │  公有化用户（deployment_mode=public）                  │
│  ├──────────────────────────────────────────────────────┤
│  │
├── admin/                              ← 管理员用户（公有化）
│   ├── nifi-data/                      ← Local 模式数据存储
│   │   ├── export_jobs/{inbox,done,error}/
│   │   ├── output_csv/ output_json/ output_tsv/
│   │   ├── tagged_output/              ← 打标后文件（hasTag=true）
│   │   ├── inbox_csv/ inbox_json/ inbox_tsv/
│   │   ├── csv_to_json/ csv_to_tsv/ json_to_csv/
│   │   ├── json_to_tsv/ tsv_to_csv/ tsv_to_json/
│   │   └── meta_backups/               ← 元数据版本备份
│   └── real_nifi_data/                 ← NiFi 模式数据存储
│       ├── export_jobs/{inbox,done,error}/
│       └── bin/nifi_db_export_worker.py
│
├── zhangsan/                           ← 公有化用户（示例）
│   ├── nifi-data/                      ← 结构同 admin
│   └── real_nifi_data/
│
├── lisi/                               ← 公有化用户
│   ├── nifi-data/
│   └── real_nifi_data/
│
│  ┌──────────────────────────────────────────────────────┐
│  │  私有化用户（deployment_mode=private）                 │
│  │  当前阶段：数据也暂存 /home/yhz/ 下，与公有化用户目录结构相同│
│  │  ceph_endpoint 记录在 DB，后续阶段迁移到工厂 Ceph        │
│  ├──────────────────────────────────────────────────────┤
│  │
├── 777/                                ← 私有化用户（示例）
│   ├── nifi-data/
│   └── real_nifi_data/
│
├── wangwu/                             ← 私有化用户
│   ├── nifi-data/
│   └── real_nifi_data/
│
├── zhaoliu/                            ← 私有化用户
│   ├── nifi-data/
│   └── real_nifi_data/
│
│  ┌──────────────────────────────────────────────────────┐
│  │  用户监控目录                                         │
│  ├──────────────────────────────────────────────────────┤
│  │
├── in_data/                            ← 所有用户的监控目录
│   └── （用于监控和审计所有用户的数据）
│
│  ┌──────────────────────────────────────────────────────┐
│  │  项目代码 + 共享资源                                   │
│  ├──────────────────────────────────────────────────────┤
│  │
├── iot/                                ← 项目代码仓库（详见 §2.1）
│   ├── README.md
│   ├── 项目框架.md                      ← 本文档
│   ├── 智慧数据平台设计文档.md
│   ├── 需求-最终版.md
│   ├── AI模块详细实施方案.md
│   ├── AI模块综合执行方案（融合NiFi、联邦学习与双模部署）.md
│   ├── ai.png
│   ├── 整体融合架构设计.pdf
│   ├── v1-backend/                     ← FastAPI 后端（详见 §4）
│   ├── v1-frontend/                    ← 前端静态页面（详见 §5）
│   ├── v2-nifi/                        ← NiFi 方案与脚本
│   ├── V1执行清单/ V2执行清单/ V3执行清单/ V4执行清单/
│   ├── docker/                         ← Docker 编排
│   ├── scripts/                        ← 部署辅助脚本
│   ├── test-data/                      ← 测试数据
│   ├── nifi_data/                      ← 旧 NiFi 配置（兼容保留）
│   ├── contract-tests/                 ← 合约测试
│   ├── real_nifi_conf/                 ← 生产 NiFi 配置
│   └── 每日工作进度/                    ← 工作日志
│
├── real_nifi_data/                     ← 全局 NiFi 共享工作区（旧路径，兼容保留）
└── nifi-data/                          ← 全局 Local 共享数据（旧路径，兼容保留）
```

### 2.1 项目代码目录结构

```
iot/
├── README.md                           ← 项目说明与运维指南
├── 项目框架.md                          ← 本文档
├── 智慧数据平台设计文档.md               ← 平台设计文档
├── 需求-最终版.md                       ← 需求文档
├── AI模块详细实施方案.md                 ← 总体实施方案
├── AI模块综合执行方案（融合NiFi、联邦学习与双模部署）.md
├── 整体融合架构设计.pdf                  ← 架构设计图
├── ai.png                              ← 项目图示
├── AI模块实施状态追踪表_简化.csv/xlsx     ← 进度追踪表
│
├── v1-backend/                         ← FastAPI 后端
│   ├── app/                            ← 应用代码
│   │   ├── main.py                     ← 主入口，70+ API 路由
│   │   ├── auth.py                     ← 认证路由（静默导出、用户存储）
│   │   ├── admin_routes.py             ← 管理员路由（用户管理、静默导出）
│   │   ├── db_connect.py               ← 数据库连接测试、表列举
│   │   ├── db_connect.py.backup        ← 旧版备份
│   │   ├── db_models.py                ← SQLAlchemy 数据模型
│   │   ├── engine_factory.py           ← 数据库引擎工厂
│   │   ├── meta_json.py                ← 元数据读写（含平台自动切换 xattr）
│   │   ├── meta_xattr.py               ← xattr + AES-256-GCM 实现
│   │   ├── nifi_orchestrator.py        ← NiFi 容器管理与 API 对接
│   │   ├── nifi_client.py              ← NiFi API 客户端封装
│   │   ├── executors.py                ← MockExecutor / NiFiExecutor
│   │   ├── export_worker.py            ← 导出任务执行器
│   │   ├── silent_export_worker.py     ← 静默导出执行器
│   │   ├── scheduler.py                ← 定时任务调度（APScheduler）
│   │   ├── local_ip.py                 ← 本地 IP 获取
│   │   └── nifi_flow_mapping.template.json
│   ├── scripts/                        ← NiFi Worker 脚本
│   │   ├── nifi_db_export_worker.py    ← 多数据源导出 Worker（8种数据库）
│   │   ├── nifi_mysql_export_worker.py ← MySQL 专用导出 Worker
│   │   ├── nifi_auto_tagging_worker.py ← 自动打标 Worker
│   │   ├── nifi_upload_convert_worker.py ← 上传转换 Worker
│   │   ├── nifi_batch_smoke.py         ← NiFi 批量冒烟测试
│   │   └── atomic_write_example.py     ← 原子写入示例
│   ├── data/
│   │   ├── app.db                      ← SQLite 数据库（2.5GB）
│   │   └── generated/                  ← 运行时文件
│   │       ├── backend_mode.json       ← 当前后端模式状态
│   │       ├── factory_reports.ndjson  ← 工厂报告日志
│   │       ├── silent_export_config.json
│   │       ├── silent_export_manifest.json
│   │       └── silent_export_requests.ndjson
│   ├── deploy/                         ← systemd 服务文件
│   │   ├── iot-backend.service
│   │   ├── iot-backend-health.service
│   │   ├── iot-backend-health.sh
│   │   ├── iot-backend-health.timer
│   │   └── iot-backend.service.override.conf
│   ├── requirements.txt                ← Python 依赖
│   └── .venv/                          ← 虚拟环境
│
├── v1-frontend/                        ← 前端静态页面
│   ├── index.html                      ← 主控制台
│   ├── login.html                      ← 登录页
│   ├── register.html                   ← 注册页
│   ├── tasks.html                      ← 任务中心
│   ├── internal.html                   ← 内部管理（管理员）
│   ├── internal.js                     ← 内部管理 JS
│   ├── config.js                       ← 全局配置
│   ├── app.js                          ← 主逻辑
│   ├── styles.css                      ← 全局样式
│   ├── serve.py                        ← 本地静态服务（调试用）
│   ├── README.md
│   └── prototypes/                     ← 原型页面
│       ├── timed_task_panel.html
│       ├── admin_silent_control.html
│       └── api_export_jobs.md
│
├── v2-nifi/                            ← NiFi 方案材料
│   ├── V1-NiFi统一实施总方案.md
│   ├── V1-NiFi统一实施可执行方案.md
│   ├── create_export_flow.py           ← NiFi 导出流程创建脚本
│   ├── 01-cleanup-old-flow.md          ← 清理旧流程
│   ├── 02-db-export-flow.md            ← 数据库导出流程
│   ├── 03-upload-convert-flow.md       ← 上传转换流程
│   ├── 04-auto-tagging-flow.md         ← 自动打标流程
│   ├── 05-silent-export-flow.md        ← 静默导出流程
│   └── 06-frontend-task-center.md      ← 前端任务中心
│
├── V1执行清单/                         ← V1 阶段文档
│   ├── 后端V1接口字段级清单.md
│   ├── 前端V1页面组件级任务拆解.md
│   ├── 登录与权限实施方案.md
│   ├── 多数据库导出执行方案.md
│   ├── 本地后端功能补充方案.md
│   ├── 导出命名与归档实施方案.md
│   ├── 需求规格-NiFi-文件输出.md
│   ├── 前后端联调检查清单.md
│   ├── 前后端联调Mock策略.md
│   ├── 方案完成度对照-2026-03-31.md
│   ├── 封板证据归档-2026-03-31.md
│   └── Day1~Day3 详细任务单与验收记录
│
├── V2执行清单/                         ← NiFi 阶段文档
│   ├── README.md
│   ├── 静默兜底导出实施方案.md
│   ├── AI模块训练与联邦学习框架.md
│   ├── 联邦学习技术路线调查报告-2026-04-15.md
│   ├── HDFS-HBase-Hive浏览导出方案.md
│   └── V2-Hadoop最小生态本机Docker真实可测方案-离线镜像导入版.md
│
├── V3执行清单/                         ← 标签与元数据阶段文档
│   ├── 说明.md                         ← xattr 原理说明
│   ├── 标签配置与文件元数据执行方案.md   ← V3 完整方案
│   ├── 标签配置与文件元数据执行方案_文件配置版.md
│   ├── 标签配置与文件元数据功能优先级清单.md
│   ├── 零文件元数据方案.md
│   ├── 网站前端功能使用手册.md
│   ├── meta.schema.json                ← 元数据 JSON Schema
│   ├── sample.csv.meta.json            ← 示例元数据
│   └── cleanup_meta.py                 ← 清理脚本
│
├── V4执行清单/                         ← 公有化私有化部署文档
│   ├── V4-公有化私有化部署总方案.md
│   ├── 公有化与私有化部署执行方案.md
│   ├── 01-数据库模型与用户注册.md
│   ├── 02-核心路径解析函数.md
│   ├── 03-后端API隔离改造.md
│   ├── 04-导出Worker与静默导出改造.md
│   ├── 05-前端注册页与用户管理.md
│   ├── 06-前端文件中心与用户隔离.md
│   └── 07-NiFi容器策略.md
│
├── docker/                             ← Docker 编排
│   ├── nifi/                           ← NiFi 容器
│   │   ├── docker-compose.yml
│   │   ├── Dockerfile
│   │   ├── .env.template
│   │   ├── build.sh / push_image.sh
│   │   ├── install_iot_nifi.sh
│   │   ├── iot-nifi.service
│   │   ├── README.md
│   │   └── nginx/                      ← Nginx 反向代理配置
│   └── hadoop/                         ← Hadoop 最小生态
│       ├── docker-compose.yml
│       └── images/                     ← 离线镜像
│
├── scripts/                            ← 部署辅助脚本
│   ├── install_autostart_services.sh   ← 安装 systemd 服务
│   ├── install_vscode_extensions.sh    ← 安装 VS Code 扩展
│   ├── apply_nginx_upload_limits.sh    ← 放开 nginx 上传限制
│   ├── setup_local_simulation.sh       ← 本地模拟环境搭建
│   ├── start_db_services.sh            ← 启动数据库服务
│   ├── ssh_tunnel.sh                   ← SSH 隧道脚本
│   └── csv_to_xlsx.py                  ← CSV 转 XLSX 工具
│
├── test-data/                          ← 测试数据
│   ├── with_header.tsv
│   └── no_header.tsv
│
├── api-worker-x64/                     ← API Worker 可执行文件
├── contract-tests/                     ← 合约测试
├── real_nifi_conf/                     ← 生产 NiFi 配置
├── nifi_data/                          ← 旧 NiFi 数据（兼容保留）
├── .github/                            ← GitHub CI/CD
├── backup_sqlite/                      ← SQLite 备份
├── .venv/                              ← 项目级虚拟环境
│
└── 每日工作进度/                        ← 工作日志
    ├── README.md
    ├── 模板/日报模板.md
    └── 2026-03-30 ~ 2026-06-10.md      ← 每日日志
```

---

## 3. 存储架构

### 3.1 用户数据隔离

> **当前阶段**：所有用户（公有化 + 私有化）都存放在 `/home/yhz/{username}/` 下。
> 私有化用户的 Ceph 端点信息记录在数据库中，但实际数据暂不迁移到 Ceph，待后续阶段实现。

```
用户注册
    │
    ├─ 公有化用户（默认）
    │   存储根: /home/yhz/{username}/
    │   自动创建: nifi-data/, real_nifi_data/
    │
    └─ 私有化用户（当前阶段同公有化）
        存储根: /home/yhz/{username}/
        数据库记录: deployment_mode=private, ceph_endpoint=...
        未来迁移: 数据迁移到 {ceph_endpoint}/
```

**路由逻辑**（`_resolve_user_storage_root`）：
```python
def _resolve_user_storage_root(username: str) -> Path:
    from .db_models import IotUser, get_engine
    from sqlalchemy.orm import sessionmaker
    # 查询数据库判断部署模式
    db_path = Path(__file__).resolve().parent.parent / "data" / "app.db"
    engine = get_engine(db_path)
    SessionLocal = sessionmaker(bind=engine)
    session = SessionLocal()
    try:
        user = session.query(IotUser).filter(IotUser.username == username).first()
        if user and user.deployment_mode == "private" and user.ceph_endpoint:
            root = Path(user.ceph_endpoint)
        else:
            root = Path(os.getenv("IN_DATA_BASE_DIR", "/home/yhz")) / _normalize_username(username)
    finally:
        session.close()
    try:
        (root / "nifi-data").mkdir(parents=True, exist_ok=True)
        (root / "real_nifi_data").mkdir(parents=True, exist_ok=True)
    except (OSError, PermissionError):
        import tempfile
        fallback = Path(tempfile.gettempdir()) / "iot_user_data" / _normalize_username(username)
        fallback.mkdir(parents=True, exist_ok=True)
        return fallback
    return root
```

### 3.2 元数据存储（V3 方案）

```
数据文件 sensor_data.csv
│
├─ 文件内容（cat 可见）: CSV/JSON/TSV 数据
│
└─ inode 扩展属性 xattr（cat/ls 不可见）
   ├─ user.meta     = AES-256-GCM 加密密文（base64）
   └─ user.checksum = SHA-256 校验码（明文，防磁盘静默损坏）
```

- **Linux 生产环境**: xattr 加密存储，零额外文件
- **macOS/Windows 开发环境**: 自动降级为 `.meta.json` 文件
- **密钥管理**: 32 字节 AES 密钥，通过环境变量 `META_XATTR_KEY` 注入，不落盘

### 3.3 元数据字段

| 字段 | 类型 | 必须 | 来源 | 说明 |
|------|------|:--:|:--:|------|
| fileId | string | 是 | 自动 | 唯一标识 |
| fileName | string | 是 | 自动 | 文件名 |
| sourceType | string | 是 | 自动 | db_export / upload / manual_tag / auto_tag |
| storageType | string | 是 | 自动 | plain / tagged |
| storagePath | string | 是 | 自动 | 绝对路径（仅后端内部使用，不对外暴露） |
| hasTag | boolean | 是 | 用户 | 是否含标签 |
| tagColumn | string | 条件 | 用户 | hasTag=true 时必填 |
| tagName | string | 条件 | 用户 | hasTag=true 时必填 |
| tagRange | array | 条件 | 用户 | hasTag=false 时必填 |
| failedTag | string | 条件 | 用户 | hasTag=false 时必填 |
| tagStrategy | string | 否 | 自动 | column_tag / manual_range / auto_rule |
| tagRule | object | 否 | 用户 | 自动打标规则 |
| trainable | boolean | 否 | 自动 | 默认 true |
| categoryId | string | 否 | 自动+用户 | 分类 ID |
| categoryName | string | 否 | 自动+用户 | 分类名称 |
| description | string | 否 | 用户 | 文件描述 |
| columns | array | 否 | 自动 | 列名列表 |
| rowCount | number | 否 | 自动 | 行数 |
| fileSize | number | 否 | 自动 | 文件大小（字节） |
| createdAt | string | 是 | 自动 | ISO 8601 |
| updatedAt | string | 是 | 自动 | ISO 8601 |

---

## 4. 后端架构

### 4.1 技术栈

| 组件 | 技术 |
|------|------|
| 框架 | FastAPI 0.116.1 |
| 服务器 | Uvicorn 0.35.0 |
| 数据库 | SQLite（主库）+ 支持连接 MySQL/PG/SQLServer/Oracle/Hive/HDFS/HBase |
| 认证 | JWT Cookie（bcrypt 哈希） |
| 元数据 | xattr + AES-256-GCM（Linux）/ .meta.json（降级） |
| 加密 | cryptography 库 |

### 4.2 代码结构

```
v1-backend/
├── app/
│   ├── main.py              ← 主入口，70+ API 路由
│   ├── auth.py              ← 认证路由（静默导出、用户存储）
│   ├── admin_routes.py      ← 管理员路由（用户管理、静默导出）
│   ├── db_connect.py        ← 数据库连接测试、表列举
│   ├── db_models.py         ← SQLAlchemy 数据模型
│   ├── meta_json.py         ← 元数据读写（含平台自动切换 xattr）
│   ├── meta_xattr.py        ← xattr + AES-256-GCM 实现
│   ├── nifi_orchestrator.py ← NiFi 容器管理与 API 对接
│   ├── executors.py         ← MockExecutor / NiFiExecutor
│   ├── export_worker.py     ← 导出任务执行器
│   ├── silent_export_worker.py ← 静默导出执行器
│   ├── scheduler.py         ← 定时任务调度
│   └── nifi_flow_mapping.template.json
├── scripts/
│   ├── nifi_db_export_worker.py      ← 多数据源导出 Worker
│   ├── nifi_auto_tagging_worker.py   ← 自动打标 Worker
│   └── ...
├── data/generated/           ← 运行时文件
├── deploy/
│   ├── iot-backend.service
│   └── iot-backend-health.service
├── requirements.txt
└── .venv/
```

### 4.3 数据库模型

```
iot_users          ← 用户表
├── id, username, password_hash, is_admin
├── deployment_mode (public/private)
├── ceph_endpoint
├── failed_attempts, locked_until
└── created_at

export_jobs        ← 导出任务表
├── id, job_name, username, owner_id
├── schedule (cron), file_format, destination
├── mode (visible/silent), enabled
├── db_config (JSON), payload (JSON)
└── status, last_run, created_at

jobs               ← 通用任务表
├── id, job_id, job_type, status
├── progress, payload (JSON)
└── created_at

files              ← 文件注册表
├── id, file_id, file_name, file_format
├── file_size, storage_type, storage_path
└── created_at

tags               ← 标签记录表
├── id, file_id, row_id, label
├── operator
└── created_at
```

### 4.4 API 路由总览

#### 认证与会话

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/auth/login | 登录 |
| POST | /api/v1/auth/register | 注册 |
| GET | /api/v1/auth/me | 当前用户信息 |
| POST | /api/v1/auth/logout | 登出 |
| POST | /api/v1/auth/refresh | 刷新会话 |

#### 数据库连接

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/db/test-connection | 测试数据库连接 |
| POST | /api/v1/db/list-tables | 列出数据库表 |

#### 导出任务

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/export-jobs | 创建导出任务 |
| GET | /api/v1/export-jobs | 列举导出任务 |
| GET | /api/v1/export-jobs/{job_id} | 查询单个任务 |
| PATCH | /api/v1/export-jobs/{job_id} | 更新任务 |
| DELETE | /api/v1/export-jobs/{job_id} | 删除任务 |
| POST | /api/v1/export-jobs/trigger | 手动触发 |
| POST | /api/v1/export | 立即导出 |
| POST | /api/v1/export/mysql | MySQL 专用导出 |

#### 文件管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/files | 文件列表 |
| GET | /api/v1/user/files/tree | 目录树 |
| GET | /api/v1/files/{file_id}/preview | 文件预览 |
| GET | /api/v1/files/{file_id}/download | 文件下载 |
| POST | /api/v1/files/upload | 文件上传（通用） |
| POST | /api/v1/files/purge-missing | 清理缺失文件 |
| POST | /api/v1/upload/inbox_csv | 上传 CSV |
| POST | /api/v1/upload/inbox_json | 上传 JSON |
| POST | /api/v1/upload/inbox_tsv | 上传 TSV |

#### 标签

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/tags/manual | 手动打标 |
| POST | /api/v1/tags/manual-table | 手动打标（表格） |
| POST | /api/v1/tags/auto | 自动打标 |
| GET | /api/v1/tags/rules | 打标规则列表 |

#### 定时任务

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/schedules | 创建定时任务 |
| GET | /api/v1/schedules | 列举定时任务 |
| PATCH | /api/v1/schedules/{id} | 更新定时任务 |
| DELETE | /api/v1/schedules/{id} | 删除定时任务 |

#### 训练文件 API

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/training/files | 查询可训练文件列表 |
| POST | /api/v1/training/submit | 提交训练任务 |
| GET | /api/v1/training/tasks/{task_id} | 查询训练进度 |

#### 内部管理（管理员）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/internal/backend-mode | 查询后端模式 |
| POST | /api/v1/internal/backend-mode | 切换后端模式 |
| GET | /api/v1/internal/users | 用户列表 |
| PUT | /api/v1/internal/users/{username}/deployment | 修改部署模式 |
| DELETE | /api/v1/internal/users/{username} | 删除用户 |
| GET | /api/v1/internal/all-users | 所有用户 |
| POST | /api/v1/internal/private-users/{username}/pull | 拉取私有化用户数据 |
| GET | /api/v1/internal/factory-reports | 工厂报告 |
| GET | /api/v1/internal/factory-jobs | 工厂任务 |
| GET | /api/v1/internal/factory-assets | 工厂资产 |
| GET | /api/v1/internal/factory-tree | 工厂目录树 |
| POST | /api/v1/internal/factory-tree/fetch | 拉取目录树数据 |
| POST | /api/v1/internal/factory-tree/refresh | 刷新目录树 |
| POST | /api/v1/admin/migrate-meta | 迁移元数据到 xattr |
| GET | /api/v1/admin/check-integrity | 完整性校验 |

#### NiFi 管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/internal/nifi/status | NiFi 状态 |
| POST | /api/v1/internal/nifi/ensure | 确保 NiFi 就绪 |
| POST | /api/v1/internal/nifi-export-jobs/sync | 同步 NiFi 导出任务 |
| GET | /api/v1/internal/nifi-export-jobs | NiFi 导出任务列表 |
| POST | /api/v1/nifi-callback | NiFi 回调 |

#### 系统

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /health | 健康检查 |
| GET | /api/v1/health/databases | 数据库健康 |
| GET | /api/v1/system/executor | 执行器状态 |
| GET | /api/v1/jobs | 任务列表 |
| GET | /api/v1/jobs/{job_id} | 任务详情 |
| POST | /api/v1/jobs | 创建任务 |
| POST | /api/v1/jobs/{job_id}/cancel | 取消任务 |
| GET | /api/v1/jobs/{job_id}/outputs | 任务输出 |

#### 静默导出（admin_routes）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /internal/tenants/{tenant}/silent-export | 静默导出配置 |
| POST | /internal/tenants/{tenant}/silent-export | 设置静默导出 |
| GET | /internal/tenants/{tenant}/silent-export/manifest | 导出清单 |
| POST | /internal/tenants/{tenant}/silent-export/trigger | 触发导出 |
| POST | /internal/admin/ensure-user-storage | 确保用户存储 |

---

## 5. 前端架构

### 5.1 技术栈

纯静态 HTML + JavaScript + CSS，无框架依赖。

### 5.2 页面结构

```
v1-frontend/
├── index.html          ← 主控制台（导出、文件中心、标签中心、上传转换）
├── login.html          ← 登录页
├── register.html       ← 注册页
├── tasks.html          ← 任务中心
├── internal.html       ← 内部管理（管理员专用）
├── config.js           ← 全局配置
├── app.js              ← 主逻辑
├── serve.py            ← 本地静态服务（调试用）
└── prototypes/         ← 原型页面
    ├── timed_task_panel.html
    └── admin_silent_control.html
```

### 5.3 页面功能

| 页面 | 功能 | 用户隔离 |
|------|------|----------|
| **login.html** | 用户名/密码登录，5 次失败锁定 10 分钟 | — |
| **register.html** | 注册（选公有/私有模式，私有需填 Ceph 地址） | — |
| **index.html** | 数据库导出、文件中心、标签中心、上传转换 | 只显示当前用户数据 |
| **tasks.html** | 任务列表、筛选、分页 | 只显示当前用户任务 |
| **internal.html** | 切换用户、静默导出、文件浏览、用户管理 | 管理员专用 |

### 5.4 前端配置（config.js）

```javascript
{
  API_BASE: "api/v1",
  DEFAULT_BACKEND_MODE: "local",    // local | nifi
  FEATURE_FLAGS: {
    localTaskFlow: true,
    localFilePreview: true,
    localTagging: true
  }
}
```

---

## 6. 双模式运行

```
┌─────────────────────────────────────────────────────┐
│                  前端右上角切换                        │
│              Local ◄────────────► NiFi               │
└─────────────────────────────────────────────────────┘
         │                              │
         ▼                              ▼
┌─────────────────┐          ┌─────────────────────┐
│   Local 模式     │          │     NiFi 模式        │
│                 │          │                     │
│ 后端直接写文件    │          │ 后端提交任务到 NiFi   │
│ 到用户目录       │          │ 容器执行后路由回用户   │
│                 │          │                 │
│ /home/yhz/{username}/ │      │ /home/yhz/{username}/ │
│   nifi-data/    │          │   real_nifi_data/    │
└─────────────────┘          └─────────────────────┘
```

---

## 7. 部署架构

### 7.1 systemd 服务

| 服务 | 说明 | 端口 |
|------|------|------|
| iot-backend.service | FastAPI 后端 | 8081 |
| iot-frontend.service | 前端静态服务 | 5174 |
| iot-backend-health.timer | 健康检查定时器 | — |
| iot-nifi.service | NiFi Docker 容器 | 8080 |

### 7.2 常用运维命令

```bash
# 重启服务
sudo systemctl restart iot-backend.service
sudo systemctl restart iot-frontend.service

# 查看状态
systemctl status iot-backend.service --no-pager --full

# 查看日志
journalctl -u iot-backend.service -f

# 健康检查
curl http://127.0.0.1:8081/health
```

### 7.3 访问地址

| 场景 | 地址 |
|------|------|
| 本机前端 | http://127.0.0.1:5174 |
| 局域网前端 | http://202.113.76.55:5174 |
| 后端 | http://127.0.0.1:8081 |
| NiFi UI | https://localhost:8080 |

---

## 8. 数据流转全景

```
用户操作（导出/上传/打标）
    │
    ▼
前端页面 ──► 后端 API
    │
    ├─ 识别当前用户（JWT Cookie）
    │   ├─ Local 模式
│   │   └─ 直接写文件 + xattr 元数据
│   │       → /home/yhz/{username}/nifi-data/
│   │
│   └─ NiFi 模式
│       └─ 提交任务 JSON
│           → /home/yhz/{username}/real_nifi_data/export_jobs/inbox/
            │
            ▼
        NiFi 容器执行（共享）
            │
            ▼
        _route_nifi_output_to_user(username)
        扫描 done/error → 路由回用户目录
    │
    ▼
后端启动时 _sync_nifi_files()
扫描所有用户目录 → 解密 xattr → 构建内存索引
    │
    ▼
API 查询 → 查内存索引 → 返回（不含 storagePath）
```

---

## 9. 安全机制

| 机制 | 说明 |
|------|------|
| 密码存储 | bcrypt 哈希 |
| 会话管理 | JWT Cookie，过期前 5 分钟提示续期 |
| 登录锁定 | 5 次连续失败 → 锁定 10 分钟 |
| 元数据加密 | AES-256-GCM，密钥不落盘 |
| 用户隔离 | 每个用户独立目录，API 层过滤 |
| storagePath 保护 | 永远不出现在 API 响应中 |
| 管理员保护 | 管理员账号不可删除 |
| 完整性校验 | SHA-256 checksum 防磁盘静默损坏 |

---

## 10. 功能完成状态

| 优先级 | 状态 | 内容 |
|--------|------|------|
| **P0** | ✅ 完成 | 元数据模块、导出/上传/预览/打标接口、前端表单、目录规则 |
| **P1** | ✅ 完成 | 训练文件列表 API、自动打标 UI、failedTag 字段 |
| **P2** | ✅ 完成 | JWT 认证、审计日志、版本备份、迁移脚本、完整性校验、训练 API |
| **P2** | ⏳ 待做 | 端到端回归测试 |

---

## 11. 关键设计决策

| 决策 | 理由 |
|------|------|
| 用户目录放 /home/yhz/ 下 | 单机部署，用户数据集中管理，避免与系统目录混淆，便于管理员直接查看 |
| xattr 存储元数据 | 零额外文件，加密不可见，文件操作自动跟随 |
| 双模式（Local/NiFi） | Local 用于演示开发，NiFi 用于生产，前端一键切换 |
| 纯 HTML 前端 | 零构建依赖，部署简单，适合内网环境 |
| SQLite 主库 | 单机部署足够，无需额外数据库服务 |
| NiFi 容器共享 | 所有用户共用 NiFi 容器，通过任务目录隔离 |