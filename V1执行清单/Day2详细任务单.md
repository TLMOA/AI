# Day2详细任务单（V1推进日）

## 1. 当日目标

在Day1基础上完成“文件中心可用 + 标签中心可用 + 真实NiFi适配准备完成”的第二阶段闭环。

## 2. 时间分块执行

## 09:30 - 10:30 后端能力补齐（文件中心）

1. 完善 `GET /api/v1/files/{fileId}/preview` 的分页和异常处理。
2. 增加文件格式兼容说明（CSV/JSON/TSV）。
3. 补充产物元数据字段校验。

完成判定：

- 预览接口支持offset/limit并返回total。
- 非法fileId返回统一错误码。

## 10:30 - 12:00 后端能力补齐（标签中心）

1. 完善 `POST /api/v1/tags/manual` 参数校验。
2. 完善 `POST /api/v1/tags/auto` 与任务系统联动。
3. 完善 `GET /api/v1/tags/rules` 规则版本字段。

完成判定：

- 手动标签返回更新条数。
- 自动标签可触发任务并回收任务ID。

## 13:30 - 15:00 前端页面扩展（文件中心）

1. 新增文件列表区域。
2. 新增文件预览弹窗（分页）。
3. 新增下载按钮与错误提示。

完成判定：

- 任务完成后前端可直接预览并下载产物。

## 15:00 - 16:30 前端页面扩展（标签中心）

1. 新增手动标签表单。
2. 新增自动标签表单（规则选择）。
3. 标签任务触发后跳转任务详情。

完成判定：

- 自动标签可完整触发并在任务页观察状态。

## 16:30 - 17:30 真实NiFi适配准备（不破坏现有可演示链路）

1. 新增执行器抽象：`MockExecutor` 与 `NiFiExecutor`。
2. 保留环境开关：`APP_EXECUTOR_MODE=mock|nifi`。
3. 整理5条流程ID映射配置模板（先占位）。

完成判定：

- 代码结构已支持无痛切换mock/nifi。
- 不影响现有前端演示。

## 17:30 - 18:00 收尾

1. 更新日报（22:00前）。
2. 记录问题和风险。
3. 准备Day3任务（调度中心+真实NiFi接入验证）。

## 3. 当日交付物

1. 文件中心可用（预览+下载）。
2. 标签中心可用（手动+自动）。
3. 执行器解耦骨架（mock/nifi开关）。
4. Day2联调记录与问题清单。

## 4. 执行状态（2026-03-31）

1. ✅ 文件中心已落地（后端 `GET /api/v1/files` + 前端列表/预览/下载）。
2. ✅ 标签中心已落地（手动标签、自动标签、规则加载）。
3. ✅ 执行器解耦已落地（`MockExecutor` + `NiFiExecutor` + 环境开关）。
4. ✅ 冒烟验证通过（任务流转、产物预览、标签接口、文件列表）。
5. ⚠️ NiFiExecutor 当前为占位适配，真实NiFi REST触发安排在 Day3。
