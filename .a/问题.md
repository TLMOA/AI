1.静默兜底导出，覆写还是追加模式没做好
2.标签存放的时候，根据列名，多少列，标签取值范围，区分是否是相同的数据集
3.重要提醒 : NiFi 容器内访问宿主机 MySQL 需使用宿主机 IP，不能用 127.0.0.1 。

4.先完善公有化私有化方案
配置信息那次开会提到的其他改动。


5.做设计文档和ppt，方便汇报，主要要是brokenble数据集全流程实现

****周日之前*****

5.1  定时导出这块对接。数据到模型那块自动训练。
5.2 配置文件加一个描述数据类型的字段


6.nifi后端、扩展属性

7.部署文档

8.按照网站形式。（这个公司做）




那你能否真实地上传这个糖尿病集的有标签和无标签的文件，然后利用这个文件进行训练和预测。就用admin用户的local端进行数据上传。然后训练就选取一个简单模型就行

自动打标和手动打标后的文件放在标签目录下的哪里，应该看原文件是放在哪个最小目录下。比如原文件在inbox_csv下，那打标后的文件应该在标签目录的inbox_csv下。
