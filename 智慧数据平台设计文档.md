文档名称：智慧数据平台技术设计说明书
版本： 1.0
日期： 2026年7月3日

---

# 1. 引言

本项目是一个面向工业物联网（IIoT）场景的智慧数据平台，旨在解决工厂侧多源异构数据库的数据导出、格式转换、文件管理、标签标注、任务调度以及多租户数据隔离等问题。平台以 FastAPI 为后端核心，前端采用轻量静态页面，通过 systemd 常驻服务运行，支持本地执行器（Local Executor）与 Apache NiFi 执行器双模切换，并兼容公有化与私有化两种部署模式。

## 1.1 文档目的

本文档旨在详细阐述智慧数据平台的技术架构、核心机制与设计细节。本平台致力于解决工业物联网场景下，工厂多源异构数据库（MySQL、PostgreSQL、SQLite、Hive、HDFS、HBase）的统一数据采集、转换、导出与文件管理问题，并为后续 AI 模型训练提供标准化数据供给。

本文档的目标读者包括：
- **系统架构师**：用于理解系统的整体技术选型与模块交互。
- **核心开发工程师**：用于指导具体功能模块的编码实现与接口对接。
- **测试与运维人员**：用于制定测试方案与部署策略。

## 1.2 背景与动机

随着工业4.0与物联网的纵深推进，工厂现场部署了多种异构数据存储系统。传统的数据采集方式面临以下核心痛点：

1. **多源异构数据库适配难**：工厂侧同时存在 MySQL、PostgreSQL、SQLite、Hive、HDFS、HBase 等多种数据源，缺乏统一的数据导出与转换工具链。
2. **数据格式碎片化**：CSV、JSON、TSV 等多种格式之间需要频繁转换，手工操作效率低下且易出错。
3. **多租户数据隔离缺失**：多个工厂（用户）的数据需要严格隔离，传统方案缺乏统一的用户认证与数据权限管控。
4. **部署模式多样化**：公有化部署（数据存公司Ceph）与私有化部署（数据存工厂Ceph）需要一套统一架构双模支撑。
5. **定时任务与静默导出**：需要支持自动化的定时数据导出，且对增量数据具备感知能力。
6. **数据流编排需求**：部分场景需要 Apache NiFi 进行复杂的数据流编排处理，平台需提供无缝对接能力。

为解决上述痛点，本项目提出了六大核心机制，旨在构建一个通用、鲁棒、高效的工业数据管理平台。

## 1.3 术语表

| 术语 | 解释 |
|------|------|
| FastAPI | 基于 Python 的现代高性能 Web 框架，支持异步处理和自动生成 OpenAPI 文档。 |
| NiFi | Apache NiFi，一个易于使用、功能强大的数据流编排与自动化平台。 |
| Local Executor | 本地执行器，在本地 Python 进程中直接完成数据导出与转换，不依赖 NiFi。 |
| NiFi Executor | NiFi 执行器，将数据导出任务提交给 NiFi 集群执行，支持复杂的数据流编排。 |
| 静默导出 | 后台自动定时执行的增量数据导出，无需用户手动触发，对前端用户透明。 |
| JWT | JSON Web Token，用于用户认证与授权。 |
| Ceph | 分布式存储系统，用于存储工厂侧或公司侧的数据文件。 |
| 公有化部署 | 数据存储在公司的 Ceph 集群中，由公司统一管理。 |
| 私有化部署 | 数据存储在工厂自己的 Ceph 集群中，管理员可拉取到公司侧。 |
| Manifest | 静默导出清单文件，记录所有需要自动导出的数据表信息。 |
| 标签（Tag） | 对数据文件中的行进行人工或自动标注，用于后续 AI 模型训练。 |
| .meta.json | 文件元数据配置文件，记录文件的标签规则、列信息等元数据。 |

---

# 2. 需求分析

本章节将详细分析系统必须解决的核心挑战，并将其转化为具体的技术需求。

## 2.1 机制一：多源异构数据库统一适配需求

**现状痛点**：
工厂侧数据源类型多样，MySQL、PostgreSQL、SQLite、Hive、HDFS、HBase 等数据库的连接方式、SQL 方言、数据类型映射各不相同。传统方案需要为每种数据库单独编写导出脚本，维护成本极高。

**需求分析**：
系统必须构建一个统一的数据库连接工厂（Engine Factory），通过统一的配置接口，自动适配不同数据库引擎，并将数据库连接、表列表查询、数据导出等操作抽象为统一接口。

### 2.1.1 统一连接引擎工厂

输入：包含 `db_type`、`host`、`port`、`username`、`password`、`database` 等字段的数据库配置 JSON。
输出：SQLAlchemy Engine 对象（或 HDFS/HBase 等专用客户端）。

系统需根据 `db_type` 自动路由到对应的连接构建器，支持 MySQL、PostgreSQL、SQLite、Hive、HDFS、HBase 共六种数据源类型。

### 2.1.2 客服友好型连接测试

需求分析：前端用户配置数据库连接后，需要即时验证连接是否可用。系统需提供友好的错误提示，捕获常见错误（如网络不通、认证失败、数据库不存在等），并返回可读的中文/英文错误信息。

### 2.1.3 本机 IP 智能解析

当用户填写的数据库主机地址恰好是本机 IP 时，系统自动将其替换为 `127.0.0.1` 直连，避免 MySQL 等数据库对客户端 IP 进行反向 DNS 解析导致 10 秒级别的卡顿。

## 2.2 机制二：双模执行器与数据流编排需求

**现状痛点**：
简单的数据导出可以直接在 Python 进程中完成，但复杂的数据流处理（如多步骤转换、条件路由、实时流处理）需要 Apache NiFi 等专业工具。系统需要同时支持两种模式，并能在前端透明切换。

**需求分析**：
系统需实现执行器抽象层，支持 Local Executor（本地直连导出）和 NiFi Executor（提交到 NiFi 集群）两种模式，通过环境变量或运行时配置切换。

### 2.2.1 Local Executor 完整实现

Local Executor 直接使用 SQLAlchemy 连接数据库，执行 `SELECT` 查询，将结果写入 CSV/JSON/TSV 文件。支持：
- 分块读取（Chunked Read）避免内存溢出
- 原子写入（先写临时文件，成功后再 rename）
- 进度追踪与状态回调

### 2.2.2 NiFi Executor 对接

NiFi Executor 通过 NiFi REST API 与 NiFi 集群交互：
- 启停 Flow（Process Group）
- 提交任务参数
- 轮询任务状态
- 同步输出文件到本地目录

系统预留了 NiFi Flow 自动部署能力（通过 `NIFI_AUTO_DEPLOY_FLOW` 环境变量控制），但默认仅做后端对接，容器与 Flow 由运维预置。

### 2.2.3 统一逻辑目录输出

无论是 Local 还是 NiFi 执行器，其输出产物最终都落在统一的逻辑目录结构中，前端无需感知底层执行器差异。

## 2.3 机制三：多租户数据隔离与公私有化部署需求

**现状痛点**：
多个工厂的数据需要严格隔离。公有化部署时数据存公司 Ceph，私有化部署时数据存工厂 Ceph，管理员需要能拉取私有化工厂的数据到本地用于训练。

**需求分析**：
系统需实现基于用户名的多租户隔离体系，每个用户（工厂）拥有独立的数据目录，并通过 JWT 认证控制访问权限。

### 2.3.1 用户注册与目录自动创建

注册时根据用户选择的部署模式（公有化/私有化）自动创建对应的数据目录结构：
- 公有化：`/home/yhz/{username}/nifi-data/` 和 `real_nifi_data/`
- 私有化：`{ceph_endpoint}/nifi-data/` 和 `real_nifi_data/`

### 2.3.2 文件访问权限控制

- 管理员：可以查看所有用户的文件，支持下拉切换用户
- 普通用户：只能查看自己的文件
- 私有化用户：前端显示"拉取"按钮，管理员可手动触发数据拉取

### 2.3.3 双树文件系统

每个用户的前端文件中心展示双树结构：
- `nifi-data/`：Local Executor 产物
- `real_nifi_data/`：NiFi Executor 产物

## 2.4 机制四：静默导出与定时调度需求

**现状痛点**：
工厂数据是持续产生的，需要定期自动导出到平台。如果每次都需要手动触发，运维成本极高且容易遗漏。

**需求分析**：
系统需实现静默导出（Silent Export）机制，支持按 cron 表达式定时自动执行数据导出，并支持增量导出。

### 2.4.1 Manifest 驱动架构

每次手动导出时，系统自动将（数据库配置、表名）注册到 Manifest 清单中。后台调度器定时遍历 Manifest，对每个条目执行导出。

### 2.4.2 增量导出机制

首次导出为全量快照，后续导出为增量追加。通过 Marker Column（如 `updated_at`）标识增量数据，Schema 变更时自动滚存新文件并保留旧文件。

### 2.4.3 调度系统

基于 APScheduler 实现，支持：
- 简单周期（如 `5m`、`15m`、`1h`、`daily`）
- Cron 表达式
- 任务启停与动态管理

## 2.5 机制五：文件格式转换与标签管理需求

**现状痛点**：
数据文件格式多样（CSV/JSON/TSV），不同下游系统对格式要求不同。此外，AI 训练需要标注数据（标签），传统方式依靠人工逐行标注，效率极低。

**需求分析**：
系统需支持文件格式间的自动转换，并提供标签配置功能，使得文件可以携带元数据信息。

### 2.5.1 多格式互转

支持 CSV ↔ JSON ↔ TSV 六种互转方向，以及无格式转换的直传（如 CSV → CSV）。

### 2.5.2 标签配置与文件元数据

每个数据文件配有一个 `.meta.json` 配置文件，记录：
- 标签列名称
- 故障编码映射（如 `{"1": "故障", "0": "正常"}`）
- 列类型信息
- 操作记录

### 2.5.3 打标与预览

前端支持在线预览文件内容，并对指定行进行手动打标。标签写入 `.meta.json` 配置文件，不修改原始数据文件。

## 2.6 机制六：JWT 认证与安全需求

**现状痛点**：
平台需要对外开放，但缺乏统一的认证授权机制。敏感操作（如删除文件、管理用户）需要权限控制。

**需求分析**：
系统需实现基于 JWT 的认证体系，支持双数据库认证（本地 SQLite + NiFi 数据库），并对管理员接口实施权限校验。

### 2.6.1 JWT Token 管理

- 登录后颁发 JWT Token（默认 15 分钟过期）
- Token 通过 HttpOnly Cookie 传递，防止 XSS 攻击
- 支持 Token 刷新

### 2.6.2 管理员权限控制

管理员接口（如用户管理、静默导出配置、内部管理页面）通过 `_require_admin` 装饰器校验，非管理员返回 403。

### 2.6.3 防暴力破解

登录失败次数追踪，超过阈值后锁定账户一段时间。

---

# 3. 概要设计

## 3.1 设计总览

```
┌────────────────────────────────────────────────────────────────┐
│                        前端层 (Static Pages)                    │
│  • 文件中心 (File Center)     • 导出任务 (Export Jobs)          │
│  • 数据转换 (Convert)          • 标签管理 (Tags)                │
│  • 用户管理 (Admin)            • 内部管理 (Internal)            │
└───────────────────────────┬────────────────────────────────────┘
                            │ HTTP /api/v1
┌───────────────────────────▼────────────────────────────────────┐
│                    FastAPI 后端 (Port 8081)                     │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐     │
│  │  auth.py │ │ main.py  │ │admin.py  │ │ db_connect.py│     │
│  │  JWT认证 │ │ 核心路由 │ │ 管理接口 │ │ 数据库连接   │     │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────┘     │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐     │
│  │executors │ │ export_  │ │ silent_  │ │ scheduler.py │     │
│  │ 执行器层 │ │ worker.py│ │ export   │ │ 定时调度     │     │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────┘     │
└───────────────────────────┬────────────────────────────────────┘
                            │
            ┌───────────────┼───────────────┐
            ▼               ▼               ▼
    ┌──────────┐    ┌──────────┐    ┌──────────────┐
    │  SQLite  │    │  MySQL/  │    │  HDFS/HBase/ │
    │  (本地)  │    │  PG/...  │    │  Hive (Docker)│
    └──────────┘    └──────────┘    └──────────────┘
            │               │               │
            └───────────────┼───────────────┘
                            ▼
                ┌──────────────────────┐
                │   数据目录 (in_data)  │
                │  {username}/nifi-data│
                │  {username}/real_... │
                └──────────────────────┘
```

## 3.2 方案一：多源数据库统一连接引擎

针对工厂侧多源异构数据库的适配问题，方案一设计了"统一连接引擎工厂（Engine Factory）"：

1. **配置驱动连接**：所有数据库连接信息统一通过 JSON 配置对象传入，包含 `db_type`、`host`、`port`、`username`、`password`、`database` 等标准字段。

2. **引擎路由表**：根据 `db_type` 字段自动路由到对应的 SQLAlchemy 连接字符串构建器，支持 MySQL（`pymysql`）、PostgreSQL（`psycopg2`）、SQLite（文件路径）、Hive（`pyhive`）、HDFS（`hdfs` 库 InsecureClient）、HBase（`phoenixdb`）共六种类型。

3. **连接测试接口**：提供 `/api/v1/db/test-connection` 接口，支持前端即时验证数据库连接可用性，返回友好错误提示（如"认证失败，请检查用户名密码"、"数据库不存在"等）。

4. **本机 IP 智能解析**：通过 `_resolve_local_host` 函数，将用户填写的本机 IP 自动替换为 `127.0.0.1`，避免 MySQL 反向 DNS 解析卡顿。

## 3.3 方案二：双模执行器与统一产物目录

为了兼容简单直连导出与复杂数据流编排两种场景，方案二设计了"双模执行器抽象层"：

1. **执行器基类**：定义 `BaseExecutor` 抽象类，统一 `execute()` 接口签名，接收任务对象、输出文件创建回调、NiFi 输出文件解析回调等参数。

2. **MockExecutor（Local）**：直接在 Python 进程中完成数据导出，使用 `export_worker.py` 中的 `run_export_job` 函数执行 SQL 查询并写入文件。支持分块读取、原子写入、进度追踪。

3. **NiFiExecutor**：通过 `nifi_client.py` 中的 `NiFiClient` 类与 NiFi REST API 交互，支持登录认证、Flow 启停、任务提交、状态轮询、重试机制。输出文件通过 `nifi_orchestrator.py` 同步到本地统一目录。

4. **统一逻辑目录**：无论哪种执行器，输出产物最终落在 `{storage_root}/nifi-data/` 或 `{storage_root}/real_nifi_data/` 目录下，前端通过统一 API 访问。

## 3.4 方案三：多租户数据隔离与双模部署

方案三设计了"基于用户名的多租户隔离与公私有化双模部署架构"：

1. **用户名即工厂标识**：每个用户就是一个工厂，用户名作为唯一标识贯穿全链路。废弃旧的 `factory_id` 字段，统一使用 `username`。

2. **存储根解析**：`_resolve_user_storage_root(username)` 函数根据用户注册时的部署模式动态解析存储根路径：
   - 公有化：`/home/yhz/{username}/`
   - 私有化：`{ceph_endpoint}/`

3. **注册时自动建目录**：`_create_user_storage_dirs()` 函数在用户注册时自动创建 `nifi-data/` 和 `real_nifi_data/` 两个子目录。

4. **权限控制**：`_check_file_access` 函数在文件操作前校验当前用户与文件所属用户是否一致，管理员可绕过限制。

5. **管理员拉取**：私有化用户的数据存储在工厂侧 Ceph，管理员通过内部管理页面手动触发数据拉取到公司侧本地。

## 3.5 方案四：静默导出与增量同步

方案四设计了"基于 Manifest 的静默导出与增量同步机制"：

1. **Manifest 清单**：每次手动导出时，系统自动将（数据库配置、表名）注册到 `silent_export_manifest.json` 清单文件中。

2. **后台调度器**：基于 APScheduler 的 `BackgroundScheduler`，支持 cron 表达式和简单周期。定时遍历 Manifest，对每个条目执行导出。

3. **增量导出**：通过 Marker Column（如 `updated_at`）识别增量数据。首次导出为全量快照，后续导出仅追加增量行。Schema 变更时自动滚存新文件。

4. **文件锁**：使用 `fcntl.flock` 文件锁防止并发写冲突。

5. **配置热更新**：静默导出配置（如 cron 表达式、保留天数、增量标记列）可通过 API 动态修改，无需重启服务。

## 3.6 方案五：文件格式转换与标签元数据

方案五设计了"格式互转引擎与文件级元数据配置体系"：

1. **六向格式互转**：通过统一的 NiFi 目录结构，CSV、JSON、TSV 之间支持六种互转方向。每种转换对应一个独立的输入/输出子目录。

2. **.meta.json 配置文件**：每个数据文件配有一个 `.meta.json` 元数据文件，记录标签配置、列信息、操作历史等。元数据文件与数据文件存放在同一目录下。

3. **标签功能**：支持手动打标和自动打标。标签信息写入 `.meta.json` 文件，不修改原始数据文件，保证数据完整性。

## 3.7 方案六：JWT 认证与安全防护

方案六设计了"基于 JWT 的双数据库认证与权限控制体系"：

1. **双数据库认证**：支持本地 SQLite 数据库（`iot_users` 表）和 NiFi 数据库两种认证后端，通过 `IOT_AUTH_DB` 环境变量切换。

2. **JWT Token**：登录成功后颁发 JWT Token，通过 HttpOnly Cookie 传递。Token 包含 `username`、`is_admin` 等字段，默认 15 分钟过期。

3. **管理员接口保护**：所有管理员接口（`/internal/*`）通过 `_require_admin` 装饰器校验，非管理员返回 403。

4. **防暴力破解**：`failed_attempts` 和 `locked_until` 字段记录登录失败次数和锁定时间，超过阈值后锁定账户。

## 3.8 效果分析

- **效果一：多源数据库统一适配**，通过 Engine Factory 统一连接六种异构数据源，前端只需填写标准配置即可完成连接，无需关心底层差异。
- **效果二：双模执行器灵活切换**，Local 模式适合简单场景，NiFi 模式适合复杂数据流编排，前端通过配置透明切换，产物统一输出。
- **效果三：多租户严格隔离**，每个用户拥有独立的数据目录和权限控制，管理员可全局管理，普通用户仅见自己数据。
- **效果四：静默导出自动化**，数据表一旦被手动导出过一次，后续自动纳入定时导出清单，实现增量同步，无需人工干预。
- **效果五：文件格式自由转换**，CSV/JSON/TSV 六向互转，配合标签元数据体系，为 AI 训练提供标准化数据供给。
- **效果六：安全认证体系**，JWT + 双数据库认证 + 防暴力破解，保障平台安全。

---

# 4. 详细设计

基于系统真实生产代码，各核心功能模块描述如下：

| 模块 | 文件 | 职责 |
|------|------|------|
| main.py | `v1-backend/app/main.py` | FastAPI 入口，挂载路由，全局异常处理，CORS 配置，目录初始化 |
| auth.py | `v1-backend/app/auth.py` | JWT 认证、用户注册/登录、Token 刷新、防暴力破解、用户目录创建 |
| db_connect.py | `v1-backend/app/db_connect.py` | 数据库连接测试、表列表查询、列信息获取、HDFS/HBase 浏览 |
| db_models.py | `v1-backend/app/db_models.py` | SQLAlchemy 数据模型：Job、File、Tag、ExportJob、IotUser |
| engine_factory.py | `v1-backend/app/engine_factory.py` | 多源数据库引擎工厂，统一创建 MySQL/PG/SQLite/Hive/HDFS/HBase 连接 |
| executors.py | `v1-backend/app/executors.py` | 执行器抽象层：MockExecutor（本地）、NiFiExecutor（远程） |
| export_worker.py | `v1-backend/app/export_worker.py` | 数据导出核心逻辑：SQL 查询、分块写入、原子写入、进度追踪 |
| silent_export_worker.py | `v1-backend/app/silent_export_worker.py` | 静默导出：Manifest 管理、增量导出、文件锁、配置热更新 |
| scheduler.py | `v1-backend/app/scheduler.py` | APScheduler 调度封装：cron/简单周期、任务启停 |
| nifi_client.py | `v1-backend/app/nifi_client.py` | NiFi REST API 客户端：登录、Flow 启停、任务提交、状态轮询 |
| nifi_orchestrator.py | `v1-backend/app/nifi_orchestrator.py` | NiFi 编排层：容器管理、资产部署、目录同步 |
| meta_json.py | `v1-backend/app/meta_json.py` | .meta.json 配置文件读写：标签规则、列信息、操作历史 |
| admin_routes.py | `v1-backend/app/admin_routes.py` | 管理员接口：静默导出配置、租户管理、用户管理 |
| local_ip.py | `v1-backend/app/local_ip.py` | 本机 IP 检测与智能解析 |

## 4.1 数据库连接与引擎工厂模块

主入口函数为 `engine_factory.py` 中的 `engine_from_config`，负责根据数据库配置创建对应的连接引擎。

### 4.1.1 连接引擎路由

系统根据 `db_type` 字段自动路由到对应的连接构建器：

| db_type | 连接方式 | 依赖库 |
|---------|---------|--------|
| mysql | `mysql+pymysql://{user}:{password}@{host}:{port}/{database}` | pymysql |
| postgresql / postgres | `postgresql+psycopg2://{user}:{password}@{host}:{port}/{database}` | psycopg2 |
| sqlite | `sqlite:///{database}` (文件路径) | sqlite3 (内置) |
| hive | `hive://{host}:{port}/{database}` | pyhive |
| hdfs | `InsecureClient(url)` | hdfs |
| hbase | `phoenixdb.connect(url)` | phoenixdb |

### 4.1.2 连接测试接口

`POST /api/v1/db/test-connection` 接口接收标准数据库配置，尝试建立连接并执行 `SELECT 1` 验证。失败时返回友好错误提示：

- 网络不通：`无法连接到 {host}:{port}，请检查网络和防火墙`
- 认证失败：`认证失败，请检查用户名和密码`
- 数据库不存在：`数据库 {database} 不存在`

### 4.1.3 本机 IP 智能解析

`local_ip.py` 中的 `_collect_local_ips` 函数收集本机所有网络接口 IP，`_resolve_local_host` 函数将匹配的本机 IP 替换为 `127.0.0.1`，避免 MySQL 反向 DNS 解析导致的连接卡顿（可达 10 秒级别）。

## 4.2 数据导出核心模块

主入口函数为 `export_worker.py` 中的 `run_export_job`，负责执行完整的数据库导出流程。

### 4.2.1 导出流程

1. **参数解析**：从任务 payload 中提取数据库配置、SQL 查询（或表名）、目标格式、用户信息。
2. **引擎创建**：调用 `engine_from_config` 创建数据库连接。
3. **SQL 执行**：通过 SQLAlchemy 的 `engine.execute()` 或 `inspect` 获取表结构。
4. **分块读取**：使用 `chunksize=50000` 分块读取查询结果，避免内存溢出。
5. **格式写入**：根据目标格式（CSV/JSON/TSV）调用对应的写入器，支持自定义分隔符。
6. **原子写入**：先写入临时文件（`.tmp`），成功后 `shutil.move` 到目标路径，防止写入中断导致文件损坏。
7. **进度追踪**：通过 `job["progress"]` 字段实时更新进度，供前端轮询。

### 4.2.2 安全措施

- **SQL 注入防护**：表名和列名通过 `_safe_ident` 正则校验（`[A-Za-z_][A-Za-z0-9_]*`），拒绝非法字符。
- **CSV 字段大小限制**：通过 `csv.field_size_limit` 设置最大字段大小（默认 20MB），防止超大单列内容导致解析错误。
- **用户隔离**：导出目录通过 `_resolve_user_storage_root` 解析，确保不同用户的导出产物物理隔离。

## 4.3 执行器抽象层

### 4.3.1 MockExecutor（本地执行器）

`MockExecutor` 实现了完整的同步/异步导出流程：
- 状态流转：`PENDING → RUNNING → SUCCEEDED / FAILED`
- 进度更新：通过 `job["progress"]` 字段从 0 到 100
- 输出文件：调用 `create_output_file` 回调生成文件记录

### 4.3.2 NiFiExecutor（远程执行器）

`NiFiExecutor` 通过 REST API 与 NiFi 集群交互：
- **登录认证**：通过 `POST /nifi-api/access/token` 获取 JWT Token
- **Flow 启停**：通过 `PUT /nifi-api/flow/process-groups/{id}` 控制 Flow 运行状态
- **任务提交**：通过 `POST /nifi-api/process-groups/{id}/input-ports/{port}/data` 提交输入数据
- **状态轮询**：通过 `GET /nifi-api/flow/process-groups/{id}` 查询 Flow 运行状态
- **重试机制**：支持配置重试次数和重试间隔，应对网络抖动

### 4.3.3 模式切换

通过环境变量 `APP_EXECUTOR_MODE` 控制：
- `mock`（默认）：使用 MockExecutor
- `nifi`：使用 NiFiExecutor

## 4.4 静默导出与定时调度模块

### 4.4.1 Manifest 管理

`silent_export_worker.py` 中的 `_load_manifest()` 和 `_save_manifest()` 函数管理 Manifest 清单文件。Manifest 结构：

```json
{
  "tenant_001": {
    "db_key_001": {
      "table_name": "sensor_data",
      "db_config": { "db_type": "mysql", "host": "...", ... },
      "last_run": "2026-07-03T10:00:00+08:00",
      "marker_column": "updated_at",
      "last_marker_value": "2026-07-03T09:00:00"
    }
  }
}
```

### 4.4.2 增量导出逻辑

`process_loop_once` 函数遍历 Manifest 中的每个条目：
1. 解析 Marker Column 和上次 Marker Value
2. 构造 `SELECT * FROM {table} WHERE {marker_column} > '{last_value}'` 增量查询
3. 如果 Marker Column 无新数据，跳过
4. 如果表结构发生变更（列数变化），生成新的带时间戳的文件名，保留旧文件
5. 写入完成后更新 Manifest 中的 `last_marker_value`

### 4.4.3 调度器封装

`scheduler.py` 封装了 APScheduler 的 `BackgroundScheduler`：
- `parse_simple_schedule(spec)`：解析调度规格，支持 `5m`/`15m`/`1h`/`daily` 以及标准 cron 表达式
- `schedule_job(job_id, spec, func)`：注册定时任务
- `remove_scheduled(job_id)`：移除定时任务
- `start_scheduler()` / `stop_scheduler()`：启停调度器

## 4.5 文件格式转换模块

### 4.5.1 转换目录结构

每种格式转换对应一个独立的输入/输出子目录：

| 转换方向 | 输入目录 | 输出目录 |
|---------|---------|---------|
| CSV → JSON | `inbox_csv` | `csv_to_json` |
| JSON → CSV | `inbox_json` | `json_to_csv` |
| TSV → JSON | `inbox_tsv` | `tsv_to_json` |
| JSON → TSV | `inbox_json` | `json_to_tsv` |
| CSV → TSV | `inbox_csv` | `csv_to_tsv` |
| TSV → CSV | `inbox_tsv` | `tsv_to_csv` |

### 4.5.2 转换 API

前端通过 `/api/v1/convert` 接口提交转换任务，指定源文件路径和目标格式。后端读取源文件，执行格式转换，将结果写入对应的输出目录。

## 4.6 标签与文件元数据模块

### 4.6.1 .meta.json 结构

每个数据文件对应一个 `.meta.json` 配置文件，结构如下：

```json
{
  "metaVersion": "1.0",
  "fileId": "uuid",
  "fileName": "export.csv",
  "filePath": "/path/to/export.csv",
  "tagColumn": "label",
  "failedTags": { "1": "故障", "0": "正常" },
  "columns": [
    { "name": "timestamp", "type": "datetime" },
    { "name": "sensor_1", "type": "numeric" }
  ],
  "tags": [
    { "rowId": "1", "label": "1", "operator": "admin", "createdAt": "..." }
  ],
  "createdAt": "2026-07-03T10:00:00+08:00",
  "updatedAt": "2026-07-03T10:00:00+08:00"
}
```

### 4.6.2 标签操作

- **手动打标**：前端选中文件行，选择标签值，调用 `POST /api/v1/tags` 写入 `.meta.json`
- **自动打标**：根据配置文件中的 `failedTags` 规则自动匹配列值
- **标签查询**：`GET /api/v1/tags/{file_id}` 返回文件的所有标签

## 4.7 用户认证与权限模块

### 4.7.1 用户模型

`IotUser` 模型（`iot_users` 表）字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 主键自增 |
| username | String(128) | 用户名，唯一索引 |
| password_hash | String(256) | SHA256 密码哈希 |
| is_admin | Integer | 是否管理员（0/1） |
| deployment_mode | String(32) | 部署模式：public/private |
| ceph_endpoint | String(512) | 私有化 Ceph 端点 |
| failed_attempts | Integer | 登录失败次数 |
| locked_until | DateTime | 锁定截止时间 |

### 4.7.2 登录流程

1. 前端提交 `username` + `password` + 可选 `db` 配置
2. 后端查询 `iot_users` 表（或 NiFi 数据库）验证凭据
3. 验证通过后生成 JWT Token，写入 HttpOnly Cookie
4. 返回用户信息（含 `is_admin`、`deployment_mode`）

### 4.7.3 权限校验

- `_get_current_user_from_token`：从 Cookie 中解析 JWT，返回用户信息
- `_require_admin`：校验用户是否为管理员，否则返回 403
- `_check_file_access`：校验当前用户是否有权访问指定文件

## 4.8 NiFi 编排模块

### 4.8.1 NiFi 目录结构

`nifi_orchestrator.py` 管理 NiFi 侧的数据目录结构：

```
real_nifi_data/
├── export_jobs/
│   ├── inbox/          ← 待处理任务
│   ├── done/           ← 已完成任务
│   ├── error/          ← 失败任务
│   └── .iot_mysql_export_flow_v1.ready.json  ← Flow 部署标记
├── output_csv/         ← CSV 导出结果
├── output_json/        ← JSON 导出结果
├── output_tsv/         ← TSV 导出结果
├── inbox_csv/          ← CSV 输入
├── inbox_json/         ← JSON 输入
├── inbox_tsv/          ← TSV 输入
├── tagged_output/      ← 打标输出
└── bin/
    └── nifi_db_export_worker.py  ← NiFi Worker 脚本
```

### 4.8.2 NiFi 容器管理

通过 `nifi_orchestrator.py` 中的函数与 Docker 容器交互：
- `ensure_nifi_dirs()`：确保 NiFi 数据目录存在
- `ensure_mysql_export_worker_assets()`：部署 Worker 脚本到 NiFi 容器
- `ensure_flow_deployed()`：通过 NiFi API 部署预定义的 Flow 模板

### 4.8.3 生产环境策略

默认实施口径：NiFi 容器与 Flow 由运维或脚本预置，后端只负责对接、启停既有 Flow、提交任务和同步结果。自动创建容器或自动部署 Flow 仅作为显式开关能力（通过 `NIFI_AUTO_CREATE_CONTAINER`、`NIFI_AUTO_START_CONTAINER`、`NIFI_AUTO_DEPLOY_FLOW` 环境变量控制）。

---

# 5. 部署架构

## 5.1 服务清单

| 服务名 | 类型 | 描述 |
|--------|------|------|
| iot-backend.service | systemd | FastAPI 后端服务，监听 `127.0.0.1:8081` |
| iot-frontend.service | systemd | 前端静态服务，监听 `0.0.0.0:5174` |
| iot-backend-health.timer | systemd timer | 后端健康检查定时器，配合断电恢复 |
| api-worker.service | systemd | CursorPool 依赖进程 |
| iot-nifi.service | systemd | NiFi Docker 容器服务 |
| Hadoop Stack | Docker Compose | 最小 Hadoop 生态（HDFS/HBase/Hive） |

## 5.2 健康检查

| 端点 | 说明 |
|------|------|
| `GET /health` | 后端基础健康检查 |
| `GET /api/v1/health/databases` | 数据库连接状态检查 |

## 5.3 访问地址

| 地址 | 说明 |
|------|------|
| `http://127.0.0.1:5174` | 本机前端 |
| `http://202.113.76.55:5174` | 局域网前端 |
| `http://127.0.0.1:8081` | 后端 API |
| `http://127.0.0.1:8081/health` | 后端健康检查 |

---

# 6. 总结

本文档详细阐述了智慧数据平台的技术实现方案。通过多源数据库统一连接引擎、双模执行器抽象层、多租户数据隔离、静默导出与增量同步、文件格式转换与标签元数据、JWT 认证与安全防护这六大核心机制，系统成功解决了工业数据管理中的数据源异构、部署模式多样、数据隔离严格、导出自动化要求高等关键痛点。

平台构建了一个完整的工业数据采集、转换、管理与供给流水线，从数据库连接、数据导出、格式转换到文件管理、标签标注，高度自动化且支持多种数据库类型。通过 Manifest 驱动的静默导出、JWT 认证授权、公私有化双模部署等设计，大幅降低了人工运维成本，提高了数据管理的可靠性和安全性。

当前版本（V4）已完成：
- 六大数据库类型的统一连接与导出
- Local 执行器完整实现，NiFi 执行器框架就绪
- 多租户用户隔离与公私有化双模部署
- 静默导出与定时调度
- 文件格式互转与标签元数据管理
- JWT 双数据库认证与权限控制
- systemd 服务化部署与健康检查