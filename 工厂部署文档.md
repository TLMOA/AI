# IoT 智慧平台工厂部署文档

> 版本：v1.0 | 日期：2026-08-09
> 适用范围：将 IoT 智慧平台（数据处理模块 + AI 大模型模块）部署到工厂生产环境

---

## 目录

1. 部署架构总览
2. 硬件与系统要求
3. 前置依赖安装
4. 数据处理模块部署
5. AI 大模型模块部署
6. NiFi 容器部署
7. 数据库初始化
8. 安全加固清单
9. 生产环境配置
10. 启动与自检
11. 系统服务管理
12. 备份与恢复
13. 监控与日志
14. 故障排查手册
15. 上线检查清单

---

## 1. 部署架构总览

```
浏览器（工厂用户/管理员）
    │
    ▼
┌──────────────────────┐        ┌──────────────────────────┐
│  Nginx (HTTPS 443)   │        │  AI 前端 (3002)          │
│  · 静态资源           │        │  · AI 大模型模块 UI        │
│  · 反向代理 /api/v1   │        │  · 需独立 TLS             │
└─────────┬────────────┘        └──────────┬───────────────┘
          │                                │
          ▼                                ▼
┌──────────────────────┐        ┌──────────────────────────┐
│ 数据处理后端 (8081)    │        │  AI 后端 (8002)          │
│  FastAPI + systemd    │        │  FastAPI + conda env     │
│  /home/iot/{user}/    │        │  ludwig/optuna/pycaret   │
└─────────┬────────────┘        └──────────┬───────────────┘
          │                                │
          ▼                                ▼
┌──────────────────────┐        ┌──────────────────────────┐
│  MySQL (nifi 库)      │        │  NVIDIA GPU (A5000)      │
│  · 用户/认证           │        │  · 模型训练/推理          │
└─────────┬────────────┘        └──────────────────────────┘
          │
          ▼
┌──────────────────────┐
│  NiFi 容器 (8080)     │
│  iot-nifi            │
│  · DB导出/转换/打标    │
│  · 挂载 /home/iot     │
└──────────────────────┘
```

### 1.1 组件清单

| 组件 | 技术栈 | 端口 | 启动方式 |
|------|--------|------|----------|
| 数据处理前端 | 原生 HTML/JS + serve.py | 5174 | `python3 serve.py` |
| 数据处理后端 | FastAPI (uvicorn) | 8081 | systemd `iot-backend` |
| AI 前端 | Vue3 + Vite | 3002 | `npm run dev` |
| AI 后端 | FastAPI (uvicorn) | 8002 | conda env + nohup |
| NiFi | Apache NiFi 2.8.0 (容器) | 8080 | docker compose |
| MySQL | 认证源 | 3306 | systemd |
| Nginx | 反向代理 + TLS | 443 | systemd |

### 1.2 数据目录规则

每个用户根目录下 4 个数据根，各含 12 个子目录：

```
/home/iot/{username}/
├── nifi-data/              ← Local 无标签
├── real_nifi_data/         ← NiFi 无标签
├── tagged_nifi_data/       ← Local 有标签（hasTag=true）
└── tagged_real_nifi_data/  ← NiFi 有标签（hasTag=true）
    ├── inbox_csv/  inbox_json/  inbox_tsv/
    ├── csv_to_json/  csv_to_tsv/
    ├── json_to_csv/  json_to_tsv/
    ├── tsv_to_json/  tsv_to_csv/
    └── output_csv/  output_json/  output_tsv/
```

---

## 2. 硬件与系统要求

### 2.1 服务器要求

| 配置项 | 最低要求 | 推荐配置 |
|--------|----------|----------|
| CPU | 8 核 | 16 核（AI 训练并行） |
| 内存 | 16 GB | 32 GB（Ludwig 训练） |
| 磁盘 | 100 GB | 500 GB SSD（数据增长） |
| GPU | 无（纯数据处理） | **NVIDIA 16GB+**（AI 模块必需） |
| 系统 | Ubuntu 20.04+/CentOS 8+ | Ubuntu 22.04 LTS |
| Python | 3.10+ | 3.11（数据处理）、3.10（AI conda） |

### 2.2 网络要求

| 端口 | 用途 | 对外暴露 |
|------|------|----------|
| 443 | HTTPS（Nginx） | ✅ 必需 |
| 5174 | 数据处理前端 | ❌ 仅内网（或经 Nginx） |
| 8081 | 数据处理后端 | ❌ 仅本机 |
| 3002 | AI 前端 | ❌ 仅内网（或经 Nginx） |
| 8002 | AI 后端 | ❌ 仅本机 |
| 8080 | NiFi | ❌ 仅内网（管理用） |
| 3306 | MySQL | ❌ 仅本机 |

> **安全原则**：所有后端端口禁止直接暴露公网，统一由 Nginx 443 反向代理。

---

## 3. 前置依赖安装

### 3.1 系统基础包

```bash
sudo apt update && sudo apt install -y \
  python3 python3-pip python3-venv \
  nginx mysql-server docker.io docker-compose \
  git curl wget htop \
  libpq-dev unixodbc-dev gcc g++ make
```

### 3.2 Python 环境

```bash
# 数据处理模块虚拟环境
cd /home/iot/iot/v1-backend
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

# AI 模块 conda 环境
conda create -n iot_clone python=3.10 -y
conda activate iot_clone
cd /home/iot/solo_ai_iot/banckend
pip install -r requirements.txt
```

### 3.3 前端依赖

```bash
# 数据处理前端（纯静态，无需构建）
# AI 前端
cd /home/iot/solo_ai_iot/frontend
npm install
```

---

## 4. 数据处理模块部署

### 4.1 目录结构

```
v1-backend/
├── app/
│   ├── main.py              # 主应用（FastAPI）
│   ├── auth.py              # 认证（JWT + MySQL）
│   ├── meta_json.py         # 元数据 JSON
│   ├── meta_xattr.py        # xattr 扩展属性
│   ├── nifi_orchestrator.py # NiFi 编排
│   └── silent_export_worker.py
├── scripts/
│   ├── nifi_upload_convert_worker.py
│   ├── nifi_db_export_worker.py
│   └── nifi_auto_tagging_worker.py
├── deploy/
│   ├── iot-backend.service
│   └── iot-backend.service.override.conf
└── requirements.txt
```

### 4.2 安装 systemd 服务

```bash
sudo cp v1-backend/deploy/iot-backend.service /etc/systemd/system/
sudo cp v1-backend/deploy/iot-backend.service.override.conf /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable iot-backend
sudo systemctl start iot-backend
```

### 4.3 前端启动

```bash
cd v1-frontend
nohup python3 serve.py > /tmp/frontend.log 2>&1 &
```

> **注意**：前端必须用 `serve.py`（内置 /api/v1 → 8081 代理），禁止用 `python3 -m http.server`（无代理）。

---

## 5. AI 大模型模块部署

### 5.1 后端启动

```bash
cd /home/iot/solo_ai_iot/banckend
nohup /home/iot/miniconda3/envs/iot_clone/bin/python main.py > logs.log 2>&1 &
```

### 5.2 前端启动

```bash
cd /home/iot/solo_ai_iot/frontend
nohup npm run dev > /tmp/ai_frontend.log 2>&1 &
```

### 5.3 数据要求

| 用途 | 目录 | 要求 |
|------|------|------|
| 训练 | `{user}/nifi-data/` 或 `real_nifi_data/` | 需配套 `.meta.json`（tagColumn/datesetname/failedTag） |
| 预测 | `{user}/tagged_nifi_data/` 或 `tagged_real_nifi_data/` | meta 的 datesetname 须与已部署模型匹配 |

### 5.4 模型管理

- 训练后 leaderboard「一键部署至 IoT」→ `deployed_models/admin/`
- 预测接口 `/api/predict/batch_file` 按 dataset_name 匹配已部署模型
- 支持 10 种模型：xgboost/lightgbm/catboost/hgb/TabNet/TabTransformer/Deep-MLP/CNN/LSTM/GRU

---

## 6. NiFi 容器部署

### 6.1 构建镜像

```bash
cd /home/iot/iot/docker/nifi
docker build -t iot-nifi-python:latest .
```

### 6.2 启动容器

```bash
cd /home/iot/iot/docker/nifi
docker compose up -d
```

### 6.3 容器配置

| 配置项 | 值 |
|--------|-----|
| 容器名 | iot-nifi |
| 镜像 | iot-nifi-python:latest |
| 端口 | 8080:8443（HTTPS） |
| 挂载 | `/home/iot/real_nifi_data` → `/opt/nifi/.../data/iot` |
| 挂载 | `/home/iot` → `/home/iot:rw`（用户目录可见） |
| 重启策略 | unless-stopped |

### 6.4 Worker 脚本部署

```bash
docker cp v1-backend/scripts/nifi_upload_convert_worker.py iot-nifi:/opt/nifi/nifi-current/data/iot/bin/
docker cp v1-backend/scripts/nifi_db_export_worker.py iot-nifi:/opt/nifi/nifi-current/data/iot/bin/
docker cp v1-backend/scripts/nifi_auto_tagging_worker.py iot-nifi:/opt/nifi/nifi-current/data/iot/bin/
```

> NiFi flow 已持久化在 FlowFile Repository，重启容器自动恢复。

---

## 7. 数据库初始化

### 7.1 MySQL（认证源）

```sql
CREATE DATABASE IF NOT EXISTS nifi CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER IF NOT EXISTS 'iot'@'localhost' IDENTIFIED BY '<强密码>';
GRANT ALL PRIVILEGES ON nifi.* TO 'iot'@'localhost';
FLUSH PRIVILEGES;
```

### 7.2 用户表结构

```sql
CREATE TABLE IF NOT EXISTS iot_users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(64) NOT NULL UNIQUE,
    password_hash VARCHAR(255) NOT NULL,
    is_admin TINYINT(1) DEFAULT 0,
    deployment_mode VARCHAR(20) DEFAULT 'public',
    ceph_endpoint VARCHAR(255) DEFAULT '',
    failed_attempts INT DEFAULT 0,
    locked_until DATETIME NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 7.3 初始管理员

```bash
# 通过注册接口创建 admin 用户，或直接插入
INSERT INTO iot_users (username, password_hash, is_admin)
VALUES ('admin', '<bcrypt_hash>', 1);
```

---

## 8. 安全加固清单

> ⚠️ **以下为上线前必须处理的安全问题**（基于 2026-08-09 安全审查）：

### 8.1 严重（必须修复）

| # | 问题 | 风险 | 修复方案 |
|---|------|------|----------|
| 1 | **JWT 密钥默认值** `dev-secret-change-me` | 攻击者可伪造任意用户 token（含 admin） | 设置环境变量 `IOT_SECRET_KEY` 为强随机值 |
| 2 | **前端硬编码数据库凭据**（app.js 内 202.113.76.55 的 root 密码等） | 敏感信息泄露 | 移除前端默认凭据，改为空值+用户填写 |
| 3 | **CORS `allow_origins=["*"]` + `allow_credentials=True`** | 跨站请求伪造风险 | 改为具体域名白名单 |
| 4 | **路径穿越**：`_normalize_username` 不过滤 `..`/`/` | 任意文件读写 | 用户名统一用 `_sanitize_filename_component` 过滤 |
| 5 | **DB 导出 SSRF**：允许任意 host/port | 内网探测/攻击 | 限制 host 白名单或禁用任意 IP 连接 |

### 8.2 重要（建议修复）

| # | 问题 | 风险 | 修复方案 |
|---|------|------|----------|
| 6 | 前端/后端无 TLS | 数据明文传输 | Nginx 配置 HTTPS 证书 |
| 7 | MySQL 默认密码 root/root | 数据库入侵 | 修改强密码 + 仅本机监听 |
| 8 | NiFi 默认密码 admin@nifi123 | NiFi 被接管 | 修改 login-identity-providers.xml |
| 9 | NiFi 挂载整个 `/home/iot` 可写 | 容器逃逸风险 | 最小化挂载为 `/home/iot/{user}` |
| 10 | 无登录速率限制（仅失败锁定） | 暴力破解 | 加 IP 级限速 |
| 11 | 无审计日志 | 无法追溯 | 启用请求日志 + 操作审计 |
| 12 | Docker 无资源限制 | 容器耗尽资源 | compose 加 mem/CPU limit |

### 8.3 生产环境变量

```bash
# /etc/systemd/system/iot-backend.service.override.conf
[Service]
Environment=IOT_SECRET_KEY=<强随机密钥>
Environment=IOT_AUTH_DB=nifi
Environment=NIFI_DB_TYPE=mysql
Environment=NIFI_DB_HOST=127.0.0.1
Environment=NIFI_DB_PORT=3306
Environment=NIFI_DB_USER=iot
Environment=NIFI_DB_PASSWORD=<强密码>
Environment=NIFI_DB_NAME=nifi
Environment=META_XATTR_KEY=<base64 32字节>
Environment=IOT_ACCESS_EXPIRE_SECONDS=3600
Environment=NIFI_REAL_EXECUTION=true
Environment=IOT_COOKIE_SECURE=true
```

---

## 9. 生产环境配置

### 9.1 Nginx 反向代理

```nginx
server {
    listen 443 ssl;
    server_name iot.factory.com;

    ssl_certificate     /etc/nginx/ssl/iot.crt;
    ssl_certificate_key /etc/nginx/ssl/iot.key;

    # 数据处理前端
    location / {
        proxy_pass http://127.0.0.1:5174;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # 后端 API
    location /api/v1/ {
        proxy_pass http://127.0.0.1:8081;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # AI 前端（可选）
    location /ai/ {
        proxy_pass http://127.0.0.1:3002/;
        proxy_set_header Host $host;
    }

    # 上传大小限制
    client_max_body_size 100m;
}
```

### 9.2 环境变量清单

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `IOT_SECRET_KEY` | JWT 签名密钥（**必须设置**） | dev-secret-change-me |
| `IOT_AUTH_DB` | 认证源（nifi=MySQL） | nifi |
| `NIFI_DB_*` | MySQL 连接配置 | — |
| `META_XATTR_KEY` | xattr AES-256 密钥 | — |
| `NIFI_REAL_EXECUTION` | 启用真实 NiFi 执行 | false |
| `IOT_ACCESS_EXPIRE_SECONDS` | 会话有效期 | 86400 |
| `IOT_COOKIE_SECURE` | Cookie Secure 标志 | false |

---

## 10. 启动与自检

### 10.1 启动顺序

```bash
# 1. MySQL
sudo systemctl start mysql

# 2. NiFi 容器
docker compose -f docker/nifi/docker-compose.yml up -d

# 3. 数据处理后端
sudo systemctl start iot-backend

# 4. 数据处理前端
cd v1-frontend && nohup python3 serve.py > /tmp/frontend.log 2>&1 &

# 5. AI 后端
cd solo_ai_iot/banckend && nohup python3 main.py > logs.log 2>&1 &

# 6. AI 前端
cd solo_ai_iot/frontend && nohup npm run dev > /tmp/ai_frontend.log 2>&1 &

# 7. Nginx
sudo systemctl start nginx
```

### 10.2 自检命令

```bash
# 后端健康
curl -s http://127.0.0.1:8081/api/v1/health
curl -s http://127.0.0.1:8081/api/v1/auth/me

# 前端
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:5174

# AI 后端
curl -s http://127.0.0.1:8002/api/gpu/status

# NiFi
curl -sk https://127.0.0.1:8080/nifi-api/access/config

# 数据目录
ls /home/iot/admin/nifi-data/
```

### 10.3 功能自检

1. 浏览器访问 `https://iot.example.com`，用 admin 登录
2. 上传 CSV → 检查 `inbox_csv/` 有文件
3. 转换 CSV→JSON → 检查 `csv_to_json/`
4. 自动打标 → 检查 `tagged_nifi_data/` 或 `tagged_real_nifi_data/`
5. DB 导出 → 检查 `output_csv/`
6. AI 模块：训练一个模型 → 部署 → 预测

---

## 11. 系统服务管理

### 11.1 常用命令

```bash
# 后端
sudo systemctl status iot-backend
sudo systemctl restart iot-backend
sudo journalctl -u iot-backend -f

# 前端（手动进程）
pkill -f "serve.py"
cd v1-frontend && nohup python3 serve.py > /tmp/frontend.log 2>&1 &

# AI
pkill -f "main.py"
cd solo_ai_iot/banckend && nohup python3 main.py > logs.log 2>&1 &

# NiFi
docker compose -f docker/nifi/docker-compose.yml restart
```

### 11.2 开机自启

```bash
# 后端
sudo systemctl enable iot-backend

# 前端/AI（建议创建 systemd 服务）
# iot-frontend.service、ai-backend.service、ai-frontend.service
```

---

## 12. 备份与恢复

### 12.1 需备份内容

| 内容 | 路径 | 频率 |
|------|------|------|
| 用户数据 | `/home/iot/{user}/` | 每日 |
| 数据库 | MySQL `nifi` 库 | 每日 |
| NiFi flow | `/home/iot/iot/real_nifi_conf/` | 每次变更 |
| 模型 | `/home/iot/solo_ai_iot/banckend/storage/` | 每次训练 |
| 配置 | `/home/iot/iot/v1-backend/deploy/` | 每次变更 |

### 12.2 备份脚本示例

```bash
#!/bin/bash
# /home/iot/scripts/backup.sh
BACKUP_DIR=/data/backup/$(date +%Y%m%d)
mkdir -p $BACKUP_DIR

# MySQL
mysqldump -u iot -p$MYSQL_PASS nifi > $BACKUP_DIR/nifi.sql

# 用户数据（排除日志）
rsync -a --exclude='*.log' /home/iot/ /data/backup/users/

# 保留 7 天
find /data/backup -type d -mtime +7 -exec rm -rf {} \;
```

### 12.3 恢复

```bash
# 数据库
mysql -u iot -p nifi < nifi.sql

# 用户数据
rsync -a /data/backup/users/ /home/iot/

# NiFi flow
docker cp real_nifi_conf/flow.json.gz iot-nifi:/opt/nifi/nifi-current/conf/
docker restart iot-nifi
```

---

## 13. 监控与日志

### 13.1 日志位置

| 服务 | 日志 |
|------|------|
| 数据处理后端 | `journalctl -u iot-backend -f` |
| 数据处理前端 | `/tmp/frontend.log` |
| AI 后端 | `/home/iot/solo_ai_iot/banckend/logs.log` |
| AI 前端 | `/tmp/ai_frontend.log` |
| NiFi | `docker logs -f iot-nifi` |
| Nginx | `/var/log/nginx/access.log`、`error.log` |

### 13.2 关键监控指标

| 指标 | 检查方式 | 告警阈值 |
|------|----------|----------|
| 后端存活 | `systemctl is-active iot-backend` | 非 active |
| 磁盘空间 | `df -h /home/iot` | < 20% |
| 内存 | `free -h` | < 10% |
| GPU | `nvidia-smi` | 显存 < 2GB |
| NiFi 队列 | NiFi UI | 堆积 > 1000 |
| MySQL 连接 | `mysqladmin status` | 连接数 > 80% |

### 13.3 建议监控方案

- **Prometheus + node_exporter**：采集系统指标
- **cAdvisor**：容器指标
- **Grafana**：可视化面板
- **cron + curl**：简单存活探针

---

## 14. 故障排查手册

### 14.1 后端无法启动

```bash
journalctl -u iot-backend -n 50
# 常见原因：端口占用、MySQL 未启动、依赖缺失
ss -tlnp | grep 8081   # 检查端口
systemctl status mysql  # 检查数据库
```

### 14.2 前端 404 / API 不通

```bash
# 确认 serve.py 在运行
ps aux | grep serve.py
# 确认后端可达
curl -s http://127.0.0.1:8081/api/v1/health
# 确认 Nginx 配置
nginx -t
```

### 14.3 NiFi 任务不执行

```bash
# 检查容器
docker ps | grep iot-nifi
docker logs iot-nifi --tail 50
# 检查 flow 状态
docker exec iot-nifi ls /opt/nifi/nifi-current/data/iot/inbox_csv/
# 检查 worker 脚本
docker exec iot-nifi ls /opt/nifi/nifi-current/data/iot/bin/
```

### 14.4 AI 训练失败

```bash
# 查看 AI 后端日志
tail -100 /home/iot/solo_ai_iot/banckend/logs.log
# 检查 GPU
nvidia-smi
# 检查训练文件 meta.json
cat /home/iot/{user}/nifi-data/pima_diabetes/pima_train.meta.json
```

### 14.5 常见错误对照

| 现象 | 原因 | 解决 |
|------|------|------|
| 401 登录失败 | 密码错误/账号锁定 | 检查 MySQL 用户表 |
| 403 无权限 | 非本人文件 | 确认登录用户 |
| 423 锁定 | 失败次数过多 | 等待或重置 locked_until |
| 上传空文件 | 断连/权限 | 检查磁盘空间、目录权限 |
| 打标无结果 | tag_rule 配置错误 | 检查 meta.json 的 tagColumn |

---

## 15. 上线检查清单

### 15.1 部署前

- [ ] 服务器硬件满足要求（含 GPU）
- [ ] 系统已安装依赖
- [ ] 代码已从 GitHub 拉取最新
- [ ] MySQL 已初始化 + 强密码
- [ ] 所有安全项已修复（见第 8 章）
- [ ] `IOT_SECRET_KEY` 已设置为强随机值
- [ ] TLS 证书已就绪

### 15.2 部署中

- [ ] 后端 systemd 服务已安装并启动
- [ ] 前端 serve.py 已启动
- [ ] AI 前后端已启动
- [ ] NiFi 容器已启动
- [ ] Nginx 已配置 HTTPS
- [ ] 自检命令全部通过

### 15.3 部署后

- [ ] admin 登录成功
- [ ] 上传/转换/打标/导出全流程可用
- [ ] AI 训练/部署/预测全流程可用
- [ ] 备份脚本已配置并测试
- [ ] 监控已启用
- [ ] 防火墙仅开放 443
- [ ] 用户数据目录权限正确

---

> 文档结束。如有部署问题，请联系系统管理员。