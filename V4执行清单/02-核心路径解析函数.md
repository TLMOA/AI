# 02 — 核心路径解析函数

> 依赖：01-数据库模型与用户注册
> 状态：✅ 已实现
> 涉及文件：`v1-backend/app/main.py`

---

## 一、factory_id → username 统一

### 改动范围

| 文件 | 涉及字段/变量 | 改动 |
|------|-------------|------|
| `db_models.py` | `ExportJobModel.factory_id` | 改为 `username`（保留旧列做 fallback） |
| `main.py` | `DEFAULT_FACTORY_ID` | 废弃，改为从 JWT 获取当前用户名 |
| `main.py` | `BACKEND_MODE_STATE["factory_id"]` | 改为 `"username"` |
| `main.py` | `_normalize_factory_id()` | 改为 `_normalize_username()` |
| `main.py` | 所有 API 的 `factory_id` 参数 | 改为 `username` |
| `export_worker.py` | `factory_id` 变量 | 改为 `username` |
| `silent_export_worker.py` | `tenant` 参数 | 保持 `tenant` 不变（语义已是用户名） |

### 兼容策略
- 数据库 `ExportJobModel` 保留 `factory_id` 列，新增 `username` 列
- 读取时优先使用 `username`，fallback 到 `factory_id`
- 历史数据 `factory-001` 保持不变，新数据写入 `username`

---

## 二、核心路径解析函数

### 2.1 `_resolve_user_storage_root(username)`

```python
def _resolve_user_storage_root(username: str) -> Path:
    """根据用户部署模式返回存储根路径，自动创建子目录。
    
    公有化: /home/yhz/{username}/
    私有化: {ceph_endpoint}/
    """
    session = SessionLocal()
    try:
        user = session.query(IotUser).filter(IotUser.username == username).first()
        if user and user.deployment_mode == "private" and user.ceph_endpoint:
            root = Path(user.ceph_endpoint)
        else:
            root = IN_DATA_BASE_DIR / username
        (root / "nifi-data").mkdir(parents=True, exist_ok=True)
        (root / "real_nifi_data").mkdir(parents=True, exist_ok=True)
        return root
    finally:
        session.close()
```

### 2.2 `_get_user_nifi_dir(username, sub)`

```python
def _get_user_nifi_dir(username: str, sub: str = "") -> Path:
    """返回用户 nifi-data 下的指定子目录，自动创建。"""
    root = _resolve_user_storage_root(username) / "nifi-data"
    if sub:
        root = root / sub
    root.mkdir(parents=True, exist_ok=True)
    return root
```

### 2.3 `_get_user_real_nifi_dir(username, sub)`

```python
def _get_user_real_nifi_dir(username: str, sub: str = "") -> Path:
    """返回用户 real_nifi_data 下的指定子目录，自动创建。"""
    root = _resolve_user_storage_root(username) / "real_nifi_data"
    if sub:
        root = root / sub
    root.mkdir(parents=True, exist_ok=True)
    return root
```

### 2.4 `_get_user_upload_dirs(username)`

```python
def _get_user_upload_dirs(username: str) -> Dict[str, Path]:
    """返回用户隔离的上传/转换目录，自动创建。"""
    nifi = _get_user_nifi_dir(_normalize_username(username))
    dirs = {
        "inbox_csv": nifi / "inbox_csv",
        "inbox_json": nifi / "inbox_json",
        "inbox_tsv": nifi / "inbox_tsv",
        "csv_to_json": nifi / "csv_to_json",
        "csv_to_tsv": nifi / "csv_to_tsv",
        "json_to_csv": nifi / "json_to_csv",
        "json_to_tsv": nifi / "json_to_tsv",
        "tsv_to_json": nifi / "tsv_to_json",
        "tsv_to_csv": nifi / "tsv_to_csv",
    }
    for d in dirs.values():
        d.mkdir(parents=True, exist_ok=True)
    return dirs
```

---

## 三、全局常量保留作 fallback

| 常量 | 保留方式 |
|------|---------|
| `NIFI_BASE_DIR` | 保留为默认值，标记 `# deprecated` |
| `NIFI_REAL_BASE_DIR` | 保留为默认值，标记 `# deprecated` |
| `IN_DATA_BASE_DIR` | 保留（根目录不变，子目录按用户） |
| `NIFI_OUTPUT_DIR` 等 16 个常量 | 保留但不再直接使用，所有调用改为通过 `_get_user_nifi_dir()` |

---

## 四、路径解析示例

| 用户 | deployment_mode | ceph_endpoint | nifi-data 路径 | real_nifi_data 路径 |
|------|:---:|---|------|------|
| `zhangsan` | public | (空) | `/home/yhz/zhangsan/nifi-data/` | `/home/yhz/zhangsan/real_nifi_data/` |
| `wangwu` | private | `/home/yhz/wangwu` | `/home/yhz/wangwu/nifi-data/` | `/home/yhz/wangwu/real_nifi_data/` |
| `admin` | private | `/home/yhz/admin` | `/home/yhz/admin/nifi-data/` | `/home/yhz/admin/real_nifi_data/` |